# PRD: Enterprise GPT Frontend Rebuild

## 1. Overview

**Problem.** The `enterprise-ui` client has drifted past the point where migration pays. The API has grown projects, conversation favorites, a document ingest and download pipeline, model and MCP catalogs, admin user management, and — as of the current working tree — a fully framed assistant-status stream. The client consumes almost none of it, holds 17 writable signals in a single root service across four unrelated domains, has zero tests, and renders model output through `bypassSecurityTrustHtml` with `html: true`. It is also **already broken**: `docs/conversations/streaming-contract.md` §7 records that the framed SSE release breaks `ConversationService.streamChat` knowingly and with the user's agreement, so the current client appends literal `data: {"kind":"TextDelta",…}` frames into chat bubbles.

**Solution.** Delete `enterprise-ui/` and regenerate the client at `enterprise-gpt-ui/` from `ng new` on Angular 21.2.19 zoneless, with `@ngrx/signals` 21.1.1, Bootstrap 5.3, MSAL v6, and Vitest. Nothing is migrated. The rebuilt client covers chat with a live chronological activity timeline, projects, documents, and an admin area — and every backend capability that is missing today is either delivered as an `[enabler]` story in this document or shipped as a visible unavailable state, never as mock content.

**Success criteria.**

- **Streaming fidelity**: all eight `AssistantUiEventKind` values fold correctly, measured by unit tests over recorded event fixtures in `domain/stream/`, including a fixture where one `data:` frame is split across two `read()` chunk boundaries. Target: 100% of the eight kinds covered, 0 failures.
- **Content safety**: an assistant response containing `<img src=x onerror=alert(1)>`, `<script>`, and `javascript:` URLs renders as inert text with no script execution, measured by unit tests over the `ngx-markdown` pipeline plus a lint rule that fails the build if `bypassSecurityTrustHtml` appears anywhere under `src/`. `ngx-markdown` performs the trust internally, so application code needs no call site of its own. Target: 0 call sites, 0 executions.
- **No inherited defects**: the four known defects of the old client do not reappear, measured by four named regression tests — conversation search sends `name=` and filters; `mcpServerIds` restores the MCP selection on reopen; a rename echoes `projectId` and does not unlink the conversation; a production build resolves the API base URL from `config.json` and never from a compiled-in `localhost:7045`. Target: 4 of 4 passing.
- **Accessibility**: 0 serious or critical axe-core violations on the chat, projects, documents, and admin routes in both themes, measured by the automated axe run added in US-1405.
- **Capability honesty**: every feature with no backing API renders `UnavailablePanel`, measured by a test asserting that `DocumentLibraryStore` and `ReportsStore` expose no seeded or placeholder records. Target: 0 mock records rendered anywhere in the app.

A signed-in user lands on an empty prompt box, types, and watches the answer arrive alongside a chronological list of what the assistant is doing — reasoning, a function call, an MCP tool, an agent and its children — each card labelled with its display name and a separate kind badge. The conversation is created on that first prompt without remounting the page, the server names it out of band, and the name appears in the sidebar a moment later. The user can stop mid-answer and keep the partial text as a detached card that says plainly it was not saved. Their conversations group into projects; their uploads carry staged progress; administrators manage users, models, and MCP servers from deep-linkable tabs.

## 2. Goals & non-goals

**Goals.**

- Replace `enterprise-ui/` with a client that consumes the current API surface in full, including the framed SSE stream that the existing client cannot read.
- Render the assistant's work — reasoning, function calls, MCP tool calls, sub-agent calls — as a chronological timeline built on the contract the server already emits, not on a protocol invented for this project.
- Close the eleven backend capability gaps that block requested features, each as an `[enabler]` story inside the feature epic it unblocks, so the Documents screen, response feedback, usage reports, and conversation export have a path to working rather than a permanent placeholder.
- Make every list screen honest about what the API can and cannot do, rather than offering sort and filter controls the server does not support.
- Establish a state layer on `@ngrx/signals` that new features extend by composition, replacing the single mutable root service the old client grew.
- Ship dark and light theming, three responsive breakpoints, and keyboard and screen-reader support as acceptance criteria on the stories that introduce each surface, not as a cleanup pass.

**Non-goals.**

- Migrating any code, component, style, or test from the current `enterprise-ui/`. The directory is deleted; `docs/design/` — the Claude Design handoff bundle — is the surviving UI reference, and its six boards are the visual authority for every screen in this document.
- Server-side rendering, hydration, or a PWA/offline mode.
- Changing the SSE wire format, the `Andes.Extensions.AI.UI` event contract, or the RFC 9457 problem type URIs — those are opaque identifiers clients match verbatim, and changing one is a breaking API change.
- Resumable streams, `Last-Event-ID`, or reconnect-and-continue. The stream is explicitly not resumable (streaming contract §6.3); a dropped connection cancels the turn.
- Persisting or replaying the activity tree for past turns. Only the answer text is transcribed (streaming contract §6.2); reopening a conversation replays the answer, never the work that produced it.
- Virtualizing the transcript. Transcripts are bounded by the Cosmos 2 MB item cap, and a virtual scroller fights the streaming tail and breaks in-page find.
- Real-time collaboration, multi-user conversations, or conversation sharing between users.
- Adding EF Core migrations. `Repository/Migrations/` is empty and `Database.Migrate()` runs at startup; the enabler stories here add only columns and endpoints that the existing `EnsureCreated`/`Migrate` path already covers, and the first real migration is flagged separately.

## 3. Users & access

**Personas.**

- **Chat user**: a signed-in employee who holds conversations with the model, uploads documents into them, groups them into projects, and selects models and MCP tool servers per turn. The default persona; every user is one.
- **Administrator**: a chat user who additionally holds the `Administrator` permission (`a0b1c2d3-e4f5-4a6b-8c7d-9e0f1a2b3c4d`) and manages the user directory, the model catalog, the MCP server catalog, and usage reports.
- **Backend engineer**: owns the `[enabler]` stories in this document — the eleven API capabilities the frontend features depend on. Works in `enterprise-gpt-api/` under `.claude/rules/csharp.md` and `aspnet-rest-apis.md`.
- **Frontend engineer**: owns every non-enabler story, working in the regenerated `enterprise-gpt-ui/` under the `ngrx-signal-store` and `angular-developer` skills.

**Role-based access.**

- **Anonymous**: may reach `/auth` and `/login-failed` only. Every other route is behind `MsalGuard` plus `sessionGuard`.
- **Chat user**: full access to their own conversations, projects, and documents. Ownership is enforced server-side and a resource belonging to another user returns 404, not 403, so the client must never treat 404 as "deleted" in a way that leaks existence.
- **Chat user with `Upload File`** (`b1c2d3e4-f5a6-4b7c-8d9e-0f1a2b3c4d5e`): may upload into conversations and projects. Seeded as a default grant, but it is revocable and **administrators are not implicitly granted it** — the client checks it explicitly before rendering any upload affordance.
- **Administrator**: additionally reaches `/admin` and its four tabs. The admin route chunk is guarded with `canMatch` so a non-admin never downloads it. Two server guardrails return 400 and need friendly copy: an administrator cannot revoke their own `Administrator` permission, and cannot deactivate themselves.
- **MCP-derived permissions**: each MCP server may carry a `PermissionId`; `GET /api/mcps` returns only the servers the caller is permitted, so the MCP picker needs no client-side filtering.

## 4. Functional requirements

| ID    | Requirement                                                                                                                                                          | Priority | Epic(s)            |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------ |
| FR-1  | Users authenticate with Entra ID through MSAL redirect; no application route renders before an active account exists                                                 | P0       | EP-2               |
| FR-2  | Sign-in bootstraps through `POST /api/users/me`, then loads permissions, models, permitted MCP servers, conversations, and projects                                  | P0       | EP-2               |
| FR-3  | The admin route chunk is never downloaded by a user without the `Administrator` permission                                                                           | P0       | EP-2               |
| FR-4  | Upload affordances render only when the caller holds `Upload File`, checked explicitly and never inferred from `Administrator`                                       | P0       | EP-2, EP-8         |
| FR-5  | A collapsible sidebar lists conversations and persists its collapsed state across reloads                                                                            | P0       | EP-3               |
| FR-6  | Sidebar conversation rows support rename, favorite/unfavorite, soft-delete, and project assignment/unassignment                                                      | P0       | EP-3               |
| FR-7  | "New conversation" opens an empty composer; the conversation is created only on the first prompt, without remounting the page                                        | P0       | EP-3, EP-4         |
| FR-8  | The composer offers file and image attachment, a model picker, an MCP picker with a switch and brand mark per server, voice input, and a send/stop control            | P0       | EP-4, EP-8         |
| FR-9  | MCP selection is disabled when the selected model reports `isToolEnabled === false`                                                                                  | P0       | EP-4               |
| FR-10 | The client parses the framed SSE stream from `POST /api/conversations/{id}/stream` and renders answer text incrementally                                             | P0       | EP-4               |
| FR-11 | Stop aborts the request; the partial answer is retained outside the transcript, labelled as not saved, with Copy and Retry                                           | P0       | EP-4               |
| FR-12 | A stream that ends without a `Finished` event is reported as an incomplete turn, since that is the only truncation signal available                                  | P0       | EP-4               |
| FR-13 | A 409 `conversation-busy` is surfaced as a retryable warning and never auto-retried                                                                                  | P1       | EP-4               |
| FR-14 | The server-generated conversation title appears after the first turn without a manual refresh                                                                        | P1       | EP-4               |
| FR-15 | Reopening a conversation restores its last model and MCP server selection from `ConversationDetailDto`                                                               | P1       | EP-4               |
| FR-16 | _Retired (US-414/US-415)._ Tool servers are provisioned for users, so there is no interactive per-user consent state and the API emits no consent problem. The number stays retired rather than reused, since other documents cite it | —        | —                  |
| FR-17 | Reasoning, function calls, MCP tool calls, and sub-agent calls render as separate cards in the order they happened, interleaved with answer text                     | P0       | EP-5               |
| FR-18 | Child activities render nested inside their parent card; sub-status lines, duration, and failure state are shown                                                     | P1       | EP-5               |
| FR-19 | Per-turn token usage from the `Finished` event is displayed                                                                                                          | P1       | EP-5               |
| FR-20 | Model output is rendered as markdown with raw HTML disabled and the result sanitized before insertion                                                                | P0       | EP-6               |
| FR-21 | Code blocks render with theme-matched syntax highlighting and a copy control                                                                                         | P1       | EP-6               |
| FR-22 | Diagrams and math render on demand and are absent from the initial bundle                                                                                            | P2       | EP-6               |
| FR-23 | The transcript stays pinned to the newest content unless the user scrolls up, in which case a jump-to-latest control appears                                         | P0       | EP-6               |
| FR-24 | A user can copy their own prompt and the assistant's response                                                                                                        | P1       | EP-6               |
| FR-25 | The conversations screen supports search by name, offset pagination, favorites filtering, and multi-select bulk delete                                               | P1       | EP-7               |
| FR-26 | No list screen offers a sort control the API cannot honor; where sorting is offered over a truncated set, the truncation is stated                                   | P1       | EP-7, EP-9         |
| FR-27 | Files attach to a conversation or project and their ingest progress is polled on a staged schedule                                                                   | P1       | EP-8               |
| FR-28 | An expired or unknown upload job renders differently from a failed one                                                                                               | P1       | EP-8               |
| FR-29 | Document download fetches the signed URL on click only; the URL is never prefetched, persisted, logged, or placed in a router URL                                    | P1       | EP-8               |
| FR-30 | The projects screen supports search by name, rename, description editing, and soft-delete                                                                            | P1       | EP-9               |
| FR-31 | Project detail edits instructions, adds and removes project files, starts a conversation in the project, and lists that project's conversations with per-row actions | P1       | EP-9               |
| FR-32 | Projects can be favorited and favorited projects appear in the sidebar with their linked conversations                                                               | P2       | EP-9               |
| FR-33 | A documents screen lists documents uploaded to any conversation, filterable by name and groupable by conversation                                                    | P1       | EP-10              |
| FR-34 | Documents created by the model are distinguishable and filterable                                                                                                    | P2       | EP-10              |
| FR-35 | An assistant response can be rated thumbs up or thumbs down, anchored to a stable message identity                                                                   | P2       | EP-11              |
| FR-36 | Admin users tab supports paginated search over name and email, create, update, deactivate, and permission-set editing                                                | P1       | EP-12              |
| FR-37 | Admin users can be filtered by permission                                                                                                                            | P2       | EP-12              |
| FR-38 | Admin models tab supports add, update, soft-delete, and client-side search by provider and model name                                                                | P1       | EP-12              |
| FR-39 | Admin MCP tab supports full CRUD over the administrative MCP server representation, including the brand mark a server carries                                                                                   | P1       | EP-12              |
| FR-40 | Admin tabs are deep-linkable routes, each lazy-loading its own store                                                                                                 | P1       | EP-12              |
| FR-41 | Reports are fetched on every entry to the tab and never served from cache                                                                                            | P2       | EP-13              |
| FR-42 | The app supports dark and light themes with no light-mode frame painted before a dark-mode load                                                                      | P0       | EP-1               |
| FR-43 | Configuration is fetched at runtime before bootstrap; no environment value is compiled into the bundle                                                               | P0       | EP-1               |
| FR-44 | All ten RFC 9457 problem types the API emits are typed and handled, including `provider-not-configured` and `storage-not-configured`                                 | P0       | EP-1               |
| FR-45 | The app is zoneless and every component uses `OnPush`                                                                                                                | P0       | EP-1               |
| FR-46 | The initial bundle stays within budget and the build fails if the diagram or math chunk enters it                                                                    | P0       | EP-1               |
| FR-47 | Every interactive surface is operable by keyboard, streaming output is announced to assistive technology, and motion honors `prefers-reduced-motion`                 | P0       | EP-14              |
| FR-48 | The app is usable at ≥1024px, 768–1023px, and <768px with the sidebar behavior defined per breakpoint                                                                | P0       | EP-14              |
| FR-49 | Every capability with no backing API renders a visible unavailable state and no placeholder data                                                                     | P0       | EP-1, EP-10, EP-13 |
| FR-50 | A conversation can be exported to Markdown, Word, or PDF, covering prompts and completed answers only                                                                | P2       | EP-15              |
| FR-51 | Fonts, icons, brand assets, and Bootstrap are self-hosted from npm; the app issues no third-party network request at runtime                                         | P0       | EP-1               |

## 5. User experience

**Entry points & first-time flow.** The user opens the app URL and is redirected to Entra ID. On return, `/auth` completes the MSAL redirect, `sessionGuard` resolves the session by calling `POST /api/users/me`, and the shell renders with the sidebar populated. A first-time user has no conversations, so the sidebar shows an empty state and the chat route shows the centered wordmark, the "How can I help you today?" subtitle, and four suggested prompt chips, per frame `1a` in `01 Chat and Streaming.dc.html`.

**Core experience.**

1. The user types in the composer, optionally attaches files, picks a model, and toggles MCP servers.
2. Send posts to the stream endpoint. On a brand-new conversation the client first creates it, then updates the URL in place via `Location.replaceState` — a single `UrlMatcher` serves both `/chat` and `/chat/:conversationId` so the page does not remount and composer state survives.
3. The answer streams in. Between request-accepted and the first activity or text delta the timeline is empty, because this application emits no request-level `Status` events of its own (streaming contract §9) — that gap is filled by a thinking indicator, not by a fabricated status line.
4. Activity cards appear in arrival order interleaved with text. Each card shows `displayName` as its label with `kind` as a separate badge, `source` as a subtitle, `subStatuses[]` beneath, children nested, and `durationSeconds` on completion. **One carve-out, added after `Andes.Extensions.AI.UI` 0.7.0**: an `McpTool` activity's `displayName` is the raw tool name the model called, prefixed with the server it was leased from, so the client strips that prefix and reads the rest as words before rendering it — the server itself is already the subtitle (see the note under US-406).
5. `Finished` ends the turn, carrying `usage`. The answer is re-rendered once, canonically, with full syntax highlighting.
6. On a first turn, the server names the conversation out of band and does not send the name down the stream, so the client refetches `GET /api/conversations/{id}` and the sidebar row updates.

**Edge cases & UI states.**

- **Empty**: no conversations, no projects, no documents, no search results — each surface has a designed empty state, distinct from a loading skeleton.
- **Loading**: skeleton rows for lists; a thinking indicator for the pre-first-delta window; per-row spinners that do not disable the rest of the list.
- **Stopped**: partial answer as a visually detached card reading "Stopped — not saved to this conversation", with Copy and Retry. Dropped on reopen, route change, and refresh. The optimistic user bubble rolls out too, because a cancelled turn transcribes neither the answer nor the prompt.
- **Truncated**: a body that ends with no `Finished` event. Indistinguishable from a network drop after the first frame, so it renders as "This response was cut off" with Retry — never as a completed answer.
- **Busy**: 409 `conversation-busy` renders a transient warning with a retry affordance and no automatic retry.
- **Unavailable**: `UnavailablePanel` for every capability-gated feature, naming what is missing and showing no data.
- **Upload expired**: a 404 on `GET /api/documents/upload-status/{jobId}` after the retention window means expired or unknown, and reads differently from a `Failed` state.
- **Fatal**: a failed `config.json` fetch renders a static fatal shell in `index.html` rather than a blank page.

**UI/UX highlights.** The visual authority is `docs/design/` — six boards, five shared `.dc.html` components, and `theme.css`. Bootstrap 5.3 utilities carry the layout; custom CSS is confined to the token layer below.

- **Tokens.** The CSS custom properties in `docs/design/project/theme.css` are copied verbatim into `src/styles/_tokens.scss`: the Bootstrap overrides (`--bs-body-bg`, `--bs-body-color`, `--bs-border-color`, `--bs-secondary-color`, `--bs-link-color`, `--bs-link-hover-color`), the surfaces (`--surface`, `--surface-2`), the semantics (`--muted`, `--brand`, `--accent`, `--ok`, `--warn`, `--warn-bg`, `--warn-border`, `--fail`), the chat set (`--bubble`, `--bubble-fg`, `--think-bg`, `--active-bg`, `--ring`), the code set (`--code-bg`, `--code-head`, `--code-fg`), the primary-button set (`--btnP-bg`, `--btnP-fg`, `--btnP-hover`), and the asset-swap pair (`--show-light`, `--show-dark`). `--brand` deliberately flips from Summit Navy `#14324F` in light to Glacier Blue `#21A8D8` in dark.
- **Theme attribute.** `[data-bs-theme="light"|"dark"]` on `<html>`, set by the pre-paint inline script. This is Bootstrap 5.3's own attribute, so its components theme for free and no bridging layer exists.
- **Typography.** Montserrat 600/700/800 for headings and KPI numbers, Inter 400/500/600 for body and UI, JetBrains Mono 400/500/600 for token counts, durations, ids, deployment names, and trace ids. Self-hosted subsetted `woff2` under `public/fonts`, `font-display: swap`, preloaded.
- **Bootstrap.** Consumed from the npm package's SCSS source and imported module by module in `styles.scss`, so the initial-bundle budget stays meaningful. **None of Bootstrap's JavaScript ships.** Modal and offcanvas are the native `<dialog>` with `showModal()`; the dropdown menu and the tooltip are hand-rolled `position: fixed` layers. Measured, Bootstrap's `Modal`, `Offcanvas`, `Dropdown`, and `Tooltip` cost 59 kB of JavaScript and 27 kB of CSS — `Dropdown` and `Tooltip` each bundle Popper — against ~3.9 kB for the native replacements, and `<dialog>`'s top layer inerts the page for real and supplies `aria-modal` and a cancellable Escape, none of which Bootstrap's `focusin`-cycling focus trap matches. No global script, and no third-party origin at runtime.
- **Icons.** Bootstrap Icons from npm, built into a single SVG sprite covering the 74 glyphs the app uses — the 73 distinct `bi-*` names the boards reference, plus `bi-file-earmark` as the attachment chip's fallback for an unrecognised file extension; markup is `<svg><use href="#bi-…"/></svg>`, each icon carrying `aria-hidden="true"` or `role="img"` with a label. The full icon font is not shipped.
- **Brand.** The Andes wordmark and mark, light and dark variants, swapped by `--show-light`/`--show-dark` with no JavaScript. The ridgeline motif is the sidebar divider, the thinking indicator, and the empty-state illustration.
- **Motion.** Exactly four keyframes, all defined in `theme.css`: `blink` (streaming caret, `1s steps(1)`), `spin` (`.9s linear`), `ringpulse` (jump-to-latest control and voice recording, `1.8s ease-out`), and `ridgedash` (thinking indicator, `1.5s linear`).
- **Layout.** The conversation column is capped at 820px and centred; the chat header bar is 52px; the sidebar is 260px expanded and a 60px icon strip collapsed.
- **Toasts.** Top-right at `top: 76px; right: 20px`, 8px stacking gap, a 4px left type accent and an icon for one of success, error, warning, or info. **Success auto-dismisses after 5 seconds with a draining 3px edge indicator; a failure persists until the user dismisses it** — an error that disappears before it is read is a defect (frame `4l`).
- **Transitions.** One set, stated once here and referenced rather than restated elsewhere: 150ms for hover and colour changes, 200ms for the sidebar width, 200ms for message appearance. Every transition and all four keyframes are suppressed under `prefers-reduced-motion: reduce`.
- **Accessibility precedence.** Where the boards and this document disagree on accessibility, this document wins. The prototypes carry known gaps — no `aria-live` region for streaming text or toasts, icon-only controls with no accessible name, and clickable `<i>` elements — which EP-14 corrects rather than reproduces.

## 6. Technical considerations

**Integration points.** All real, verified against the working tree.

| Concern                                                          | Where                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chat turns, transcript, conversation list, favorite, bulk delete | `Enterprise.Gpt.Api/Endpoints/ConversationEndpoints.cs` — a minimal-API module. There is no `ConversationsController`; the last MVC controller was removed                                                                                                                                                                                                                                                                                           |
| SSE framing and streaming headers                                | `ConversationEndpoints.StreamConversationAsync`, documented end to end in `docs/conversations/streaming-contract.md`                                                                                                                                                                                                                                                                                                                                 |
| Event type and TypeScript mirror                                 | `Andes.Extensions.AI.UI` 0.5.0; contract at `~/.nuget/packages/andes.extensions.ai.ui/0.5.0/typescript/andes-assistant-ui.ts`                                                                                                                                                                                                                                                                                                                        |
| Projects, project documents                                      | `Enterprise.Gpt.Api/Endpoints/ProjectEndpoints.cs`                                                                                                                                                                                                                                                                                                                                                                                                   |
| Upload, upload status, download, supported extensions            | `Enterprise.Gpt.Api/Endpoints/DocumentEndpoints.cs`                                                                                                                                                                                                                                                                                                                                                                                                  |
| Model catalog                                                    | `Enterprise.Gpt.Api/Endpoints/ModelEndpoints.cs` — `GET ""` for permitted, `GET "all"` for admin                                                                                                                                                                                                                                                                                                                                                     |
| MCP catalog                                                      | `Enterprise.Gpt.Api/Endpoints/McpEndpoints.cs` — `GET ""` returns `McpDto`, `GET "all"` returns `McpServerDto`                                                                                                                                                                                                                                                                                                                                       |
| Users, self-provisioning                                         | `Enterprise.Gpt.Api/Endpoints/UserEndpoints.cs` — `POST me` is the bootstrap call                                                                                                                                                                                                                                                                                                                                                                    |
| Permissions and grants                                           | `Enterprise.Gpt.Api/Endpoints/PermissionEndpoints.cs`                                                                                                                                                                                                                                                                                                                                                                                                |
| Problem type URIs                                                | `Enterprise.Gpt.Api/Problems/ProblemTypes.cs`                                                                                                                                                                                                                                                                                                                                                                                                        |
| Usage audit data                                                 | `ConversationUsage`, `ConversationUsageToolCall`; documented in `docs/conversations/usage-and-favorites.md`                                                                                                                                                                                                                                                                                                                                          |
| Design reference                                                 | `docs/design/` — the Claude Design handoff bundle: boards `00`–`06`, the five shared components (`Sidebar`, `Composer`, `Transcript`, `ReportsDashboard`, `AdminNav`), `theme.css`, and the brand assets. Served with `node docs/design/serve.mjs` (port 4300) or the `serve-design` VS Code task; it **must** be served over HTTP, because `file://` is an opaque origin and every `<dc-import>` fetch fails, rendering the shared components empty |

The response envelope for every paginated list is `PaginatedResponseDto<T>`: `{ items, totalCount, pageSize, currentPage, totalPages }`, with `take` clamped server-side to 1–100.

Two notes on consuming the design bundle. First, its third-party dependencies — React and ReactDOM from unpkg, Bootstrap and Bootstrap Icons from jsDelivr, and the three families from Google Fonts — are **prototype-only**. They exist so the boards render in a browser and must not appear in the application, which self-hosts all four from npm per FR-51. Second, `04 Libraries.dc.html` lines 106–107 carry a truncated `<span style="font-size` tag, so frame `4c`'s caption renders as a literal style string; it is a defect in the prototype markup, not a design instruction, and must not be reproduced.

**Data storage & privacy.** Conversation message history lives in Cosmos DB partitioned by `/userId` via the raw `CosmosClient`; everything else is SQL Server 2025 with soft delete on nullable `DateDeactivated` and no query filters. Client-side, `localStorage` holds only the theme and the sidebar collapsed state — B3 (US-909) landed before US-910 built anything, so the device-local project pins this sentence once anticipated were never written; favourite projects are a server flag instead, held in memory like every other project field. No token, conversation content, or document metadata is written to `localStorage`. The stream carries no prompt content, tool arguments, or tool results by design (streaming contract §6.1), so nothing the timeline renders is user input echoed back. `DocumentDownloadDto.downloadUrl` is a bearer credential with roughly a five-minute TTL: fetched on click only, never prefetched for a list, never persisted, never logged, never placed in a router URL. The download route already sends `Cache-Control: no-store` and signs `Content-Disposition`, so an `<a download>` is sufficient.

**Security.**

- One functional `authInterceptor` over one `TokenService`. `MsalInterceptor` and `withInterceptorsFromDi()` are not used, because the raw `fetch` that reads the stream bypasses `HttpClient` interceptors entirely and both paths must share one token source.
- Markdown rendering is `ngx-markdown` over **marked**, and marked has no `html: false` equivalent — parser-level raw-HTML suppression is simply not available. The two-layer posture is restored by (a) DOMPurify behind `ngx-markdown`'s `SANITIZE` provider with an explicit `ALLOWED_TAGS`/`ALLOWED_ATTR` profile and `ALLOW_DATA_ATTR: false`, and (b) a `MarkedRenderer` override that drops raw `html` tokens outright, so unsafe markup never reaches the sanitizer in the first place. `ngx-markdown` performs the `bypassSecurityTrustHtml` internally, so application code has **zero** call sites, pinned by a `no-restricted-syntax` lint rule over all of `src/`. Model output is attacker-influenceable through uploaded documents and MCP tool results, so this remains the primary trust boundary in the client.
- Stores hold text, never `SafeHtml`. Sanitized HTML is a view concern; keeping it in state is unserializable and doubles transcript memory.
- `/problems/mcp-authorization-required` arrives as 403 and must never enter the token-refresh path.
- Retry only idempotent GETs, only on 502/503/504, with jittered backoff. Never auto-retry 409.
- MSAL is a two-major jump from the old client (`msal-angular` 4 → 6, `msal-browser` 4 → 5); the configuration is written against the current API rather than ported. **Settled by US-201 (2026-08-10): `@azure/msal-angular` is not used at all** — its `MsalGuard.canMatch()` answers a signed-out user with `false` instead of redirecting, which is the one guard shape US-203 needs; `MsalService` replays its cached redirect hash into every later handshake, which can bounce a _successful_ sign-in to `/login-failed`; and `MsalBroadcastService` routes events through `NgZone.run`, a no-op under zoneless. The client sits directly on `@azure/msal-browser`.

**Scalability & performance.**

- Decoded stream events are buffered on a ~16ms budget and folded as one batch, so a turn produces at most one `patchState` per frame regardless of token rate. `bufferTime` rather than `requestAnimationFrame`, because the former is deterministic under test without a browser scheduler.
- Streaming text lives in one string on `TurnStore` and is appended to the transcript exactly once, on completion — a chunk must not cost O(transcript).
- Markdown rendering splits head/tail at the last block boundary (`lastIndexOf('\n\n')`, or the fence open inside an unclosed fence), memoizes the head by length, re-renders only the tail per flush, and skips highlighting on the tail. The single canonical full render happens on `Finished`. `ngx-markdown`'s `<markdown [data]>` re-renders its whole input on every change, so the split is expressed as two `<markdown>` instances — a stable head and a volatile tail — rather than one bound to the accumulating string, which would blow the ~16ms budget on a long answer.
- Upload polling escalates 500ms → 1s → 2s → 4s, capped at 5s. Server progress is monotonic; a local value never moves backwards.
- Auto-scroll uses an `IntersectionObserver` on a zero-height bottom sentinel with `rootMargin: '0px 0px 80px 0px'`, matching frame `1b`'s jump-to-latest threshold — not a `(scroll)` binding, which would dirty the component on every scroll event. The transcript sets `overflow-anchor: none` and `overscroll-behavior: contain`.
- No endpoint accepts a sort parameter today, so client-side sort is offered only over a fully materialised set. Four regimes apply: **A** full-set client sort after draining at `take=100` to a 500-item ceiling (projects, project conversations); **B** server order with no sort control (conversations list); **C** unpaginated, fully correct client sort and search (admin models, admin MCPs, via the `/all` routes); **D** server-paged with search and pagination only (admin users). Sorting only the loaded page is not an option — "Load more" would append a second sorted run underneath it.

**AI system requirements.** The client consumes the stream; it does not call a model directly. The evaluation surface is therefore the fold and the codec rather than model quality:

- `foldAssistantEvents` is vendored verbatim from the package and must not be reimplemented. A CI step diffs the vendored copy against `~/.nuget/packages/andes.extensions.ai.ui/0.5.0/typescript/andes-assistant-ui.ts` and fails on drift.
- Codec and timeline tests run against recorded frame fixtures with no backend: Node 20+ supplies `ReadableStream`, `TextDecoder`, `AbortController`, and `fetch` natively.
- Pass threshold: all eight event kinds folded correctly, an unknown `kind` ignored rather than thrown on, and a frame split across a chunk boundary reassembled — 100% of these cases green before the transport story is accepted.
- Activity labels are never pre-composed. `displayName` and `kind` are rendered separately; a string like "Calling Weather MCP" is a defect, because the contract keeps them apart precisely so the kind word is not repeated. That rule is unchanged by the 0.7.0 MCP carve-out: stripping a server prefix off `displayName` removes a repetition rather than composing one, and `kind` and `source` are still rendered as their own elements.

## 7. Epics & user stories

| ID    | Epic                                | Goal                                                                                  | Priority | Estimate | Depends on        |
| ----- | ----------------------------------- | ------------------------------------------------------------------------------------- | -------- | -------- | ----------------- |
| EP-1  | Foundation and application shell    | The app boots from runtime config, renders a themed shell, and fails safely           | P0       | L        | —                 |
| EP-2  | Sign-in and session                 | A user signs in with Entra ID and the client knows who they are and what they may do  | P0       | M        | EP-1              |
| EP-3  | Navigation and conversation sidebar | A user reaches and manages their conversations from a persistent sidebar              | P0       | M        | EP-2              |
| EP-4  | Chat composition and turn execution | A user sends a prompt and receives a streamed answer they can stop                    | P0       | L        | EP-1, EP-2        |
| EP-5  | Assistant activity timeline         | A user sees what the assistant is doing, chronologically, as it happens               | P0       | M        | EP-4              |
| EP-6  | Answer rendering and reading        | A user reads a safe, formatted answer and stays oriented while it streams             | P0       | M        | EP-4              |
| EP-7  | Conversation library                | A user finds, filters, and bulk-manages conversations on a dedicated screen           | P1       | M        | EP-3              |
| EP-8  | Attachments, upload, and download   | A user attaches files to a turn or project and retrieves them later                   | P1       | M        | EP-2, EP-4        |
| EP-9  | Projects                            | A user groups conversations, files, and standing instructions into projects           | P1       | L        | EP-3              |
| EP-10 | Documents library                   | A user browses every document they have uploaded, across conversations                | P1       | M        | EP-8              |
| EP-11 | Response feedback                   | A user rates an assistant response and the rating is recorded                         | P2       | M        | EP-4              |
| EP-12 | Administration                      | An administrator manages users, models, and MCP servers                               | P1       | L        | EP-2              |
| EP-13 | Usage reports                       | An administrator sees what the platform is spending and on what                       | P2       | L        | EP-12             |
| EP-14 | Access and conformance gates        | The app is operable by keyboard and screen reader, at every breakpoint, within budget | P0       | M        | EP-6, EP-9, EP-12 |
| EP-15 | Conversation export                 | A user takes a conversation out of the platform as Markdown, Word, or PDF             | P2       | L        | EP-6              |

EP-1 carries an unusually high proportion of `[enabler]` stories. That is a property of a greenfield rebuild from an empty directory rather than a decomposition failure: each enabler names the stories it unblocks, and none of them exceeds M.

### EP-1: Foundation and application shell

#### US-101: `[enabler]` Scaffold the zoneless Angular 21 workspace

- **Story**: Generate `enterprise-gpt-ui/` from `ng new` on Angular 21.2.19 with zoneless change detection and Vitest, so every later story is written against the target runtime rather than migrated onto it. Unblocks every story in this document.
- **Priority**: P0 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-09)
- **Acceptance criteria**:
  - Given the regenerated workspace, when `package.json` and the build and test `polyfills` are inspected, then `zone.js` appears in neither entry, and `provideZonelessChangeDetection()` is present in `app.config.ts`.
  - Given the workspace, when the `test` target is inspected, then it uses `@angular/build:unit-test` with the Vitest runner and `npm test` passes with zero specs.
  - Given the workspace, when dependencies are resolved, then `@ngrx/signals` 21.1.1, `@ngrx/operators`, `@azure/msal-angular` 6, `@azure/msal-browser` 5, `bootstrap` 5.3, `bootstrap-icons`, `ngx-markdown` 21.3.0 with `marked` and `prismjs`, and `dompurify` 3.4+ install with no peer-dependency warnings, and neither `@angular/animations` nor `@angular/platform-browser-dynamic` is present.
  - Given `ngx-markdown` declares `zone.js` as a peer dependency and npm installs it transitively, when the built chunks are searched, then `zone.js` appears in none of them — it is never imported and there is no `polyfills` entry, so the zoneless guarantee holds despite its presence in `node_modules`.
  - Given `tsconfig.json`, when compiled, then `strict`, `strictTemplates`, `noPropertyAccessFromIndexSignature`, and `noUncheckedIndexedAccess` are all enabled and the build produces zero errors.
  - Given `angular.json`, when inspected, then there is no global `scripts: [popper, bootstrap]` entry.

#### US-102: `[enabler]` Load configuration at runtime before bootstrap

- **Story**: Fetch `config.json` before `bootstrapApplication` so one build artifact is promoted across environments. Unblocks US-201 and US-405, and is the fix for the old client shipping a production build pointed at `localhost:7045`.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-09)
- **Acceptance criteria**:
  - Given a served app, when `main.ts` runs, then `config.json` is fetched with `cache: 'no-store'`, validated by a narrow assertion, and provided through an `APP_CONFIG` injection token before `bootstrapApplication` is called.
  - Given a `config.json` that is missing, unreachable, or fails validation, when the app loads, then the static fatal shell of frame `6d` renders — system fonts and inline styles only, because the app's theme, fonts, and brand assets have not loaded — with an explanatory message and a Refresh action, and the app does not bootstrap; a blank page is a failure of this criterion.
  - Given a production build, when the output is searched, then no environment-specific host appears in any compiled chunk and no `environment*.ts` or `fileReplacements` entry exists.
  - Given `config.json`, when read, then it carries the API base URL, the MSAL client and authority values, and the feature flags for the diagram and math chunks.

#### US-103: `[enabler]` Type and normalize every API error

- **Story**: Model the API's RFC 9457 responses as typed values so stores and forms consume one `AppError` shape. Unblocks every store that calls the API.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-09)
- **Acceptance criteria**:
  - Given the ten problem types in `ProblemTypes.cs`, when the client constants are compared, then all ten are present verbatim — `validation-error`, `upload-too-large`, `resource-not-found`, `forbidden`, `permission-required`, `conversation-busy`, `mcp-authorization-required`, `mcp-server-unavailable`, `provider-not-configured`, `storage-not-configured` — each under the relative base `/problems/`.
  - Given a problem body, when it is typed, then the extensions `maxBytes`, `permissions[]`, `serverName`, `providerId`, `traceId`, and `instance` are all reachable without a cast.
  - Given an `HttpErrorResponse`, a `fetch` `Response`, or an unknown thrown value, when `toAppError()` is applied, then the result is one discriminated `AppError` and the error toast's detail line shows the `traceId`.
  - Given a 400 carrying an `errors` dictionary keyed by property name, when `applyServerErrors(form, problem)` runs, then each key maps onto the matching reactive-form control's error state.
  - Given a 403 whose `type` is `/problems/mcp-authorization-required`, when the interceptor handles it, then no token refresh is attempted and the error is passed through unchanged.
  - Given a 502 on a GET, when it is retried, then backoff is jittered and capped; given a 409, when it is handled, then no automatic retry occurs.

#### US-104: `[enabler]` Build the reusable signal-store features

- **Story**: Write the five composable `@ngrx/signals` features every store depends on, with specs, before any store exists. Unblocks all 16 stores.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-09)
- **Acceptance criteria**:
  - Given `withRequestStatus`, when state is inspected, then status is one value across `idle | pending | fulfilled | { error }` so "loading and errored" is unrepresentable, and standalone `setPending()`, `setFulfilled()`, and `setError()` updaters exist.
  - Given `withOffsetPagination`, when applied against a `PaginatedResponseDto` envelope, then `skip`, `take`, `totalCount`, `hasMore`, and `isFullyLoaded` are derived correctly, including the boundary where `totalCount` is an exact multiple of `pageSize`.
  - Given `withPendingIds`, when one row's action is in flight, then only that row reports pending and the rest of the list stays interactive.
  - Given `withClientQuery`, when `isAuthoritative` is false, then sort is unavailable and the reason is exposed for the UI to render.
  - Given `withResetOnSignOut`, when `sessionEvents.signedOut` fires, then the composing store returns to its initial state.
  - Given all five, when reviewed, then `protectedState` is on, updaters are standalone `PartialStateUpdater` functions, feature order is `withState → withProps → withComputed → withMethods → withHooks`, and each has passing Vitest specs.

#### US-105: Switch between light and dark without a flash

- **Story**: As a user, I want the app to open in my chosen theme immediately so that I never see a light frame before a dark page.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-101, US-109
- **Status**: ✅ Done (2026-08-10)
- **Acceptance criteria**:
  - Given a stored dark-theme preference, when the app loads, then `data-bs-theme="dark"` is set on `<html>` by an inline script in `index.html` before first paint, and no light-mode frame is painted at any point during load — including on the signing-in interstitial of frames `6a` and `6f`, which renders before the app bootstraps.
  - Given no stored preference, when the app loads, then `prefers-color-scheme` decides the initial theme.
  - Given the sidebar footer theme control, when it is used, then the theme changes, the choice is written to `localStorage`, and it survives a reload.
  - Given `src/styles/_tokens.scss`, when it is compared against `docs/design/project/theme.css`, then the `[data-bs-theme=light]` and `[data-bs-theme=dark]` blocks carry the same custom properties with the same values, including `--brand` flipping from `#14324F` to `#21A8D8` — the difference frames `1b` and `1c` show side by side.
  - Given a theme change, when the wordmark or the mark is visible, then the light and dark image variants swap through `--show-light`/`--show-dark` alone — no script reads the theme to choose an asset.
  - Given a theme change, when a code block is visible, then it re-tints in the same frame as `data-bs-theme` flipping, because `src/styles/_prism.scss` maps Prism's token classes onto per-theme `--code-*` custom properties rather than importing a stylesheet. There is one code theme and it is dark in **both** themes by design — `theme.css` sets `--code-bg` to `#0B1F33` in light and `#081827` in dark and `--code-fg` to `#D7E3EE` in both, so a light Prism theme would render a surface the design does not have. No Prism stylesheet is imported, no `<link>` is toggled, and no script reads the theme; `prismjs` stays a dependency for tokenising and only its stylesheets go unused.

#### US-106: `[enabler]` Build the shared UI kit

- **Story**: Provide the wrapped Bootstrap primitives and state components every feature screen composes. Unblocks EP-3 and every screen thereafter.
- **Priority**: P0 · **Estimate**: L · **Depends on**: US-101, US-105, US-109
- **Status**: ✅ Done (2026-08-10)
- **Acceptance criteria**:
  - Given the kit, when inventoried against the boards, then modal, toast, tooltip, dropdown, offcanvas, paginator, debounced search input, data table with pager, floating bulk-action bar, project card, mobile card row, mobile pill sub-nav, status dot, kind badge, permission badge, source badge, attachment chip, empty state, skeleton, error panel, and `UnavailablePanel` all exist as standalone `OnPush` components.
  - Given the kit, when its sources are searched, then **no Bootstrap JavaScript is imported anywhere** — neither as a global script nor as an ESM symbol — because modal and offcanvas are the native `<dialog>` with `showModal()` and the dropdown and tooltip are hand-rolled `position: fixed` layers; `styles.scss` still imports Bootstrap's SCSS module by module from the npm package rather than a prebuilt bundle, and no stylesheet, script, or font is requested from a third-party origin at runtime.
  - Given a toast, when raised, then it appears at `top: 76px; right: 20px` with an 8px stacking gap, a 4px left type accent, and an icon for one of success, error, warning, or info; a success toast auto-dismisses after 5 seconds with a draining 3px edge indicator, and a failure toast **persists until the user dismisses it** (frame `4l`).
  - Given the attachment chip, when each state is exercised, then all five variants from frame `4l` render distinctly — uploading with a percentage, processing with a sub-status line, ready, unsupported-type with a Retry action, and expired-or-unknown.
  - Given the error panel and `UnavailablePanel`, when both render, then they are **distinct** components — the unavailable panel names a missing capability and offers no retry (frames `4i`, `5i`), the error panel names the `traceId` and offers Retry (frame `4k`) — and neither displays rows, records, or placeholder values.
  - Given a modal or an offcanvas, when it opens, then focus moves into it and returns to the invoking control on close.

#### US-107: `[enabler]` Vendor the Andes streaming contract with a drift check

- **Story**: Copy the package's TypeScript contract and reducer into the app verbatim, and fail CI if it diverges from the installed package. Unblocks US-404 and all of EP-5.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-10)
- **Acceptance criteria**:
  - Given the vendored file at `src/app/domain/stream/andes/assistant-ui.contract.ts`, when compared byte-for-byte against `~/.nuget/packages/andes.extensions.ai.ui/0.5.0/typescript/andes-assistant-ui.ts`, then only an added header comment pinning version `0.5.0` differs.
  - Given the vendored file, when it drifts from the package copy, then the CI diff step fails the build with a message naming both paths.
  - Given the codebase, when searched, then no hand-written declaration of `AssistantUiEvent`, `AssistantActivity`, `AssistantStatusSnapshot`, or a reimplementation of `foldAssistantEvents` exists.

#### US-108: `[enabler]` Enforce lint rules and bundle budgets in the build

- **Story**: Make the safety and size invariants build failures rather than review comments. Unblocks the security criterion in US-601 and FR-46.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-10)
- **Acceptance criteria**:
  - Given a `bypassSecurityTrustHtml` call **anywhere** under `src/`, when lint runs, then it fails with the `no-restricted-syntax` rule — there is no permitted call site, because `ngx-markdown` performs the trust internally.
  - Given `angular-eslint` and `@ngrx/eslint-plugin`, when lint runs, then `prefer-protected-state`, `with-state-no-arrays-at-root-level`, `enforce-type-call`, and `signal-store-feature-should-use-generic-type` are all active and passing.
  - Given a build where the diagram or math library has entered the initial chunk, when the initial import graph is checked, then the build fails naming the library, the input path, and the chunk it landed in. A `bundle`-type budget cannot carry this criterion: `@angular/build`'s `generateBudgetStats` populates `chunk.names` only from `initialFiles` or from `metafile.outputs[…].entryPoint`, and a chunk produced by `import('mermaid')` has neither, so the budget sums zero bytes and can never fail. `scripts/check-initial-chunk.mjs` reads the esbuild metafile and walks the static-import graph from `src/main.ts`, stopping at every dynamic import the way the builder's own initial-file traversal does.
  - Given a template referencing a `bi-` icon name absent from the checked-in sprite manifest of US-109, when the build runs, then it fails naming the missing glyph.
  - Given the workspace, when scripts are inspected, then `lint`, `format`, `test`, and `build` all exist and run clean.

#### US-109: `[enabler]` Ship the brand assets, type scale, and icon set

- **Story**: Bring the design bundle's fonts, logos, ridgeline motif, and icon glyphs into the app as self-hosted assets, so no screen has to invent a substitute and no request leaves the origin. Unblocks US-105's asset swap, US-106, and every screen that renders an icon, a logo, or the ridgeline motif.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-101
- **Status**: ✅ Done (2026-08-10)
- **Acceptance criteria**:
  - Given the three type families, when the app loads, then Montserrat 600/700/800, Inter 400/500/600, and JetBrains Mono 400/500/600 are served as subsetted `woff2` from `public/fonts` with `font-display: swap`, and the body and heading faces are preloaded.
  - Given the brand assets, when the shell renders, then `logo-full`, `logo-full-dark`, `logo-mark`, and `logo-mark-dark` are served from the app's own origin and the light/dark pair swaps through `--show-light`/`--show-dark` with **no** script reading the theme.
  - Given `bootstrap-icons` from npm, when the build runs, then it emits one SVG sprite containing exactly the 74 glyphs the app uses — the 73 distinct `bi-*` names the boards reference, independently derived twice, plus `bi-file-earmark` as the attachment chip's fallback for an unrecognised file extension, all 74 present in `bootstrap-icons` 1.13.1 — and the checked-in manifest at `src/app/shared/icon/icon-names.ts` is the source of truth for the sprite, the `IconName` template type, and US-108's build check. The full icon font is **not** shipped, because its stylesheet alone is roughly 72 kB against the initial-bundle headroom this budget allows.
  - Given the ridgeline motif, when the sidebar divider, the thinking indicator, and the empty states render, then each draws the inline SVG path from the design bundle rather than an image file, so it inherits `--muted` and `--accent` per theme.
  - Given a production build served and loaded with the network log open, when every route is exercised, then **zero** requests leave the app's own origin — no unpkg, no jsDelivr, no `fonts.googleapis.com` — satisfying FR-51.

### EP-2: Sign-in and session

#### US-201: Sign in with Entra ID

- **Story**: As an employee, I want to sign in with my work account so that I can use the assistant without a separate credential.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-102
- **Status**: ✅ Done (2026-08-11)
- **Acceptance criteria**:
  - Given an unauthenticated visitor, when they request any route other than `/auth` or `/login-failed`, then they are redirected to Entra ID and no application chunk renders first.
  - Given a completed redirect, when `/auth` handles it, then the active account is selected, `TokenService` can produce a token, and the user lands on the chat route.
  - Given the redirect is still resolving, when `/auth` renders, then the signing-in interstitial of frame `6a` shows the wordmark, the ridgeline, a `spin` ring, "Signing you in…" and "Finishing up with Microsoft Entra ID" — never a blank page and never an empty app shell — and it paints in the stored theme, so a dark-mode user sees frame `6f` rather than a light flash.
  - Given a failed or cancelled sign-in, when the redirect returns, then `/login-failed` renders frame `6b` — the wordmark, "Sign-in didn't complete", a plain-language explanation, and one "Try again" control that re-enters the Entra ID flow — and no password field appears anywhere on it.
  - Given a signed-in user, when a request is issued through either `HttpClient` or the streaming `fetch`, then both carry a token obtained from the same `TokenService`.

#### US-202: Bootstrap the session from the current user

- **Story**: As a signed-in user, I want the app to know my identity, permissions, models, and MCP servers before it renders so that no screen renders an affordance I cannot use.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-201, US-104
- **Status**: ✅ Done (2026-08-11)
- **Acceptance criteria**:
  - Given a completed sign-in, when `sessionGuard` resolves, then `POST /api/users/me` has returned and `SessionStore` holds the user and their permission set before any child route activates.
  - Given a resolved session, when the shell renders, then models, permitted MCP servers, conversations, and projects have all been requested, each sequenced after the bootstrap call rather than in parallel with it.
  - Given `POST /api/users/me` returning a non-2xx, when the guard runs, then frame `6c` renders — the mark, "We couldn't start Enterprise GPT", a plain-language line, the `traceId` in JetBrains Mono, and a Retry control — and no empty shell is shown.
  - Given a user with zero permission grants, when the session resolves, then the empty set is treated as a valid result and not as a load failure.

#### US-203: Keep the admin area off non-admin devices

- **Story**: As a security reviewer, I want the admin code never to reach a non-administrator's browser so that the admin surface is not merely hidden.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-202
- **Status**: ✅ Done (2026-08-11)
- **Acceptance criteria**:
  - Given a user without the `Administrator` permission, when they navigate to `/admin`, then the guard runs on `canMatch`, the admin chunk is not requested over the network, and they land on the forbidden page of frame `6e`, which names the Administrator permission and offers one "Back to Chat" action.
  - Given a user without the permission, when the shell renders, then the Admin entry is **absent** from the sidebar navigation — not shown-and-disabled — so frame `6e` is reachable by URL only.
  - Given an administrator, when they navigate to `/admin`, then the chunk loads and the users tab renders.

#### US-204: Gate upload affordances on the Upload File permission

- **Story**: As a user without upload rights, I want the app not to offer me an upload control so that I am not shown a 403 after choosing a file.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-202
- **Status**: ✅ Done (2026-08-11) — the enforcement primitives, since the composer is US-401's. `SessionStore.canUploadFiles` gates the attach control through `@if`, as US-203 gates the Admin entry; the `[appFileDropTarget]` directive in `shared/upload/` carries the inert-drop half, which `@if` cannot express, and `providePreventDropNavigation()` stops a file dropped anywhere else replacing the app. US-801 consumes both in two lines. Verified against a test host standing in for the composer; the frame `2h` assertion re-runs when the real composer lands.
- **Acceptance criteria**:
  - Given a user whose permission set omits `b1c2d3e4-f5a6-4b7c-8d9e-0f1a2b3c4d5e`, when the composer and the project files panel render, then no attach control, paperclip button, or drop zone is present and drag-and-drop is inert (frame `2h`).
  - Given an administrator whose permission set omits `Upload File`, when the composer renders, then the upload affordance is still absent — the check is on `Upload File` alone and never inferred from `Administrator`.
  - Given a user who does hold the permission, when the composer renders, then the attach control and the drop zone are both available.

#### US-205: Sign out and leave nothing behind

- **Story**: As a user on a shared machine, I want signing out to clear my data from the browser so that the next person sees nothing of mine.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-202, US-104
- **Status**: ✅ Done (2026-08-11)
- **Acceptance criteria**:
  - Given a signed-in session with loaded conversations, projects, and an open transcript, when the user signs out, then `sessionEvents.signedOut` fires and every store composing `withResetOnSignOut` returns to its initial state.
  - Given sign-out, when `localStorage` is inspected, then theme and sidebar preferences remain and no conversation, project, document, or user data is present.
  - Given an in-flight turn, when the user signs out, then the request is aborted and no orphaned reader continues writing to a store.

Three things exceed the criteria above, each because the criteria are written for one tab and the story ("the next person sees nothing of mine") is not.

- **Cross-tab sign-out.** MSAL's `cacheLocation` is `sessionStorage`, which is per tab, so signing out in one window otherwise leaves every other one fully signed in. `core/session/session-channel.ts` broadcasts, and each receiving tab tears down and leaves.
- **A `/signed-out` landing page**, and `postLogoutRedirectUri` now points at it. When `logoutRedirect` fails before navigating, the Entra session cookie is still live, so landing on `/` would take the user through `/chat` → `authGuard` → a _silent re-authentication_ — the exact failure this story names. Registering `/signed-out` in the Entra app registration is a deployment step; without it Entra shows its own signed-out page, which degrades gracefully.
- **A `localStorage` allowlist as a build gate.** `core/storage/local-preferences.ts` is the only permitted call site and `scripts/check-forbidden-apis.mjs` fails the build on any other write, so the second criterion holds for stories not yet written rather than only for today's code.

The third criterion has no streaming turn to abort yet (EP-4). `injectSignedOutAbort()` is the primitive it will use — `takeUntil` stops delivery but leaves the connection open and the server generating, so only aborting the `fetch` ends the request. The criterion is met today for every store that exists, via `takeUntil(injectSignedOut())`.

US-205 also builds the frame `3a` sidebar footer — initials avatar, display name, theme control, sign out — which no story owned. It is hosted in the temporary shell bar until US-301 moves it into the sidebar.

### EP-3: Navigation and conversation sidebar

#### US-301: Collapse the sidebar and have it stay collapsed

- **Story**: As a user, I want to collapse the sidebar and find it still collapsed next time so that my layout preference persists.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-106
- **Status**: ✅ Done (2026-08-11). The boards state that the collapse control "lives inside the sidebar" (frame `3f`) without drawing one, so the glyph is the application's choice: `bi-chevron-left`/`bi-chevron-right` in the sidebar header, both already in the 74-glyph sprite, so no manifest change was needed. The preference lives in `UiStore` (`core/ui/`), which deliberately does **not** compose `withResetOnSignOut` — US-205 requires it to survive sign-out. No pre-paint script is needed for the width the way `index.html` needs one for the theme, because the startup shell covers the page until bootstrap; what is needed is withholding the width _transition_ until after the first render, or a returning user watches their sidebar animate shut on every load. Frame `3b` draws only an avatar in the strip's footer; the theme control and sign out are kept there as 44px icon buttons, because a collapsed sidebar is a persisted state and hiding them would make signing out require expanding first.
- **Acceptance criteria**:
  - Given an expanded sidebar at 260px, when the in-sidebar collapse control is used, then it animates to a 60px icon strip over 200ms and the main content reflows.
  - Given a collapsed sidebar, when the page is reloaded, then it is still collapsed, restored from `localStorage`.
  - Given the collapsed strip, when a user hovers or focuses an icon button, then a tooltip flyout renders at `left: 54px` on `#0B1F33` in white 12px type showing the full label, and each button is a 44px square target (frame `3b`).

#### US-302: See my recent conversations

- **Story**: As a user, I want my conversations listed in the sidebar so that I can return to one in a click.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-202, US-104
- **Status**: ✅ Done (2026-08-11), together with the shell that hosts the sidebar. `ConversationListStore` (`core/conversations/`) is the reference list store this document's risk table names: `withEntities` + `withRequestStatus` + `withOffsetPagination(50)` + `withResetOnSignOut` last, loaded declaratively by one `rxMethod` wired in `onInit` to a computation over `started` and `name` — so `SessionBootstrap` only flips a flag and every later search refetches itself. It carries no `debounceTime`, because `SearchInput` already debounces and emits only the committed value. `withPendingIds` is not composed yet; US-304 adds it with the first per-row action. Three things the criteria do not state but the story needs: the list is paged, so a `Load more` row states the truncation rather than passing the first 50 off as all of them; a further page that fails raises a toast instead of replacing the loaded rows with an error panel; and rows navigate to `/chat/{id}` through **US-401's `UrlMatcher`, built here**, so one route configuration serves `/chat` and `/chat/{id}` and the page never remounts. Selection is `routerLinkActive` with `exact: true` plus `ariaCurrentWhenActive`, so the visual and announced states are one fact. Only nav entries whose routes exist are rendered — Chat, and Admin behind the permission; EP-7, EP-9 and EP-10 add theirs.
- **Acceptance criteria**:
  - Given a signed-in user, when the shell renders, then `GET /api/conversations/search` is called with `name=` omitted and the returned items render newest-first in server order.
  - Given a user with no conversations, when the sidebar renders, then a designed empty state shows, distinct from the loading skeleton.
  - Given a conversation is open, when the sidebar renders, then its row is visually selected.
  - Given the list overflows, when the user scrolls it, then it scrolls independently of the rest of the sidebar.
  - Given the search field, when the user types, then the request sends `name=` — not `filter=` — and the list narrows.

#### US-303: Start a new conversation without creating one

- **Story**: As a user, I want "New conversation" to give me an empty prompt box so that abandoning the idea does not litter my sidebar with empty conversations.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-302
- **Status**: ✅ Done (2026-08-11), with **one criterion deliberately deferred**. The centred wordmark and "How can I help you today?" are built and nothing is created until the first prompt — "New conversation" is a `routerLink` to `/chat`, so there is no request to suppress. The **suggested prompt chips are deferred to US-401**, agreed with the product owner on 2026-08-11: a chip's only behaviour is to seed the composer's text, and the composer is EP-4, so shipping them now would put four controls on screen that visibly do nothing. The same reasoning settled the criterion's phrase "renders an empty composer": the composer arrives whole in US-401–403 rather than as an inert box. Nothing moves focus on arrival — this is the application's landing screen rather than a state transition, and taking focus would jump a keyboard user past the shell's skip link on every cold start.
- **Acceptance criteria**:
  - Given any screen, when the user selects "New conversation", then the chat route renders an empty composer and no `POST /api/conversations` request is issued.
  - Given that empty state, when the user navigates away without sending, then no conversation exists in the sidebar after a reload.
  - Given the empty state, when it renders, then the centered wordmark, the "How can I help you today?" subtitle, and the suggested prompt chips are shown.

#### US-304: Rename a conversation without unlinking it

- **Story**: As a user, I want to rename a conversation so that I can find it later — and I want the rename to leave its project membership alone.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-302, US-106
- **Status**: ✅ Done (2026-08-11). Three decisions made here outlive the story. **The rename form is the application's first Signal Form** — a product-owner decision of 2026-08-11 makes `@angular/forms/signals` the standard for every form that follows, accepting that Angular 21.2 still marks the API `@experimental`; classic `ReactiveFormsModule` gains no call sites, and `serverMessagesFor` (`core/errors/`) is the Signal Forms counterpart to US-103's `applyServerErrors`, feeding a declarative `validate` rule that shows the server's message only while the field still holds the exact rejected name — which is what clears it on the next edit without an imperative `setErrors` the API does not have. **The flows live in one root `ConversationActionsStore`** (`core/conversations/`), with the dialog mounted once in the shell, because its two invokers — the sidebar row kebab here, the header kebab of US-308 — live in features that must not import each other; the row kebab ships with Rename as its only item, since Favorite (US-305) and the project moves (US-307) do not exist yet. On success the store dispatches `conversationEvents.updated`, which is how the open conversation's header stays correct even for a deep link the sidebar list never held. **`toUpdateBody()` exists from this story**, so US-307's one-builder invariant is the shape of the very first `PUT api/conversations` caller rather than a retrofit; `withPendingIds` joined `ConversationListStore` with this first per-row action, as US-302 planned.
- **Acceptance criteria**:
  - Given a conversation in a project, when the user renames it, then the `PUT /api/conversations` body echoes the current `projectId`, and after a refetch the conversation is still in that project.
  - Given the rename modal, when it opens, then frame `3c` renders — a 420px modal, a text input pre-filled with the current name, a live JetBrains Mono counter reading `25 / 256` against the server limit, and Cancel and Rename buttons — never an inline input and never a browser `prompt()`; focus is trapped inside it and returns to the invoking control on close.
  - Given a name the server rejects, when Rename is pressed, then the server's message renders beneath the field with the input in its invalid state and the counter still visible (frame `3c`, right-hand variant), and nothing is saved.
  - Given a successful rename, when the response is pending, then the new name shows optimistically and rolls back if the request fails.

#### US-305: Favorite a conversation

- **Story**: As a user, I want to mark a conversation as a favorite so that I can filter to the ones I return to.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-302
- **Status**: ✅ Done (2026-08-13), taken early — out of P4, alongside EP-6's rendering stories — because it is what releases US-308's deferred header star; that criterion is now met, and the header star and the sidebar row flip together — the row's own toggle being a new Favourite / Unfavourite item in its kebab. It added exactly one method to `ConversationListStore` (`favoriteRow`) and one flow to `ConversationActionsStore`, changing nothing US-304/306 established. Three decisions outlive the story. **The request is an idempotent SET, not a toggle** — the body carries the state being asked for, so a duplicate or retry cannot land on the opposite value — and with no dialog in front of it the per-row pending id is the _whole_ re-entry story: a double-click's second call is refused in the store, and both the optimistic patch and the pending flag are written inside the `rxMethod` projection (which runs synchronously with the call) so that guard can see them. **The success arm re-asserts the flag rather than refetching**: the 204 carries no DTO, and a detail `refreshRow` or a search's `setAllEntities` landing mid-flight would otherwise write the server's pre-`PUT` value back permanently, leaving the header star — which reads the list row — showing the opposite of what the server holds. **`conversationEvents.updated` is dispatched with the target DTO plus the accepted flag**, the one deliberate synthesis in that group; it is bounded by the endpoint being a single-field idempotent SET, and a future 204 that changes more than one field refetches instead. The header star prefers the list copy over the detail DTO (the reverse of `current`) precisely because the list copy is the optimistically patched one; a deep link the list never held falls back to the detail DTO and lags a toggle by the round trip. The sidebar row's star stays display-only — a button inside the row's `<a>` would nest an interactive element in a link — so the row's toggle is its kebab item.
- **Acceptance criteria**:
  - Given a conversation, when the user toggles favorite, then `PUT /api/conversations/{id}/favorite` is called and the row updates optimistically.
  - Given the optimistic patch, when it is applied, then `dateModified` is left untouched, because favoriting deliberately does not bump it server-side and a local bump would diverge on the next fetch.
  - Given a failed favorite call, when the error returns, then the toggle rolls back and an error toast names the `traceId`.

#### US-306: Delete a conversation

- **Story**: As a user, I want to delete a conversation I no longer need so that my sidebar stays relevant.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-302, US-106
- **Status**: ✅ Done (2026-08-11), through the `ConversationActionsStore` + shell-mounted-dialog shape US-304 established. Two sequencing facts the criteria imply but do not state are now explicit in the code: **removal is optimistic, navigation is not** — the row disappears on confirm, but leaving `/chat/{id}` for the empty state waits for the 204 (announced as `conversationEvents.deleted`, dispatched only from the success path), because a failed delete restores the row and the user must still be standing on the conversation it belongs to; and **the restore is position-faithful** — `removeRow` captures the row's index and shifts `skip`/`totalCount` back (the inverse of `prependNewest`'s bookkeeping, so the next "Load more" window stays aligned), and `restoreRow` reinserts at that index, clamped, as a no-op if a refetch already brought the row back. Deleting the conversation removes the modal's focus-return target, so the dialog moves a fallen-through focus to `#main-content` — the skip link's landmark, the one always-present programmatic target. The API always answers 204 (idempotent, never 404), so the only failure paths are transport and 5xx; deletes of different conversations may overlap (`mergeMap`), while same-id overlap is excluded by the per-row pending id that also keeps US-308's header kebab disabled for the flight.
- **Acceptance criteria**:
  - Given a conversation, when the user opens its row kebab — where Delete is the only red item (frame `3a`) — and selects Delete, then frame `3d`'s confirmation modal names it and states that its messages are deleted while its uploaded files stay in the user's documents, and offers a red Delete action — red is used **only** here and on bulk delete.
  - Given confirmation, when `DELETE /api/conversations/{id}` succeeds, then the row is removed optimistically and a success toast is raised.
  - Given the deleted conversation was the one open, when the delete completes, then the app navigates to the empty chat state rather than leaving a dead transcript on screen.
  - Given the request fails, when the error returns, then the row is restored and an error toast is raised.

#### US-307: Move a conversation into or out of a project

- **Story**: As a user, I want to assign a conversation to a project so that related work sits together.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-304, US-901
- **Status**: ✅ Done (2026-08-15). The move itself added no mechanism — `moveToProject` is favourite's flow one field over, and **one full-representation `PUT` covers into, between and out of a project**, which is why criterion 3's "without a second round trip" was free rather than engineered. Four decisions outlive the story. **The picker is not a `Menu`, deliberately**: `Menu` renders its own trigger, its panel is `role="menu"` — whose only valid children are `menuitem*` — and its keyboard handling swallows the arrows and closes on Tab, three separate collisions with a search field. It is a non-modal `role="dialog"` holding a listbox, with the field as its **single tab stop** and roving arrow-key focus over the rows, because 500 projects would otherwise be 500 tab stops in one popover; what it borrows from `Menu` is `anchoredPosition` (extracted to `shared/overlay/anchored-panel.ts`, now shared) and `onDismiss`. **A root `ProjectLookupStore` drains every project to the 500 ceiling and is released by `SessionBootstrap`**, which closes US-202's fourth criterion — the last interim behavior on that row. It serves the picker's list *and* `nameOf(projectId)`, because `ConversationDto` carries the id and no name, so no chip anywhere could be rendered without it; unlike the projects grid it **keeps** the pages already drained when a later one fails, since a lookup that can still name 200 of 500 projects beats one that names none. **`projectId` reaches `toUpdateBody` verbatim** — an `undefined` anywhere on that path turns "remove from project" into a silent no-op, which is criterion 4's invariant biting the one caller it was written for. And **the project dialogs moved from `ProjectsLayout` to `Shell`** (US-903 forecast this), taking that layout's destroy-time cancel with them; one consequence is accepted rather than overlooked — a project dialog dismissed with browser Back now stays open over the next route, exactly as the conversation dialogs always have. Two departures from the boards, both deliberate: **Remove from project is absent rather than inert** for a standalone conversation, and the picker **states the drain ceiling** when it hits one, which frame `2e` does not draw. The composer's pill is absent on empty `/chat` — nothing to move, a deliberate scope boundary — and a static chip on a project's own screen. Both tables capture the row's **id** and derive the DTO, never a frozen snapshot: `toUpdateBody` echoes the name, so a snapshot would revert a rename that landed while the panel was open. Initial bundle **668.90 kB / 168.23 kB transfer**, 1.1 kB under the 670 kB warning line, all of the growth being the root-scoped lookup store.
- **Acceptance criteria**:
  - Given a standalone conversation, when the user assigns it to a project from the row kebab's Move-to-project submenu or from the composer's project picker — one searchable list with a pinned "New project" action (frame `2e`) — then the `PUT /api/conversations` body carries the target `projectId` together with the unchanged current name.
  - Given a conversation in a project, when the user unassigns it, then `projectId` is sent as null and the conversation appears as standalone after a refetch.
  - Given a conversation in project A, when the user moves it to project B, then it leaves A and appears under B without a second round trip.
  - Given every update path in the app, when the request body is built, then it goes through one `toUpdateBody()` helper, so no caller can omit `projectId` by accident.

#### US-308: Act on the open conversation from its header

- **Story**: As a user, I want the conversation I am reading to carry its own title bar and actions so that I do not have to hunt for its row in the sidebar to rename, favourite, or delete it.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-302
- **Status**: ✅ Done (2026-08-11) **with three criteria deliberately deferred**, per the build order's scheduling note and a product-owner decision of 2026-08-11: the favourite star waits for US-305, the move/remove-project kebab items wait for US-307, and the sub-768px collapse into frame `1d`'s mobile navbar waits for **US-1403** — that navbar's hamburger opens frame `3e`'s sidebar overlay, and shipping the bar before the responsive shell exists would ship a dead control. What shipped: the 52px header's kebab (Rename, Delete) routed through US-304/306's `ConversationActionsStore` flows — nothing reimplemented, and the shell-mounted dialogs serve this second invoker unchanged. The kebab renders only once a full `ConversationDto` is in hand (`ConversationStore.current`, detail preferred for its fresh `projectId`, sidebar copy while the detail request flies, absent for one round trip on a deep link — acting on a name alone would guess at the rename's `projectId` echo), and its items disable while the conversation's own action is in flight (`actionPending`, off the list store's pending ids). The header stays absent on empty `/chat`, unchanged from US-302.
- **Acceptance criteria**:
  - Given an open conversation, when the chat route renders, then a 52px header bar above the transcript shows the conversation name, its favourite star, and a kebab, separated from the transcript by a 1px `--bs-border-color` rule (frame `1b`).
  - Given the header star, when it is used, then it performs US-305's favourite toggle and both the header and the sidebar row reflect the new state.
  - Given the header kebab, when it is opened, then it offers rename, move to project, remove from project, and delete, each routed through US-304, US-307, and US-306 rather than reimplemented.
  - Given the empty chat state with no conversation open, when it renders, then the header bar is **absent** — not rendered empty (frame `1a`).
  - Given a viewport below 768px, when the chat route renders, then the header collapses into the 54px mobile navbar of frame `1d`, keeping the kebab and dropping the inline title.

### EP-4: Chat composition and turn execution

#### US-401: Send a first prompt and have the conversation created around it

- **Story**: As a user, I want typing and sending to start the conversation so that I never have to create one first.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-303, US-405
- **Status**: ✅ Done (2026-08-12), with **one deliberate deviation agreed 2026-08-12**: the URL update is `router.navigateByUrl('/chat/{id}', { replaceUrl: true })` rather than the criterion's literal `Location.replaceState`. `replaceState` moves only the browser URL and leaves the Router's state at `/chat`, which breaks three sibling behaviours at once — the route-bound `conversationId` input never updates (no 52px header, no detail fetch, and US-306's "is this the one open" guard misfires), US-302's `routerLinkActive` selection never lights for the new row, and "New conversation" becomes a same-URL no-op. `replaceUrl` is replaceState _through_ the router: history is replaced and the page does not remount, which is the criterion's intent. The send flow lives in the route-scoped `TurnStore` (`features/chat/`): `exhaustMap` is the double-send guard, the 201 patches `boundConversationId` and calls `prependNewest` _before_ navigating (so `bindRoute`'s equality guard recognises its own navigation and the header name renders from the list row instantly), and a failed create reseeds the prompt into the composer beside a notice with no Retry of its own — the composer is the retry surface. US-303's deferred prompt chips ship here, seeding the composer through the same one-shot `composerSeed` the stopped card's Retry uses.
- **Acceptance criteria**:
  - Given the empty chat state, when the user sends their first prompt, then `POST /api/conversations` runs, then the stream request is issued against the new id, and the answer begins rendering.
  - Given that sequence, when the URL updates to include the new conversation id, then the page does not remount — one `UrlMatcher` serves both `/chat` and `/chat/:conversationId` and `Location.replaceState` performs the update.
  - Given a send with empty text and no attachments, when the send control is evaluated, then it is disabled.
  - Given the conversation creation fails, when the error returns, then the prompt text stays in the composer and is not lost.

#### US-402: Choose the model for a turn

- **Story**: As a user, I want to pick which model answers so that I can trade cost against capability per question.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-202
- **Status**: ✅ Done (2026-08-12). Selection lives in the root `TurnSettingsStore` (`core/chat/`), where `selectedModelId: null` means _follow the catalog default_ — which makes preselection declarative (the composer snaps to `isDefault` whenever the late-loading catalog arrives), self-heals a reload that removes the selected model, and restores follow-default on sign-out with no seeding effect anywhere. `streamSelection` — null until a model resolves, covering pending, failed, and empty catalogs alike — is the one artifact US-401 spreads into the transport request, and `applyConversationSettings` is the seeding surface US-410 will call. Provider names and dot colours come from `domain/api/provider.ts`, a mirror of the three contract-stable seeded provider ids (`Providers.cs` documents them as matching the seeded rows verbatim), degrading to a muted dot and a caption without the provider segment for an id this build does not know; Amazon Bedrock has no frame value, so its dot takes the palette's green ramp. Both pickers ride the shared `Menu`, extended additively — projected trigger face, `stayOpen`, `disabled` (`aria-disabled`, never native), `panelWidth`, `direction='up'` — so the existing kebab consumers are untouched. The composer ships with send `[disabled]`-bound to its real conditions (no model, or empty text) but with no click handler: US-401 wires the send itself.
- **Acceptance criteria**:
  - Given a loaded model catalog from `GET /api/models`, when the composer renders, then the model marked `isDefault` is preselected.
  - Given the picker is opened, when it renders, then each row carries a provider colour dot, the display name, and a `Provider · 400k context` caption whose context figure is in JetBrains Mono; exactly one row shows the `DEFAULT` badge and the current selection carries a check (frame `2b`).
  - Given the picker, when the user changes model, then the selection persists for subsequent turns in that session.
  - Given the model catalog request fails, when the composer renders, then frame `2j` applies — the model pill reads "Models unavailable", a warning line states that the catalog failed to load and that sending is disabled with a retry from the model menu, and the send control is disabled rather than sending without a `modelId`.

#### US-403: Select MCP servers, and be stopped from selecting them where they cannot work

- **Story**: As a user, I want to enable tool servers for a turn and be prevented from doing so on a model that cannot call tools, so that I do not get an opaque 400.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-402
- **Status**: ✅ Done (2026-08-12), with **two deliberate deviations agreed 2026-08-12**. The rows carry the server name only: `McpDto` is `{ id, name, description }` and **no key field exists** to fill frame `2c`'s mono column, so it is omitted rather than fabricated from the name — a backend enabler can add a real key later. Criterion 4's tool-server-unavailable panel is **deferred to US-406/407**: its non-visual half (the `mcp-server-unavailable` arm, `serverName` extraction, retry policy, and copy) shipped tested in US-103, and the `1h` card is one of six sibling turn-edge-state cards that should be designed with the transcript that hosts them — a lone card now would have no reachable render path before US-401. The gating invariant is enforced twice on purpose: `selectModel` performs the visible clear (frame `2d`'s warning, sticky until the model changes again), while `effectiveMcpServerIds` re-derives emptiness whenever the selected model cannot call tools — so even a seeding path that bypasses `selectModel` (US-410 restoring stale ids) cannot produce the server's 400 on `McpServers`. Rows are `menuitemcheckbox` items with icon-glyph checks, because a native checkbox input is not a valid child of `role="menu"`; the panel stays open while toggling and Escape, Tab, outside-press, and scroll still dismiss.
- **Acceptance criteria**:
  - Given `GET /api/mcps`, when the picker renders, then only the servers the caller is permitted appear under a "Tool servers" heading, each a checkbox row carrying the server name and its key in JetBrains Mono, each toggling independently while the dropdown stays open, with the footnote "Stays open while you toggle — applies to the next turn" and a button label reading "Tools", "1 Tool", or "N Tools" (frame `2c`).
  - Given a selected model whose `isToolEnabled` is false, when the picker renders, then it drops to opacity `.45`, a warning line names the model and states that the previous selection was cleared, the button label returns to "Tools", and the stream request carries an empty `mcpServers` array (frame `2d`).
  - Given selected servers, when the turn is sent, then `mcpServers` carries `{ id }` for each — the `mcpServerIds` field is not dropped, which the old client did.
  - Given a 502 `/problems/mcp-server-unavailable`, when it returns before the first frame, then frame `1h`'s tool-server-unavailable panel names the `serverName` extension, states that the turn produced no answer, and offers Retry.

#### US-404: `[enabler]` Decode the SSE frame stream

- **Story**: Parse the wire format into `AssistantUiEvent` values, correctly across chunk boundaries. Unblocks US-406 and all of EP-5.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-107
- **Status**: ✅ Done (2026-08-12). Both codecs are synchronous push decoders (`decode(chunk)` / `flush()`) behind one `StreamCodec` interface in `domain/stream/`, so the transport selects the raw-text fallback by `features.rawStreamCodec` with a single expression and everything downstream of the transport is identical for both — the fallback synthesizes `TextDelta` events, with `timestamp` pinned to the .NET default sentinel because the contract forbids ordering by it and a clock read would make the fixtures nondeterministic. Unknown kinds are dropped through an exhaustive `satisfies Record<AssistantUiEventKind, true>` table, so a contract upgrade that adds a ninth kind fails compilation rather than silently discarding events in production. Malformed frames are skipped silently — the wire has no error surface to relay after the first frame, and throwing would destroy a live turn over one defective frame. `flush()` salvages a complete final frame that lost only its terminating blank line; a tail cut mid-JSON is precisely the "body just stops" ending and drops. The §6 pass threshold — all eight kinds folded, unknown kind ignored, split frame reassembled — is pinned by `assistant-ui.fold.spec.ts` beside the vendored reducer, which stays byte-identical.
- **Acceptance criteria**:
  - Given a byte stream of `data: {json}\n\n` frames, when decoded, then one `AssistantUiEvent` is emitted per frame, in order, with the `data: ` prefix stripped.
  - Given a chunk boundary that splits a frame mid-JSON, when the next chunk arrives, then the partial-line buffer is carried across reads and the frame is parsed once, intact — verified by a fixture that deliberately splits one frame across two reads.
  - Given a frame whose `kind` is not one of the eight known values, when decoded, then it is ignored rather than thrown on, so a server-side protocol addition does not break the client.
  - Given `TextDecoder`, when it is constructed, then `{ stream: true }` is set so multi-byte characters spanning a chunk boundary are not corrupted.
  - Given a raw-text fallback codec, when the config flag selects it, then unframed bytes render as plain answer text.

#### US-405: `[enabler]` Stream over an abortable fetch that reads errors first

- **Story**: Provide the transport that acquires a token, checks the response before reading any body bytes, and can be aborted. Unblocks US-401, US-406, and US-407.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-103, US-404
- **Status**: ✅ Done (2026-08-12). `ConversationStreamClient` (`core/stream/`) returns `Observable<readonly AssistantUiEvent[]>` batches — `bufferTime(16)` plus a non-empty filter, deterministic under vitest fake timers as §6 requires, with the residual buffer flushed on completion. `fetch` is injected through the `STREAM_FETCH` token so specs substitute a fake by provider override rather than a global stub. Three decisions here outlive the story. A bare 401 is replayed exactly once with a force-refreshed token, gated by the same `authErrorDecision` the interceptor consults — without it, a token the API just rejected would kill a turn every `HttpClient` request would have survived; it is safe because authentication is rejected before the conversation lock is taken, and it structurally cannot loop on the 403 `mcp-authorization-required`. An abort — the caller's Stop, sign-out, or unsubscribing, whose teardown aborts the underlying fetch — **errors** with the `aborted` arm: the race-free discriminator that keeps US-406's "completed without `Finished` → cut off" logic from firing on every Stop, with every flushed batch already delivered so US-407 keeps the partial output. A failure _after_ the body has begun **completes gracefully** instead: streaming-contract §3.3 makes a mid-stream fault indistinguishable from a network drop, so both endings take the single no-`Finished` path US-406 owns. Every error the observable emits is an `AppError`, documented as the class's contract.
- **Acceptance criteria**:
  - Given a stream request, when the response returns, then `response.ok` is checked and any problem body is parsed **before** a single byte of the body is read — headers are set lazily on the first frame, so 400, 403, 404, 409, 502, and 503 all arrive as ordinary `application/problem+json`.
  - Given a token requirement, when the request is built, then the bearer token comes from the same `TokenService` the `HttpClient` interceptor uses.
  - Given an `AbortController`, when `abort()` is called, then the reader stops, the request is cancelled, and no further events are emitted — there is no stop endpoint and none is called.
  - Given a running stream, when events are emitted, then they are buffered on a ~16ms budget and folded as one batch, producing at most one `patchState` per frame at any token rate.
  - Given a synthetic `ReadableStream` of frames in a Node test, when the transport reads it, then it completes with no browser and no backend.

#### US-406: Watch the answer arrive

- **Story**: As a user, I want to see the answer appear as it is generated so that I can start reading before it finishes.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-405, US-104
- **Status**: ✅ Done (2026-08-12). `TurnStore` folds each ≤16ms batch through the vendored `foldAssistantEvents` in one `patchState`, so the growing answer is exactly `snapshot.text` — one string, entries untouched until settle. The settle is discriminated on completion, not on events: `Finished` seen (`snapshot.phase === 'Completed'`) → one atomic patch appends the user and assistant entries together and clears the live buffers; completed without `Finished` → a `cutOff` entry carrying its own retry snapshot, rendered as frame `1h`'s warn card above the partial answer. The thinking ridgeline (the shared `<app-ridgeline variant="thinking">`) clears only on renderable content — a `Status`- or `ReasoningDelta`-only batch keeps it, because this app fabricates no status line. `aria-busy` rides the `Transcript` host for the length of the turn. Answer text renders pre-wrapped and plain until US-601's sanitized markdown; the settled entries keep their snapshot and ordering index so a completed turn's cards stay on screen for the session (contract §6.2: live-only, gone on reload). US-403's deferred criterion 4 is retired here: a pre-stream 502 renders frame `1h`'s tool-server-unavailable card naming `serverName`; the 409-busy and consent arms render a generic warn notice until US-408/US-412 bring their designed variants (agreed 2026-08-12).
- **Acceptance criteria**:
  - Given a turn in flight, when `TextDelta` events arrive, then the accumulating text renders progressively and is held in one string on `TurnStore`, never appended to the transcript array per chunk.
  - Given the turn completes, when `Finished` arrives, then exactly one append to the transcript occurs and the streaming buffer is cleared.
  - Given the window between request-accepted and the first `ActivityStarted` or `TextDelta`, when it is rendered, then the thinking indicator shows — the ridgeline drawn on `ridgedash 1.5s linear infinite` beside the assistant avatar (frame `1e`) — because this application emits no request-level `Status` events and no status line is fabricated to fill the gap.
  - Given the response body ends with no `Finished` event, when the reader completes, then the turn renders as "This response was cut off" with a Retry control, because after the first frame there is no error surface and a faulted turn is indistinguishable from a network drop.
  - Given a turn is in flight, when the transcript container is inspected, then `aria-busy` is set on it.
- **Note (2026-08-18) — the MCP label carve-out.** `Andes.Extensions.AI.UI` 0.7.0 changed what `displayName` carries on an `McpTool` activity: it is now the raw MCP **tool** name rather than a second copy of the server name in `source`. Nothing in the event schema moved, so the vendored-contract drift check passed on the change, and US-501's criterion 2 — `displayName` is the label, `kind` is a separate badge — is still met as written. What the client added is `domain/stream/activity-label.ts`, a pure function that strips the `{sanitizedServer}_` prefix the API stamps onto every leased tool, turns the remaining underscores into spaces and upper-cases the first character. A card that read "Weather" over "Weather" now reads **Get forecast** over **Weather**, and the US-1402 live region says the same thing from the same function. Only `McpTool` is relabelled: a `Function` name is a code identifier the design sets in mono, and an `Agent` name is already a display string. The strip is exact rather than heuristic only because `McpToolProvider` passes its catalog server name to `WithTracking` explicitly — if that ever reverts to the server's self-advertised name, every label silently stops stripping.

#### US-407: Stop a turn and keep what it produced

- **Story**: As a user, I want to stop a long answer and still keep the text I got, clearly marked as not saved, so that I am never misled about what the model will see next turn.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-406
- **Status**: ✅ Done (2026-08-12). Stop flips the phase to `stopping` synchronously — the composer's control swaps back on that patch, well inside 200ms — then aborts the per-turn `AbortController` the transport composes into its fetch. The settle discriminates the resulting `aborted` error **by phase, never by abort reason**: `stopping` means the user's Stop, so the flushed partial text becomes the dashed detached card and `pendingUserText` is nulled (the bubble is a field, not an array entry, so its removal cannot disturb the transcript); any other phase means sign-out or a route-change reset already cleared the state, and patching would resurrect a turn — for sign-out, the previous user's. Two race calls made here: `Finished` beats a pending Stop (the server transcribed the answer, so claiming "not saved" would be false — it is appended normally), and Stop during `creating` cancels the `POST` via `takeUntil` and reseeds the prompt with no card, because nothing streamed. The card's Retry restores the prompt and the turn's own model and MCP ids through `applyConversationSettings` — and deliberately does not send. The opacity-.4 dimming ships as a `composer--streaming .composer__aux` rule that matches nothing yet: attach, mic, and download are absent-not-disabled until US-801/US-413/US-1502, and adopt the class when they land.
- **Acceptance criteria**:
  - Given a turn in flight, when the composer renders, then the send button is replaced by a 38px circular Stop control with a 2px `--brand` border and a square glyph, and the attach, voice, and download controls drop to opacity `.4` for the duration of the turn (frames `1b`, `2g`).
  - Given a turn in flight, when the user presses Stop, then the fetch is aborted and the send control returns within 200ms.
  - Given the stop, when the UI settles, then the partial answer renders in a visually detached dashed-border card reading "Stopped — not saved to this conversation" with a stop icon, carrying Copy and Retry, and it is **not** appended to the transcript (frame `1h`).
  - Given the stop, when the UI settles, then the optimistic user bubble is also removed, because a cancelled turn transcribes neither the answer nor the prompt.
  - Given the detached card, when the user reopens the conversation, changes route, or refreshes, then it is gone.
  - Given Retry, when pressed, then the original prompt is restored into the composer with its model and MCP selection intact.

#### US-408: Recover when the conversation is already busy

- **Story**: As a user with two tabs open, I want a clear message when a turn is already running so that I do not think the app is broken.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-405
- **Status**: ✅ Done (2026-08-13). The store side already existed — `settlePreStream` lands the typed 409 in `turnError` with its retry snapshot, `canRetry` says yes, and `isTransientRetriable` says no to every problem-typed arm, so "no automatic retry at any interval" is structural rather than implemented. What shipped is frame `1h`'s panel: `TurnNoticeCard` gained a `layout` axis (`stack` | `row`), and the busy arm is the one `row` — icon, one sentence, Retry pushed right, no spinner and no trace id, because a second tab holding the conversation lock is not a defect to correlate. The copy is the frame's verbatim, hard-coded beside the `mcp-server-unavailable` arm's rather than taken from `userMessage`, which has to name the conversation for a toast that could fire from anywhere. One defect found in review and fixed here: retrying destroyed the button that was pressed and dropped focus to `<body>`, which the composer's settle fixup then claimed for an empty prompt box — so a keyboard user re-traversed the page on every attempt of exactly the loop this story is about. The transcript now hands focus to the replacement Retry when focus has fallen through, and the composer stands down while a notice is on screen.
- **Acceptance criteria**:
  - Given a turn already in flight for a conversation, when a second send is issued, then the 409 `/problems/conversation-busy` renders as frame `1h`'s warning panel on `--warn-bg` with a `--warn-border`, an hourglass icon, the text "Another response is still running in this conversation." and a Retry control — and **no** spinner, because nothing is being awaited.
  - Given that 409, when it is handled, then no automatic retry is scheduled at any interval.
  - Given the warning, when the user retries manually and the first turn has ended, then the second turn starts normally.

#### US-409: See the name the server gave my conversation

- **Story**: As a user, I want my new conversation to acquire a meaningful title on its own so that I can recognise it in the sidebar.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-406, US-302
- **Status**: ✅ Done (2026-08-13). `core/events/turn-events.ts` is the third event group, following `conversationEvents` verbatim, and `TurnStore` dispatches `completed` from the stream's `complete` callback — **only** on a `Finished` frame, so a stopped, cut-off or failed turn stays silent, which is criterion 3 without a special case: the server named nothing after it. `wasFirstTurn` is read _before_ the settle appends the entry that would make the turn look like it had a predecessor, and derives from "the transcript holds no `assistant` entry" — a cut-off attempt leaves a `cutOff` entry, never an `assistant` one, so the retry that actually completes is still the first, while US-410's replayed history correctly marks an existing conversation as not. `ConversationStore` listens and calls a new **silent** `refreshDetail` rather than `open`/`retry`: those go through `setPending()` and would blank the header mid-conversation, flashing the sidebar's stale name back for a round trip. Its failure is swallowed — everything on screen is still true, and a name arriving one refresh late does not deserve an error panel. The sidebar update needed no new code: the existing `next` path already calls `ConversationListStore.refreshRow`.
- **Acceptance criteria**:
  - Given the first turn of a new conversation completes, when `turnEvents.completed` fires with `wasFirstTurn`, then `GET /api/conversations/{id}` is refetched and the sidebar row shows the generated name without a manual refresh.
  - Given a subsequent turn, when it completes, then no title refetch is issued.
  - Given a first turn that was stopped or truncated, when the user retries, then the title refetch happens after the turn that actually completes, not after the abandoned one.

#### US-410: Resume a conversation with the settings it last used

- **Story**: As a user, I want reopening a conversation to restore its model and tool selection so that the next turn behaves like the last one.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-402, US-403
- **Status**: ✅ Done (2026-08-13), **re-sized S → M**: criterion 3 presupposes a transcript this client never fetched. Nothing read `GET api/conversations/{id}/messages`, so reopening a conversation showed a blank screen, and "replaced, not merged" had nothing to be true of — history replay shipped here, as `TurnStore`'s own comment had already assigned it. Settings restore is the small half: `ConversationStore` seeds `applyConversationSettings` from the detail response, deliberately unvalidated, because `selectedModel` falls back to the default for a deactivated id and `effectiveMcpServerIds` re-intersects with the permitted catalog — a stale selection cannot reach the wire. Criterion 2 needed new state: `restoredModelId` is remembered separately from the selection so a model that is _gone_ can be reported rather than silently absorbed, and `restoredModelUnavailable` gates on the catalog having actually resolved — every deep link races the catalog, and an empty `models()` would otherwise flash "your model was deactivated" at every conversation and then withdraw it. Replay folds each stored message through the **same vendored reducers** a live turn uses (`replayAssistantText`, over a synthetic `TextDelta` shared with the raw-text codec), so a replayed answer renders through the existing `AssistantTurn` with no second code path and no invented `Completed` phase — the activity timeline around it stays absent, which is honest, because the stream that produced it was never persisted. "Replaced, not merged" is enforced by giving history its own descending id range: a refetch rebuilds the whole negative block while entries taken since keep their identity, so a shorter read back after an aborted turn _shrinks_ the transcript instead of leaving the abandoned pair on screen claiming the model saw it. Replay hangs off `bindRoute`'s reset path rather than the route input, which is what stops US-401's own `replaceUrl` from reading back a conversation whose first turn is still streaming. Two things review caught, both about seeding from a response that has nothing to say: an **empty** detail payload is not a selection of "nothing" — the server writes `modelId` and the MCP set from the newest usage row, so a conversation whose first turn has not finished reports both empty, and seeding it cleared the model and tool servers the user picked for the turn creating it, undoing US-402 and US-403 by a round trip. It is skipped now. And the history panel's Retry replaces the whole transcript rather than preserving live entries: the panel deliberately keeps the composer usable, so a turn taken while history was broken is _in_ the response the retry installs, and keeping both rendered the exchange twice. **One addition with no frame**: the "previous model is unavailable" line reuses the composer's `2d`/`2j` amber note treatment, since no board draws that state.
- **Acceptance criteria**:
  - Given a conversation with a previous turn, when it is opened, then `GET /api/conversations/{id}` supplies `modelId` and `mcpServerIds`, and both pickers reflect them.
  - Given a conversation whose stored model has since been deactivated, when it is opened, then the picker falls back to the default model and states that the previous model is unavailable.
  - Given a refetched transcript that is shorter than the locally held one — which happens after an aborted turn — when the conversation is opened, then the local transcript is **replaced**, not merged.

#### US-411: `[enabler]` Carry the consent scope on the MCP authorization problem (B9)

- **Status**: ❌ Removed (2026-08-22), superseded by US-414. Tool servers are provisioned for users by an administrator who consents on the tenant's behalf, so the interactive per-user consent this enabler was widening the problem body for is not a state this product has. Putting a `scope` on the wire would have funded a consent action nothing could complete. The `/problems/mcp-authorization-required` type is deleted rather than extended.

#### US-412: Understand an MCP consent requirement

- **Status**: ❌ Removed (2026-08-22), superseded by US-415, which deletes what this shipped. The card was honest about its own dead end — it named the server and told the reader to contact an administrator — but the requirement behind it was wrong: there is nothing here for a user to consent to. Its one durable finding outlives it, that `canRetry` and not the card decides whether an arm grows a Retry, and the table pinning that stays.

#### US-414: `[enabler]` Remove the MCP consent contract from the API

- **Story**: Delete `/problems/mcp-authorization-required` and the exception behind it, so a token acquisition that cannot complete without user interaction reports what it actually is — a tool server this deployment cannot reach. Supersedes US-411. Unblocks US-415.
- **Priority**: P1 · **Estimate**: S · **Depends on**: —
- **Status**: ✅ Done (2026-08-22). Smaller than it looked: consent was one exception, one problem type, one handler arm and two throw sites — no endpoint, no DTO, no service, no table, no callback ever existed. **`MsalUiRequiredException` derives from `MsalException`**, so its explicit catch was redundant and is gone; `MsalClaimsChallengeException` and `MsalThrottledUiRequiredException` derive from *it* and land in the same arm, which is why Conditional Access needs no separate handling. `MicrosoftIdentityWebChallengeUserException` derives from `Exception`, not from `MsalException`, so it has to stay named or it escapes as a 500 — the two are now one `when (ex is … or …)` filter, and a `[Theory]` pins all three inputs rather than the one the old test covered. **One behaviour change worth stating rather than discovering**: the client offers **Retry** on the 502 arm and offered none on the consent card. That is accepted — 502 also covers genuinely transient failures where a retry helps, and a second attempt against a mis-registered server fails identically instead of misleading the reader about who can fix it. Three public docs were widened in step (the exception, the problem type, and the `<exception>` tag), because all three still said "could not be reached" while 502 had become the token-acquisition channel too. `EntraIdOnBehalfOf` and `Scope` are untouched: this removes the consent *surface*, not the auth type.
- **Acceptance criteria**:
  - Given an MCP server whose on-behalf-of token acquisition raises `MsalUiRequiredException` or `MicrosoftIdentityWebChallengeUserException`, when the turn fails, then it returns 502 `/problems/mcp-server-unavailable` carrying the `serverName` extension, and no 403 consent problem exists anywhere in the API.
  - Given the removal, when the exception handler is inspected, then no path escapes as a bare 500 — both MSAL arms fold into `McpServerUnavailableException` rather than being dropped.
  - Given `EntraIdOnBehalfOf`, when a server is registered with it, then the auth type and its scope still work exactly as before: this removes the consent *surface*, not the auth type.

#### US-415: Remove the client's MCP consent surface

- **Story**: As a user, I want a tool server that cannot be reached to say so plainly, so that I am not told to seek an authorization nobody can grant. Supersedes US-412.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-414
- **Status**: ✅ Done (2026-08-22). The five `as const satisfies Record<AppErrorKind, …>` tables are what made this safe: dropping the union arm without dropping all five keys is a compile error, and `retry-policy.spec.ts` asserts the exact key set, so nothing could be missed silently. `bi-shield-lock` **stays** in the sprite manifest — the notice card was not its only consumer, four others remain. The delicate part was the specs: roughly a dozen used the consent fixture to prove *general* invariants ("a 403 never triggers a refresh", "a typed 403 is never retried", "a problem body parses into its arm"), and each was retargeted to an ordinary 403 or a 502 rather than deleted. Review caught the one that lost real coverage in that move — the transcript's `[role="status"]` live region on the **error** path, which is what a screen reader hears and which no other test asserted — and it is restored. **The removal is safe across a mixed-version deploy**: an API still emitting the old type falls to the `http` arm at 403, where `authErrorDecision` gates on `status === 401` and `canRetry` on `status >= 500`, so it gets neither a refresh loop nor a dead Retry — the two things the deleted arm existed to prevent.
- **Acceptance criteria**:
  - Given the API no longer emits it, when the error layer is inspected, then the `mcp-authorization-required` arm is gone from `PROBLEM_TYPE`, `AppError` and all five exhaustive `Record<AppErrorKind, …>` tables, and the type count in their docs agrees.
  - Given a turn that fails because a tool server is unreachable, when it renders, then frame `1h`'s tool-server-unavailable card shows — unchanged, and now the only card a failed tool acquisition can produce.
  - Given the retired arm carried the only coverage of "a 403 never triggers a token refresh", when the specs are read, then that invariant is still pinned, against an ordinary 403 instead.

#### US-416: `[enabler]` Carry an icon key on the MCP server

- **Story**: Add a nullable `IconKey` slug to `McpServer` and expose it on both the administrative and the user-facing DTO, so a client can draw a registered server's real brand mark. Unblocks US-418 and US-1210.
- **Priority**: P2 · **Estimate**: S · **Depends on**: —
- **Status**: ✅ Done (2026-08-22). A tenth migration, `20260822191806_AddMcpServerIconKey`, a plain nullable `AddColumn` with no index and no data movement. **The split is the decision, not the column**: the database stores a key and the client ships the artwork, so adding an icon is a client-only deploy and the API validates the key's *shape* — 64 characters, lowercase slug — never its membership in a list of known icons. A key a newer client wrote therefore round-trips through an older one unchanged and degrades to its generic glyph. Two alternatives were rejected: matching on the server's **name** loses the icon silently the moment an administrator renames a row, and storing a URL or raw SVG puts untrusted markup on the page with no theming and no CSP story. `McpDto` gains its first field beyond `{ id, name, description }`, and its comment now says why an icon crosses where `url`, `authType` and `scope` deliberately do not. Review moved the rule from `!string.IsNullOrEmpty` to `is not null`, so `""` is refused rather than stored beside the `NULL`s meaning the same thing, and added the first tests either action validator has ever had — the rule is duplicated verbatim between create and update, and only the create path was exercised.
- **Acceptance criteria**:
  - Given a server registered with an icon key, when either `GET api/mcps` or `GET api/mcps/all` returns it, then the response carries `iconKey` — including the user-facing `McpDto`, because an icon is presentation rather than a connection detail, which is why it crosses where `url`, `authType` and `scope` do not.
  - Given a create or update body, when `iconKey` is present, then it is validated for **shape only** — at most 64 characters, lowercase slug — and never against a server-side list of known icons, because the artwork ships in the client and adding one must stay a client-only deploy.
  - Given a full-representation PUT that omits `iconKey`, when it is applied, then the stored value is cleared, exactly as every other field on that mapper behaves.

#### US-417: Toggle tool servers with switches

- **Story**: As a user, I want each tool server to carry a switch rather than a checkbox glyph, so that the picker reads the way every other on/off control in the product does.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-403
- **Status**: ✅ Done (2026-08-22). One constraint shapes the whole story: **`role="switch"` is not an allowed owned element of `role="menu"`**, which permits only `menuitem`, `menuitemcheckbox`, `menuitemradio`, `group` and `separator` — the same rule that kept a native checkbox out of these rows in US-403. So `app-switch` (`shared/switch/`) draws the track and thumb and nothing else: no role, always `aria-hidden`, state supplied by the caller. The row's `menuitemcheckbox` and `aria-checked` are unchanged, and forms keep Bootstrap's real `.form-check.form-switch` with `[formField]`, which `ModelFormDialog` already used — the new component matches its geometry so the two read as one idiom rather than becoming a second mechanism. **No new design token**: the off track is transparent with a `--muted` border and thumb, the on track is `--btnP-bg` with a `--btnP-fg` thumb, which measures 5.27:1 on `--surface` and 4.76:1 on `--surface-2` against SC 1.4.11's 3:1. State is carried by thumb *position* as well as fill, so it survives SC 1.4.1, reduced motion (a transition, not a fifth keyframe — `check:forbidden` allows only four) and forced colours. `bi-square` and `bi-check-square-fill` left the manifest with their only consumer, taking it to 75. `check-tokens.mjs` gained a `FOCUS_SURFACE_EXCLUSIONS` entry for `btnP-fg`, newly painted as a background here; review found the two neighbouring rationales had gone stale in the same commit and both were rewritten.
- **Acceptance criteria**:
  - Given the Tools picker is open, when a row renders, then it carries a switch showing its on/off state, and the pill's label still counts "Tools", "1 Tool", "N Tools" (frame `2c`).
  - Given the row sits inside `role="menu"`, when it is inspected, then the switch is `aria-hidden` decoration with no role of its own and no `<input>`, and the row's `menuitemcheckbox` plus `aria-checked` still carry the semantics — a native `<input type="checkbox" role="switch">` is not a valid child of `role="menu"`.
  - Given the switch, when either theme renders it, then its off state is legible against the panel surface, it reads only existing design tokens, and it animates by transition rather than by a fifth keyframe.

#### US-418: Recognise a tool server by its brand mark

- **Story**: As a user, I want a tool server I know to be recognisable at a glance, so that I can pick the right one without reading every row.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-416, US-417
- **Status**: ✅ Done (2026-08-22). The sprite gained a **second lane**, and one fact separates the two: a Bootstrap Icons glyph is rewritten to `fill="currentColor"` so one symbol serves both themes, while a brand mark must keep the fills its owner specifies, so `build-icon-sprite.mjs` emits the `brand-` symbols without that rewrite. `BrandIcon` is a sibling of `Icon` rather than a widening of it, because `icon.scss`'s `fill: currentColor` is exactly what a logo must not inherit, and the two manifests stay independently checkable. Sources are checked in under `shared/icon/brand/`: the Microsoft mark is bootstrap-icons' own `microsoft.svg` with its four-subpath `d` split one path per quadrant and coloured with the brand hexes, so it sits at the same optical weight and on the same 16-unit grid as every other glyph; Context7's is the project's own `context7-icon-green.svg` (MIT), kept at its 28-unit viewBox because a `<symbol>` establishes its own viewport. `check-icon-names.mjs` is deliberately **not** extended — it exists for a raw `class="bi bi-…"` the type system cannot see, there is no icon-font class for a brand mark, and a `brand-` scan would false-positive on every `brand-logo__*` class; `strictTemplates`, the dev-mode guard and the sprite⇄manifest spec cover it instead, the last now asserting the two lanes are disjoint and that only the `bi-` lane carries `currentColor`. `mcp-icon.ts` lives in `shared/icon/`, not `core/catalog/` beside `mcp-server-form.ts`: `core` may not import `shared`, and the registry is typed by `BrandIconName`. **No re-baseline**: the initial bundle measures **671.45 kB raw / 169.08 kB transfer** against 675 / 720 kB — **0.61 kB _below_** the previous figure, because the consent removal gave back more than the two components cost, and all three new files are reached only from lazy chunks.
- **Acceptance criteria**:
  - Given a server whose `iconKey` names a mark this build ships, when the picker renders, then that mark is drawn **in its own colours** rather than tinted to `currentColor`, which is what every other glyph in the sprite is.
  - Given a server with no icon key, or one this build ships no artwork for, when the picker renders, then it falls back to `bi-plug` rather than to an empty box.
  - Given the sprite build, when it runs, then the brand marks come from checked-in sources under `src/app/shared/icon/brand/` and their own manifest, and a name in either manifest with no matching source fails the build rather than rendering blank.

#### US-413: Dictate a prompt

- **Story**: As a user, I want to speak my prompt so that I can compose hands-free.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-401
- **Status**: ✅ Done (2026-08-13). TypeScript 5.9's `lib.dom.d.ts` ships `SpeechRecognitionResult`, `…ResultList` and `…Alternative` but **not** `SpeechRecognition`, its events, or the `webkit`-prefixed constructor, so `core/speech/speech-recognition.ts` declares the four members used and nothing more — a dependency for that would be four lines of types. The `SPEECH_RECOGNITION` token resolves `SpeechRecognition ?? webkitSpeechRecognition` and returns **null** where neither exists, which is what makes "absent, not disabled" a rendering consequence rather than a check: the composer asks the token whether to draw the control at all. It also follows `STREAM_FETCH`'s precedent, so specs substitute a fake by provider override instead of assigning to `window` — necessary here, because the real interface exists in no test environment. `DictationStore` is component-provided: a session belongs to the prompt box that started it, and `onDestroy` aborts, since nothing else releases the microphone. `interimResults` stays **off** against the spec's own default — interim guesses rewrite themselves word by word, and a textarea the user may also be typing in is the wrong place to watch that; finalized phrases append rather than replace, so speaking after typing extends the prompt. Stop uses `stop()` rather than `abort()`, which lets the engine emit the last phrase heard. The pill takes the button's place per frame `2i`, so nothing in the control row shifts mid-session — as **one morphing button**, for the reason the send control morphs into Stop: swapping elements destroys the control the user just pressed and drops focus to `<body>`, and the label changing on a still-focused button is also what announces that recording started. It reuses the existing `ringpulse` — no fifth keyframe. The microphone carries `composer__aux` while idle, retiring US-407's rule that had matched nothing; the pill does not, because a dimmed control that still works reads as broken and stopping must stay available during a turn.
- **Acceptance criteria**:
  - Given a browser supporting speech recognition, when the user activates the voice control, then transcribed text is inserted into the composer and the microphone is replaced by a pill on `rgba(33,168,216,.14)` animating `ringpulse 1.8s ease-out infinite`, carrying a filled mic glyph and the elapsed time in JetBrains Mono (frame `2i`).
  - Given a browser without support, when the composer renders, then the voice control is **absent** rather than present-and-disabled.
  - Given the user denies microphone permission, when recording is attempted, then a message explains the denial and the composer stays usable.
  - Given a turn is streaming, when the composer renders, then the voice control is disabled alongside send.

### EP-5: Assistant activity timeline

#### US-501: See what the assistant is doing, in the order it happened

- **Story**: As a user, I want each tool call, MCP call, and sub-agent call shown as its own card in chronological order alongside the answer text so that I can follow the assistant's work as it happens.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-404, US-406
- **Status**: ✅ Done (2026-08-12). The ordering index is `foldTimeline` (`domain/stream/turn-timeline.ts`), a framework-free reducer beside the vendored fold that records only arrival order: text nodes are `[start, end)` offsets into the snapshot's single `text` string and activity nodes are `scopeId` references resolved at render time — neither structure duplicates the other, and the recorded `fullTurnEvents()` fixture pins the ordering in a Node spec. Every `ActivityStarted` becomes its own chronological card regardless of depth, per this story's "shown separately"; `parentScopeId` is recorded now so US-502's nesting is a render-side filter, not a reducer change. One pre-existing defect fixed: `KindBadge` mapped `Unknown` to a fifth label "Tool" — it now reads "Reasoning", completing the criterion's exactly-four set. `source` renders verbatim (the "Atlassian · host" composition is the server's one string), sub-statuses are an indented bulleted list, and the three state indicators follow frame `1f` with `durationSeconds` in mono on completion. Cards carry no chevron or expansion — that affordance arrives with US-504's usage strip.
- **Acceptance criteria**:
  - Given a turn emitting interleaved `TextDelta` and `ActivityStarted` events, when the timeline renders, then text blocks and activity cards appear in the order the events arrived, each activity shown separately even though the underlying model is hierarchical.
  - Given an activity card, when it renders, then `displayName` is the label and `kind` is a **separate** badge drawn from exactly four values — `Reasoning`, `Function`, `MCP tool`, `Agent` (frame `1f`); a composed string such as "Calling Weather MCP" fails this criterion.
  - Given an activity, when it has a `source`, then that renders as a subtitle in the form `Atlassian · mcp.andessoftware.net/jira`; when it has `subStatuses`, they render as a bulleted list indented beneath the card title (frame `1f`).
  - Given `state`, when it is `Running`, `Completed`, or `Failed`, then an `--accent` ring on `spin .9s linear`, a `check-circle-fill` in `--ok`, or an `exclamation-triangle-fill` in `--warn` renders respectively, with `durationSeconds` in JetBrains Mono on completion (frame `1f`).
  - Given the activity state, when it is computed, then it comes from the vendored `foldAssistantEvents` and the ordering index is a thin separate structure — neither duplicates the other.
  - Given a recorded fixture of a full turn, when it is folded in a Vitest spec, then the resulting ordering matches the recorded arrival order exactly.

#### US-502: See nested work inside the activity that caused it

- **Story**: As a user, I want an agent's own tool calls shown inside that agent's card so that I can tell nested work from top-level work.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-501
- **Status**: ✅ Done (2026-08-13). Entirely render-side, as US-501 planned: the vendored fold already implements all three folding criteria, so nothing in `foldTimeline` or the contract changed. **The filter is root membership, not `parentScopeId`** — `AssistantTurn` opens a top-level card only for a scope that is a root of `snapshot.activities`. The obvious test (`parentScopeId !== undefined`) is wrong in both directions: the fold _promotes_ a child whose parent it has never seen to a root, so that test would delete the card the third criterion requires to survive, and it would also drop a child whose parent arrived late, which the fold rooted for the same reason. Depth styling is per-level custom properties (`--activity-rail`, `--activity-gap`, `--activity-glyph`, `--activity-name`) rather than descendant selectors, because a nested card is a separate component instance whose own declarations override what it inherits — one rule per element, whatever depth it renders at. The kind badge inverts against its card through `--kind-badge-bg`, which is `KindBadge`'s only change and needs no new input. **The rail is 12px at every level**, per this criterion's literal value; the board draws the second one at 10px, a 2px difference kept as one rule rather than two. A fourth level reuses the third's styling — the design draws three and an agent tree nests arbitrarily. Below 768px only the outermost rail insets (frame `1d`'s "one indent level"): the deeper ones keep their border, which is what says "inside", but stop compounding the ~33px each costs, which on a 390px viewport leaves a third-level card no room for its label.
- **Acceptance criteria**:
  - Given an `ActivityStarted` carrying a `parentScopeId`, when it is folded, then it renders nested inside the matching parent card and does **not** push a new top-level timeline node.
  - Given an `ActivityStarted` with no `parentScopeId`, when it is folded, then it becomes a new top-level card.
  - Given a child whose `parentScopeId` matches no known scope, when it is folded, then it is placed at the top level rather than dropped.
  - Given nesting three levels deep, when it renders, then each level indents behind a `padding-left: 12px` rail with a 2px left border, the card surfaces alternate `--surface` → `--surface-2` → `--surface`, the corner radius steps 12px → 10px → 9px, and the result stays legible at the <768px breakpoint (frames `1f`, `1d`).

#### US-503: Read the model's reasoning as it streams

- **Story**: As a user, I want to see the model's reasoning summary so that I can judge how it reached the answer.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-501
- **Status**: ✅ Done (2026-08-13). Two facts about the contract shape the whole story, and the first is easy to miss. **Every turn's first `ReasoningDelta` is server-authored** — the service seeds `"Reviewing the request and preparing a response.\n\n"` ahead of the model's first event (streaming contract §4.1, §4.3) — and the vendored fold accumulates it into `reasoningText` like any other. Rendering it would put a request-level status line on screen, which is exactly what US-406 decided this app does not do, and _measuring_ from it would report queue and network latency as thinking time. So the seed is identified **by position, not by its text** (hard-coding the server's copy in the client would be the brittle way), measured out of the displayed summary by length, and skipped when the clock starts. A turn whose only reasoning is the seed therefore shows **no region at all**, which is what makes criterion 3 true in practice rather than only for replayed history. **The second fact is that no duration is on the wire**: `ReasoningDelta` leaves `timestamp` at its default (§4.2) and `Finished`'s `durationSeconds` is the whole turn's, which the fold discards. `domain/stream/reasoning-timing.ts` is a third framework-free reducer beside `foldTimeline`, taking the clock as a parameter so it stays pure and a spec can pin a turn's seconds without owning a fake clock; `TurnStore` folds it in the same single `patchState` — from `performance.now()`, not `Date.now()`, because a clock step between the two ends of an elapsed measurement would render a negative duration. The window closes on the first `TextDelta` or `ActivityStarted`, and reasoning that resumes mid-answer neither restarts nor extends it, because the model has already answered; `Finished` closes an open window so a reasoning-only turn still reports. **The region takes the pre-answer gap over from the ridgeline** once the model reasons — `showThinking` and `showLiveTurn` split on that, so the summary is readable _as it streams_ rather than appearing whole at the first token, in one component instance for the length of the turn. Frame `1e`'s ridgeline still owns the gap on every turn where the model reasons silently, which is most of them. The text renders as **plain text**, not markdown: it is a summary written for a reader, and a third `<markdown>` instance per turn would spend the budget US-602 exists to protect; `pre-wrap` keeps the model's paragraph breaks. Expansion is component-local state, which is exactly "persists for the duration of the turn".
- **Acceptance criteria**:
  - Given `ReasoningDelta` events, when they arrive, then the reasoning text accumulates in its own region, visually distinct from the answer text.
  - Given a turn with reasoning, when the region renders, then it is collapsible — collapsed as a `--think-bg` pill carrying a right chevron, the label "Reasoning" and the duration in JetBrains Mono; expanded as a `--think-bg` card carrying a down chevron and the reasoning text in `--muted` — and its expanded state persists for the duration of the turn (frame `1f`).
  - Given a turn with no `ReasoningDelta` events, when it renders, then no empty reasoning region appears.

#### US-504: See what a turn cost

- **Story**: As a user, I want the token count for a completed turn so that I understand my usage without leaving the chat.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-501
- **Status**: ✅ Done (2026-08-13). Two surfaces, one formatter (`features/chat/transcript/turn-usage.ts`), pinned to `en-US` rather than the ambient locale — the app ships one language, the design fixes the grouping separator, and a formatter that followed the host would render `1.842` on a German machine and make every spec depend on where it ran. Absence is the rule that matters: a count the provider did not report is **omitted**, while a reported `0` is a measurement and renders. Three "claims nothing" cases are structural rather than checked — a stopped turn keeps only its text, a cut-off turn never folded `Finished`, and a replayed turn never had a stream — and all three are pinned. The footer ships **always visible**, not hover-revealed as frame `1g` annotates: a hover-only footer is unreachable by keyboard and touch until it holds something focusable, and that is US-607's Copy control, which is also what the criterion means by "beside". **The per-activity strip shows what is known.** No server populates `AssistantActivity.usage` — the contract carries `usage` on `Finished` only and the vendored fold never writes the field — so the strip renders `duration 3.1s` today and gains the token columns the moment something attributes them, the field already being in the type. Rendering the duration there rather than gating the whole strip on tokens is what keeps the chevron a live affordance instead of dead markup, and it is what the frame draws: the expanded card shows the duration in the header _and_ in the strip. A running activity has no chevron, because nothing has settled to show.
- **Acceptance criteria**:
  - Given a `Finished` event carrying `usage`, when the turn completes, then the message footer shows `1,842 in · 512 out · 2,354 total` in JetBrains Mono beside the Copy control (frames `1f`, `1g`).
  - Given a `Finished` event whose `usage` reports only some counts, when it renders, then only the reported counts are shown — an absent count is not rendered as zero.
  - Given an activity carrying its own `usage`, when its card is expanded, then a `--surface-2` strip shows that activity's split as `input 1,204  output 88  duration 3.1s` in JetBrains Mono (frame `1f`).
  - Given a stopped or truncated turn, when it settles, then no token counts are claimed, because `Finished` never arrived.

#### US-505: Understand an activity that failed

- **Story**: As a user, I want a failed tool call to say so on its card so that a partial answer is explicable.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-501
- **Status**: ✅ Done (2026-08-13). **Almost all of this was already structural**, and saying so is more useful than inventing work: the vendored fold sets `Failed` and `durationSeconds` on the targeted scope and touches no other, so a failed child cannot mark its parent — and `Finished` is what completes a turn, so a failed tool call never fails one. What US-502 changed is that criterion 2 is now _observable_, the child being drawn inside its parent rather than beside it, and that is what these specs assert. One thing shipped: the card's `visually-hidden` state line now carries the duration ("Failed after 0.6s"), because colour and glyph were the only channels for it and "Failed" alone says nothing about whether the call timed out or refused immediately. **Frame `1f`'s `--warn` reason line is deliberately not built**: `ActivityFailed` carries `scopeId` and `durationSeconds` and nothing else, so there is no failure text on the wire and writing one would be exactly the fabricated status line US-406 refuses.
- **Acceptance criteria**:
  - Given an `ActivityFailed` event, when it is folded, then the card's state becomes `Failed`, a warning indicator renders, and `durationSeconds` is shown.
  - Given a failed child activity, when it renders, then the parent card's own state is not silently marked failed unless the parent also failed.
  - Given a turn where an activity failed but the answer completed, when the turn ends, then the answer renders normally with the failed card retained in place.

### EP-6: Answer rendering and reading

#### US-601: Render model output as markdown that cannot execute

- **Story**: As a security reviewer, I want model-authored markdown sanitized before it reaches the DOM so that an uploaded document or an MCP tool result cannot inject script into the client.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-108
- **Status**: ✅ Done (2026-08-13). **The bundle question this story gated on is answered by moving, not by paying**: `provideChatMarkdown()` is provided at the `Chat` component, so the whole 124.8 kB stack rides the lazy chat chunk and the initial graph carries none of it; `scripts/check-initial-chunk.mjs` gained a FORBIDDEN entry for `ngx-markdown|marked|prismjs|dompurify` so a static import from eagerly reachable code fails the build. The baseline was re-measured anyway and is **660.24 kB raw / 161.01 kB transfer** initial (chat chunk 179.47 kB, styles 62.08 kB), with the initial **warning** line re-stated 660 → 665 kB and the error ceiling unchanged at 720 kB — see §9. The ~11.5 kB of initial growth is _not_ the markdown libraries: it is Angular's own `DomSanitizerImpl`, which `MarkdownService` injects and which lands in a chunk shared between initial and lazy code, plus ~1.6 kB of global CSS. Three things about the pipeline outlive the story. **The two layers are independent**: `renderer.html = () => ''` drops raw HTML at the parser (marked has no `html: false`), and DOMPurify filters what survives against a closed `CHAT_ALLOWED_TAGS`/`CHAT_ALLOWED_ATTR` profile with `ALLOW_DATA_ATTR: false`, passed per call so no other caller can inherit or widen it. **Supplying a `SANITIZE` function replaces Angular's `DomSanitizer` in `ngx-markdown` rather than adding to it**, so DOMPurify is the only DOM-boundary layer — which is why the profile is closed and why the spec drives real payloads through the real pipeline instead of stubbing a layer. **Images are dropped as policy, not as a gap**: FR-51 says the client issues no third-party request at run time, and a remote image in an answer is both such a request and a read receipt, so `img` is absent from the profile and `renderer.image` renders the alt text in its place — escaped, because alt text is raw source and would otherwise re-enter as markup. Two accessibility overrides ship with it: task-list checkboxes are **named** ("Completed" / "Not completed") rather than hidden, since the box is the only thing carrying done-ness and the text beside it reads the same either way; and answer headings shift down **one** level, clamped at `h6`, because the conversation title is the page's `h1` and shifting by two would jump `h1` → `h3`, itself a heading-order fault. The heading override is a `function` rather than an arrow so `this.parser` is the parser carrying these options — the static `Parser.parseInline` would build one with marked's defaults and quietly exempt heading content from the raw-HTML suppression, which a spec now pins. `breaks: true` preserves the pre-US-601 treatment of single newlines. Prism registers seven grammars as a deliberate floor, `Prism.manual = true` cancels its document-wide `highlightAll`, and `allowedCommonJsDependencies: ["prismjs"]` plus a `@types/prismjs` devDependency are the build's price for it.
- **Acceptance criteria**:
  - Given a response containing `<img src=x onerror=alert(1)>`, `<script>alert(1)</script>`, and an anchor with a `javascript:` href, when it renders, then no script executes, the payload appears as inert text or is stripped, no raw HTML reaches the DOM, and a unit test asserts each case.
  - Given `ngx-markdown` is configured, when its providers are inspected, then DOMPurify is supplied through the `SANITIZE` provider with an explicit `ALLOWED_TAGS`/`ALLOWED_ATTR` profile and `ALLOW_DATA_ATTR: false`.
  - Given marked offers no `html: false` equivalent, when the renderer is inspected, then a `MarkedRenderer` override drops raw `html` tokens outright — restoring parser-level suppression as the first of two layers rather than leaving the sanitizer to carry the boundary alone.
  - Given the whole of `src/`, when it is searched, then it holds **zero** `bypassSecurityTrustHtml` call sites, because `ngx-markdown` performs the trust internally; introducing one fails the US-108 lint rule.
  - Given any store, when its state is inspected, then it holds markdown source text and never a `SafeHtml` value.
  - Given `ngx-markdown`, `marked`, and Prism have entered the initial chunk, when the production build is measured, then the initial-bundle baseline behind US-108's budget is re-measured and re-stated — or the transcript renderer moves behind the lazy chat route so the existing budget still holds.

#### US-602: Read a streaming answer without the page slowing down

- **Story**: As a user reading a long answer, I want rendering to stay smooth to the end so that a long response does not degrade as it grows.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-601, US-406
- **Status**: ✅ Done (2026-08-13), with **two refinements of the criteria's letter, both in service of its intent**. The split (`domain/markdown/streaming-split.ts`, framework-free and tested in Node) takes the **last** of three block starts rather than the criteria's two: the last blank line, the opener of an unclosed fence, or _the line after a closed fence_ — the third is what stops the boundary jumping backwards when a fence had no blank line before it, which on an answer with no blank lines at all would collapse the head to nothing and keep it there. It also refuses a boundary inside a fence, one followed by an indented line (the halves are parsed separately and marked re-bases indentation), and one at the very end of the text, whose following line has not arrived — so a text ending in a blank line keeps it in the tail for one more delta rather than committing to a boundary it might have to take back. And **memoization is plain string equality, not a length cache**: the head is a prefix that only ever grows, so an unchanged head is an unchanged `[data]` and never reaches the renderer as a change — the invariant a spec pins across every prefix of several fixtures, and the thing to protect if the boundary rules are ever touched. Everything else is as specified: two `<markdown>` instances rather than one, the tail's fence info strings stripped so a half-written code block is not highlighted against a grammar it may not satisfy, the caret on whichever renderer ends the text, and at settle one canonical render of the untouched source that a spec asserts is character-identical to a single full render. The pair is wrapped in one element so the growing answer is spaced exactly like the single render that replaces it. Two consequences for later stories: rendered output is `innerHTML`, so its styles are global (`src/styles/_markdown.scss`, scoped by `.assistant-turn__md`) and the caret is a pseudo-element; and the renderer's write lands a microtask after change detection, which is what forces US-606's `ResizeObserver`.
- **Acceptance criteria**:
  - Given accumulated text, when it is split for rendering, then the split point is the last block boundary (`lastIndexOf('\n\n')`), except inside an unclosed fence — an odd count of line-start triple backticks — where the fence open is the split point.
  - Given the head, when it is rendered, then it is memoized by head length and re-rendered only when the head grows; only the tail is re-rendered per flush.
  - Given `ngx-markdown` re-renders its whole `[data]` input on every change, when a streaming answer renders, then the head and the tail are **two separate** `<markdown>` instances — a stable head and a volatile tail — never one instance bound to the accumulating string.
  - Given the tail, when it is rendered mid-stream, then syntax highlighting is skipped because it may be a partial code block.
  - Given a turn is streaming, when the tail renders, then an 8×17px `--accent` block with a 2px radius trails the last character, animating `blink 1s steps(1) infinite` (frame `1b`).
  - Given `Finished`, when it arrives, then the whole buffer is rendered once, canonically, with highlighting — and a unit test asserts that this canonical render is character-identical to a single full render of the same source.

#### US-603: Copy a code block

- **Story**: As a user, I want a copy button on each code block so that I can take code without selecting it by hand.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-601
- **Status**: ✅ Done (2026-08-13). The custom-renderer branch, because `ngx-markdown`'s `clipboard` directive fails all four of the story's own constraints: it needs a `clipboard.js` dependency the app does not carry, it grafts its toolbar on with `document.createElement` _after_ the render — the thing criterion 1 forbids — it copies `innerText` rather than the source, and it would rebuild a component and a `ClipboardJS` instance per `<pre>` on every one of the tail's ~16 ms flushes. So `renderer.code` emits frame `1b`'s chrome directly, which **costs two entries in the closed DOMPurify profile**: `div` and `button`. That widening hands model text no new capability — raw HTML never becomes markup at all, `renderer.html` dropping it at the parser — and it stops there: `span` stays out for the reason it always was, and a spec pins that an `onclick` or a `style` on a button is still stripped. **It is also the one place US-601's two layers stop being independent, and that is stated rather than glossed**: every other entry in the profile is safe whether or not the parser layer holds, while these two are safe _because_ it holds, since a forged `.md-code` wrapper is something the delegated copy listener would serve. A spec drives a raw `<div class="md-code"><button class="md-code__copy">` through the real pipeline so that regression fails a test instead of shipping. The info string is **restricted rather than escaped**: only `[\w+#.-]+` becomes a `language-*` class, so ` ```ts"onload=… ` cannot reach an attribute in the first place. The marker goes on the `<pre>` as well as the `<code>` — Prism's canonical markup, and what `_prism.scss` matches on; the wrapper takes over the border and the radius, since the head bar and the body share one rounded outline that only the element around both can draw, and it states the code surface itself so a fence with no info string (every fence in the streaming tail, after `stripFenceInfo`) does not paint one frame on the page background. Copying is **one delegated listener on the transcript host**, which is also the only thing that survives the tail's re-render; it reads the block back off the DOM, where Prism's highlighting has added elements but no characters, and strips the newline marked appends. The confirmation takes **two channels, because neither serves both audiences**: the visible label swaps to "Copied" for 2 seconds (`StoppedCard`'s window), written straight to the DOM since the button lives inside `innerHTML` where Angular has nothing to bind — but the label is not the accessible name, because six code blocks would otherwise offer six controls all called "Copy" and nothing to tell them apart in a rotor. Each button is named `Copy <language> code block` instead, and the confirmation goes through its own `role="status"` region, separate from the transcript's existing one, which is deliberately emptied for the length of a turn while a copy can happen mid-turn. That region is also what survives the streaming tail replacing the button within a flush. **The message is set one render late, and that is load-bearing**: a live region announces a _transition_, not a value, and change detection is scheduled rather than synchronous — clearing and re-setting the signal in one task leaves the binding's previous value untouched, so copying a second block inside the first's two-second window writes nothing to the DOM and says nothing. A spec observes the region with a `MutationObserver` for exactly that, because reading its text would pass on the stale value. The accessible name follows the visible label into "Copied" and back, or SC 2.5.3 fails for those two seconds and speech input has nothing to activate. **The board's `bi-copy` glyph is not in the bar**: the icon set is an SVG sprite consumed through `<use>`, and allowing `svg`/`use` into the profile is a larger surface than a Copy control is worth. Two literal hexes from the board became `--code-head-fg` and `--code-head-muted` in the app's extra token maps, identical in both themes like the rest of the `code-*` palette, because `--code-head` is dark in both.
- **Acceptance criteria**:
  - Given a rendered code block, when it appears, then the copy affordance is part of the rendered output — either `ngx-markdown`'s `clipboard` directive with its `clipboardButtonComponent` or a custom renderer — and is never grafted onto the DOM after render.
  - Given a hand-rolled control is used in place of the directive, when the transcript is inspected, then one delegated listener on the transcript container handles every copy click, not a listener per `<pre>`.
  - Given a copy, when it succeeds, then the exact block source is on the clipboard and a confirmation state shows for 2 seconds.
  - Given either implementation, when the code block renders, then US-604's chrome is preserved — the `--code-head` bar with its language label on the left and the Copy control on the right.

#### US-604: Read code in a theme that matches the page

- **Story**: As a dark-mode user, I want syntax highlighting that is legible in dark mode so that code blocks are not a white rectangle.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-105, US-603
- **Status**: ✅ Done (2026-08-14), and **most of it had already shipped inside US-603** — which is what makes the interesting part of this story the second criterion rather than the first. `docs/design/project/theme.css` fixes `--code-bg` and `--code-head` **dark in both themes** (#0B1F33 / #081827 and #122A42 / #0F2438), `scripts/check-tokens.mjs` enforces that parity against the design bundle, and §9's rule is that the board wins on everything but accessibility. So there is no light Prism variant to swap to and **there will not be one**: `_prism.scss` is a single hand-authored palette tinted entirely from custom properties, and a theme change re-tints code on the identical paint that flips `data-bs-theme` — which is also why criterion 3 is free, with no `<link>` to swap and therefore no light-styled first frame to see. Agreed with the product owner before implementation. What that leaves is a criterion resting entirely on twelve colour values, and **a criterion that is only true by inspection is a criterion that drifts** — so the work is the enforcement. `check-tokens.mjs` now measures every code-surface pair in both maps: the seven Prism token colours and `--code-fg` against `--code-bg`, the two head-bar colours against `--code-head`, **and against `--code-head` composited with the hover wash**, because `.md-code__copy` sits on a white 8% overlay while hovered or confirming and the unhovered figure overstates the margin the reader actually has (4.72:1 light, not 5.99:1). `--accent` is measured too, at SC 1.4.11's 3:1, since the focus ring and the streaming caret are non-text marks on a code surface. Twenty-six pairs, all clearing their minimum, worst case 4.72:1. The list is hand-written, so it is also checked _against the stylesheets_: a `var(--code-*)` in `_prism.scss` or `_markdown.scss` that no pair measures fails the build, which is what stops a repointed token leaving the gate measuring a colour nothing renders. `check-forbidden-apis.mjs` gained the other half — any import of `prismjs/themes/`, unanchored so `~`, `node_modules/…` and `url(…)` spellings are all caught, **and scanned in `angular.json` as well as `src/`**, because the `styles` array is the route ngx-markdown's own README prescribes and the one no other gate can see.
- **Acceptance criteria**:
  - Given a rendered code block, when it appears, then a `--code-head` bar carries the language label in JetBrains Mono on the left and a Copy control on the right, over a `--code-bg` body in `--code-fg` at a 10px radius (the `Transcript` component, frame `1b`).
  - Given a theme change, when it is applied, then the Prism stylesheet swaps between its light and its dark variant.
  - Given a dark theme on first load, when the first code block paints, then it is already using the dark stylesheet — no light-styled frame is painted.
  - Given both themes, when a code block is measured, then its foreground-to-background contrast meets WCAG 2.1 AA at 4.5:1 for normal text.

#### US-605: Render diagrams and math without paying for them upfront

- **Story**: As a user, I want diagrams and formulas to render when a response contains them, without every other page load carrying the cost.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-601, US-108
- **Status**: ✅ Done (2026-08-14), with **two interpretations of the criteria recorded rather than glossed**. The first is that `ngx-markdown`'s own `mermaid` and `katex` integrations are **not used**, exactly as US-603 declined its `clipboard` directive and for the same class of reason: they resolve from `window` globals and throw from an unawaited async method if either is missing; `extendsRendererForMermaid` mutates the _shared_ `MarkedRenderer` and marked's global state permanently and idempotently, so one component enabling it enables it for every instance in the injector; it interpolates the diagram source **unescaped**, which mangles any label containing `<`; and `mermaid.run()` has no error hook at all — it wipes the element and paints its own error graphic, taking the source off the page, which makes criterion 4 unreachable on that path. So the branch lives in the renderer this app already owns. `renderer.code` stays a **pure function of the token** and reads no configuration: its whole change is adding `md-diagram` to the otherwise-identical `.md-code` chrome. That one class is what makes criterion 3 cost zero code — with the flag off nothing runs, so the block _is_ a code block and there is no disabled branch to get wrong — and criterion 4 cost zero reconstruction, since the escaped source was never removed. The second interpretation is **when**: `appMarkdownExtras` rides the settled render only. The tail cannot carry it (`stripFenceInfo` has removed its info strings), and the head must not — its content is replaced wholesale whenever a block closes, and re-laying-out an SVG per flush would fight both the batch cadence and US-606's observer. A diagram therefore reads as a `mermaid`-labelled code block while the answer arrives and becomes a picture at settle, which is the contract US-602 already set for highlighting. **`CHAT_ALLOWED_TAGS` and `CHAT_ALLOWED_ATTR` gain nothing.** Mermaid's SVG is not markdown and never meets that profile — like Prism's markup it is written _after_ it — so it gets a second, independent DOMPurify call in `diagram-svg.ts`, subtracting `a`/`href` (Mermaid's `click` syntax would mint a link to an origin FR-51 forbids) and `foreignObject` (the one element that opens an HTML document inside an SVG one). **`style` is kept, and that is what makes the CSS pass necessary**: DOMPurify has no CSS parser, so it hands a `<style>` body and a `style` attribute through verbatim — and a `<style>` inside _inline_ SVG is document-scoped, not SVG-scoped. Mermaid's `themeCSS` is a directive-settable key, so `%%{init: {"themeCSS": "…"}}%%` inside a model-authored fence reaches that element, and Mermaid's own guard is a brace-balance counter that explicitly permits `url(https://…)`. A hook therefore strips `@import` and every non-fragment `url(` from both, which is what makes the FR-51 guarantee true at this boundary rather than borrowed from a dependency; the hooks are removed in a `finally`, because they are global to the DOMPurify instance `sanitizeChatMarkdown` shares. Mermaid runs `parse()` first and never renders a source that failed it, under `securityLevel: 'strict'`, `htmlLabels: false`, `suppressErrorRendering`, and bounded `maxTextSize`/`maxEdges`; the `bindFunctions` it returns is never called. Its palette comes from `getComputedStyle` at render time rather than transcribed hexes, so there is nothing new for `check-tokens.mjs` to police — with every empty value **dropped**, because a renamed token and a test environment both hand back `''` and Mermaid given `fill: ""` draws invisible nodes. An SVG cannot re-tint, so a theme flip re-draws the diagrams on screen, which is possible only because the source `<pre>` is hidden rather than removed: it is the render cache as well as the fallback. **Math is captured at the token layer, and a DOM pass over rendered output could not have worked** — which the review proved rather than argued, by running the real pipeline. Three separate things in it defeat `katex/contrib/auto-render`, the mechanism `ngx-markdown`'s own integration uses: `\(`, `\)`, `\[` and `\]` are all in marked's inline **escape** class, so `\(x^2\)` reaches the DOM as `(x^2)` and delimiters configured for those forms match nothing, ever; `breaks: true` puts a `<br>` between the markers of a `$$…$$` opened and closed on separate lines — the commonest display form there is — and auto-render only joins _consecutive text nodes_, so an element between them ends the run; and marked strips LaTeX's own escapes, so `\{`, `\}`, `\_` and the `\\` row separator vanish and sets, `aligned` bodies and escaped subscripts render **wrong** rather than not at all, which is worse because nothing reports it. A marked extension runs before every one of those rules and takes the source verbatim, which is the only point at which the expression still exists as the model wrote it; it emits an escaped placeholder that `MarkdownExtras` hands to `katex.renderToString`. The block tokenizer's `start` is **line-anchored**, and that is load-bearing rather than tidy: `start` tells marked where a block may begin and marked cuts the paragraph there, so an unanchored match splits "an expression: `$$x^2$$` inline" into three lines that `breaks: true` then joins with `<br>`. `trust: false` and `throwOnError: false` are stated rather than defaulted, `errorColor` is **omitted** rather than passed empty when `--fail` cannot be resolved (KaTeX takes `''` literally and would render a malformed expression indistinguishably from a good one), and there is **no single-`$` form** — "it costs $5 to $10" is the classic false positive and prose is far commoner in an answer than inline math. A `$$` inside a fenced block is untouched for free, because a fence is a different token. Its stylesheet and twenty woff2 faces are copied into `public/vendor/katex` by `copy-katex.mjs` and linked on demand, so the 24 kB never enters the `styles` bundle — checked against the document rather than a field, since this service is route-scoped and would otherwise append a link per visit, and with an `error` listener because that directory is generated rather than committed and a deployment missing it renders math unstyled, which is worse than not rendering it and silent without one. A drawn diagram is given `role="img"` and a name on its figure with the SVG marked `aria-hidden`, because DOMPurify strips Mermaid's own `role` and what is left is otherwise a bag of loose `<text>` runs; the source stays reachable through the Copy control above it. `check-initial-chunk.mjs` gained the two things its existing entries could not do: a **second traversal rooted at the chat chunk**, because a static import there would be off the initial graph and still downloaded by everyone who opens a conversation, and a **positive presence assertion**, because every check above passes when a library is simply absent — so deleting the dynamic import would have turned the gate green and silent. Initial bundle unchanged by the libraries themselves. **One limit is worth stating: no automated test in this repository will ever run real Mermaid** — jsdom implements no `getBBox`, `getComputedTextLength` or `document.fonts` — which is why the imports sit behind injection tokens, and why the manual pass with both flags on is load-bearing rather than optional. **That pass was run against a live API on 2026-08-14 and found nothing**: diagrams drew and redrew on a theme change, a broken source kept its text and took the notice, and all four math forms typeset while the two false-positive cases stayed prose. `public/config.json` now ships both flags **`true`** at the product owner's direction — a deployment decision the flag exists to make, and one that changes no bundle, since both libraries are still reached only by dynamic import.
- **Acceptance criteria**:
  - Given a response containing a Mermaid fence or math notation, when the flag for that feature is enabled in `config.json`, then `ngx-markdown`'s `mermaid` or `katex` integration is loaded by dynamic import at that moment and the content renders.
  - Given a build, when the initial chunk is analysed, then neither the diagram nor the math library is present, and the budget check fails the build if either enters it.
  - Given the flag is disabled, when such content appears, then the source renders as a plain code block rather than an error.
  - Given a diagram that fails to parse, when rendering is attempted, then the raw source is shown with an inline notice, not a blank area.

#### US-606: Stay with the latest message, or stay where I am reading

- **Story**: As a user, I want the transcript to follow the newest content unless I have scrolled up to read, in which case I want a way back down.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-406
- **Status**: ✅ Done (2026-08-13). Bottom detection is the specified `IntersectionObserver` on a zero-height sentinel with `rootMargin: '0px 0px 80px 0px'`, and the container sets `overflow-anchor: none` (plus `overscroll-behavior: contain`, so a flick at either end does not scroll the page behind the transcript). **The following is driven by a `ResizeObserver` on a content wrapper, not by the turn's signals** — the criteria do not say, and the naive reading is wrong: the markdown renderer writes its output a microtask after change detection returns, so a scroll taken during the render phase measures the height the page had _before_ the text it is meant to follow, and the transcript trails the answer by a flush. Watching the content means the scroll happens when the geometry actually changes, whatever produced it, and it cannot loop because moving `scrollTop` changes no element's size. Three details worth keeping: the sentinel is passed into the directive and lives outside the body's `@if` chain, so one element and one observation survive the error/transcript/empty-state swaps; `_atBottom` starts `true`, because a real observer's first record is asynchronous and treating "not yet reported" as "scrolled up" flashes the control on every mount; and the control **stays visible after the turn ends** (it merely stops pulsing), because `Finished` deliberately does not scroll and hiding it then would strand the reader. Pressing it scrolls, resumes pinning, sets the flag in the handler so the control disappears on the same change detection, and hands focus to the scroll container (`tabindex="-1"`) — otherwise focus falls to `<body>` when the control removes itself.
- **Acceptance criteria**:
  - Given a turn streaming and the user at the bottom, when new content arrives, then the transcript stays pinned to the newest content.
  - Given the user scrolls up during streaming, when new content arrives, then the view does not move and a 44px circular jump-to-latest control appears centred above the composer on `--surface` with a down arrow in `--brand`, pulsing on `ringpulse 1.8s ease-out infinite` for as long as the turn streams (frame `1b`).
  - Given the jump-to-latest control, when it is pressed, then the view scrolls to the bottom, the control hides, and pinning resumes.
  - Given a turn completes while the user is scrolled up, when `Finished` arrives, then the view does **not** jump to the bottom.
  - Given the implementation, when inspected, then bottom detection uses an `IntersectionObserver` on a zero-height sentinel with `rootMargin: '0px 0px 80px 0px'` rather than a `(scroll)` binding, and the container sets `overflow-anchor: none`.

#### US-607: Copy a prompt or a response

- **Story**: As a user, I want to copy what I asked or what I got so that I can paste it elsewhere.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-601
- **Status**: ✅ Done (2026-08-14). One `MessageCopy` component serves both messages, and it is a **real template-bound button**, which is the whole reason it needs no live region where US-603's does: that one is minted inside rendered `innerHTML` where Angular has nothing to bind, offers six identically-named controls per answer, and is replaced by the streaming tail within a flush. This one exists only on a settled turn, is named per subject (`Copy prompt` / `Copy response`), and changes that name on the element the reader just activated — `StoppedCard`'s existing reasoning, now shared with it. The label and the accessible name derive from the _same_ constants so SC 2.5.3 holds by construction rather than by convention, and `COPY_CONFIRMATION_MS` moved beside `copyText` in `core/clipboard/` because three controls are on screen at once and two confirmations of visibly different lengths read as one of them having failed. **US-504's gate inverts**: the footer is unconditional for a settled turn and the _counts_ are the conditional child, since Copy is offerable whether or not a `Finished` ever arrived — but the control itself is gated on there being text, because a turn can settle empty (a cut-off that died after its first activity) and `writeText('')` **succeeds**, so an ungated control would confirm having wiped the reader's clipboard. The reveal is `opacity` with `:hover`, `:focus-within` and `@media (hover: none)`, never `@if`: opacity keeps the control in the tab order, which is what criterion 3 means by reachable, and US-606 follows the answer with a `ResizeObserver` that a height change on hover would move. A third selector holds it open while a copy is confirming, because clicking can leave both the pointer and focus elsewhere and a "Copied" nobody can see is not a confirmation. The same reasoning made the **optimistic user bubble render an empty footer**: without it the prompt row grows by ~28px the moment its turn settles, and the whole column below it shifts under the reader. The assistant copies `snapshot.text` rather than re-joining `renderedNodes()`, whose slices belong to the timeline's contract, so a replayed or cut-off turn copies exactly what it holds.
- **Acceptance criteria**:
  - Given a completed turn, when the user hovers the user bubble or the assistant message, then the message footer appears carrying a Copy control and, on the assistant message, the turn's token counts in JetBrains Mono (frame `1g`).
  - Given the assistant copy control, when it is used, then the original markdown source is copied, not the rendered HTML.
  - Given a copy, when it completes, then a confirmation state shows for 2 seconds; the control is reachable by keyboard and the footer is revealed on focus as well as on hover.

### EP-7: Conversation library

#### US-701: Find a conversation by name

- **Story**: As a user with many conversations, I want to search them by name so that I can find one without scrolling.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-302
- **Status**: ✅ Done (2026-08-14). The server-side `name=` search already existed — US-302 built it — so what this story actually delivered is the `/conversations` screen the sidebar has been deliberately withholding a nav entry for, and the URL contract underneath it. **The screen gets a store of its own**, `ConversationLibraryStore`, rather than reusing the root `ConversationListStore`, and that is the one architectural decision here. They query the same endpoint but are not the same list: frame `4a` draws both search fields at once, so shared state would retype the sidebar's and narrow its rows; `withOffsetPagination` holds exactly one offset, which cannot serve a 50-row sidebar and a 25-row table; US-703's `isFavorite` would narrow the sidebar with **no control there to undo it**; and criterion 3 makes the URL authoritative for _this route_, while a root store outlives it — a deep link would leave the sidebar filtered after navigating to `/chat`. The two stay in step through the existing `conversationEvents.updated`/`.deleted`, server-confirmed facts only, which is what that event group is for. It copies the reference store's shape and its reasons, dropping only the `_started` release sentinel: the sidebar's list exists before any screen asks for it, while this one is created by the route that shows it and always has a term to run. **The URL is the source of truth**, the field is a view of it, and the store caches the rows it produced — one writer, one reader path, and the back button restoring the previous result set is then a property of the router rather than code. `bindQuery` takes the input **signal**, not its value, which is what makes a deep link one request carrying the term instead of an unfiltered request followed by a filtered one. The loop cannot feed itself because `SearchInput`'s `linkedSignal` compares the re-seeded text against the value it already holds and emits nothing; two rules keep that true and are stated in the component — bind `[value]` one-way, and never navigate from an effect. History is the subtle part: **refining a term replaces the entry, adding or removing one pushes**. Always-replacing fails criterion 3 outright, since back would leave the screen rather than restore the unfiltered list; always-pushing walks the reader back through their own typing one debounce at a time. Pushing at exactly the two transitions a reader thinks of as a state change also makes _clearing_ a search undoable. Rows carry the name and the model only. The favourites filter (US-703), the checkbox, select-all and bulk bar (US-704), the "Showing N of M" counter and Load more (US-702), and the project chip (US-307) are each absent with a comment naming their story — as is any sort control, which is US-705's whole subject and stays absent until US-706 gives the server one to honour. Initial bundle **663.30 kB / 163.25 kB transfer**, with the warning line re-stated 665 → 670 kB and the 720 kB ceiling untouched; the screen and its store are behind the lazy route.
- **Acceptance criteria**:
  - Given the conversations screen, when the user types in the search field, then the request is debounced and sends `name=` — a request carrying `filter=` fails this criterion.
  - Given a search returning nothing, when results render, then frame `4b`'s empty state renders — the ridgeline motif, a heading naming the term, the line "Search covers conversation names only.", and a Clear search action.
  - Given a search term, when it is applied, then it is reflected in the URL query params so the filtered list is shareable and the back button restores the prior state.

#### US-702: Page through my conversations

- **Story**: As a user, I want to load more conversations beyond the first page so that older ones are reachable.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-701
- **Status**: ✅ Done (2026-08-14). The distinction the whole story rests on is that **a query replaces the rows and a page appends to them**, so `loadingMore` is state of its own rather than a second reading of `isPending`: a next page must not blank a table the reader is already looking at, and a narrowing search must not silently become an append. `_loadNextPage` is a second `rxMethod` on `exhaustMap` — a second press while a page is on the wire is a double-click, not a request for the page after it, and `skip` has not moved yet, so letting it through would fetch the same window twice — patching with `addEntities` and `setPage`, which advances `skip` by what actually came back rather than by `take`, so a short page cannot leave a gap. Two guards keep a page inside the result set it belongs to. `_querySuperseded$` (a `Subject` completed in `onDestroy`, merged into the request's `takeUntil` alongside `signedOut$`) cancels a page a **later** query overtook; and `loadMore()` refuses while `isPending()`, which is the load-bearing half — a page started _after_ a query was already in flight carries the old `skip` against the new result set, and landing after `setFirstPage` it would append a window from the middle of a list that no longer exists, leaving a hole the reader can never reach with `hasMore()` reading false. A failed page raises a **toast rather than `setError`**, because the rows already loaded are still valid and swapping the table for an error panel would discard a result set the reader can still use; a failed first query still gets the panel. The counter is `countSummary`, whose loaded half counts the rows on screen rather than reading `skip` — the two agree after a page, but a row deleted from anywhere decrements both counters and counting rows stays honest whichever path moved them. It is rendered **`aria-hidden`** in the toolbar and the same string is handed to `DataTable`'s new optional `summary` input, so there is one announcement from the table's existing live region instead of two counts that will eventually disagree; it is withheld during the first load ("Showing 0 of 0" is not information) and beside an error panel, and kept during a narrowing search alongside the rows it still describes. Load more sits in `DataTable`'s `[tableFooter]` slot with the **`@if` inside the projected `div`** — content projection matches static nodes, so an `@if` around it compiles to an `ng-template` with no `tableFooter` attribute and is dropped silently. The control disappears rather than being disabled once everything is loaded, and because that leaves the reader standing on a node that no longer exists, focus moves to the **last** row — where the vanished button was — but only when focus actually fell through, and only once `hasMore()` is false, so a reader still paging is not pulled off the control they are pressing.
- **Acceptance criteria**:
  - Given the first page, when it renders, then a counter at the right of the toolbar reads "Showing N of M conversations" from the `totalCount` in the envelope (frame `4a`).
  - Given more results exist, when the user presses Load more, then the next page appends in server order and the counter updates.
  - Given every result is loaded, when the list renders, then the Load more control **disappears** rather than being rendered disabled (frame `4a`).
  - Given `take`, when requests are built, then it never exceeds 100, because the server clamps it.

#### US-703: Filter to my favorites

- **Story**: As a user, I want to see only my favorite conversations so that I can reach the ones I use most.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-701, US-305
- **Status**: ✅ Done (2026-08-14). The query widened from a term to a `ConversationLibraryQuery { name, favorites }` bound as **one** `rxMethod` computation, so a deep link carrying both parameters is still one request rather than two — binding them separately would have fired a second query for every change to either. The URL parameter is **`favorites=1`, not the API's `isFavorite=true`**, and that is a deliberate refusal to make the address bar a proxy for the request underneath it: `isFavorite=false` is a meaningful value on the wire ("only the ones I have _not_ starred") that this screen has no control for and no way back out of, so a URL able to spell it would invite a link the client cannot honour. `searchUrl` is the single translation point and only ever sets `true`; the input transform reads anything other than the one value the screen writes as off, so a hand-edited `?favorites=yes` cannot put the toolbar and the request out of step. The toggle carries `aria-pressed` because its text never changes, and it **always pushes** a history entry — a discrete on/off is always the add-or-remove transition `shouldReplaceHistory` exists to push at — so back undoes turning the filter on as well as off, and `queryParamsHandling: 'merge'` lets a search and the filter survive each other. The row star calls the shared `ConversationActionsStore.toggleFavorite`, and renders on the server's confirmation rather than optimistically: this store owns its own entities, the actions store cannot patch them, and a row past the sidebar's first 50 is not in the list it _can_ patch, so an optimistic flip would have no rollback behind it — the busy state carries the feedback, on `aria-disabled` rather than the native attribute, which would drop a keyboard reader onto `<body>` for the round trip. The story's second criterion resolved into an **eviction**: when the filter is on and a server-confirmed `updated` says `isFavorite: false`, the row leaves through the same `_dropRow` a deletion uses. Patching in place would leave a hollow star inside a favourites-only list with the counter already disagreeing, and refetching instead would discard every page the reader had loaded. There are now three empty states rather than two, because a favourites-only filter with nothing behind it is a different situation with a different way out and no term for frame `4b`'s heading to quote: "No favourite conversations" / "Star a conversation and it will appear here." / **Show all conversations**. Copy is British throughout, matching the sidebar kebab and the chat header star rather than the board's US "Favorites" — a reader hearing "Favorites" from this button and "Unfavourite Helios" from the star beside it is the worse of the two inconsistencies.
- **Acceptance criteria**:
  - Given the favorites filter, when enabled, then `isFavorite=true` is sent on the search request and the list narrows.
  - Given the filter is active and the user unfavorites a row, when the list refreshes, then that row leaves the filtered list.
  - Given the filter state, when it changes, then it is reflected in the URL query params.

#### US-704: Delete several conversations at once

- **Story**: As a user cleaning up, I want to select multiple conversations and delete them together so that I do not confirm one at a time.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-702, US-106
- **Status**: ✅ Done (2026-08-14). **The request lives in the root `ConversationActionsStore.deleteMany(ids, onRollback)`, not in the route-scoped library store**, and that is the story's one architectural decision. `rxMethod` binds its subscription to the injector that declares it, and the library store is provided on a route component — so a reader who confirmed a delete and then clicked a sidebar row would have had the request torn down mid-flight, with nothing deleted, nothing restored and nothing said. The flow therefore splits along what each side can see: `core/` owns the request, the per-id pending flags, the sidebar's own row removal, one toast for the **batch** (never one per row), and a `conversationEvents.deleted` per id on the 204; the library keeps only its own optimistic `takeRows`/`restoreRows`, handed over as an `onRollback` **callback** because `features → core` is the permitted direction and a store in `core/` cannot reach a route-scoped one. Two failure-path details are load-bearing and both came out of review: the sidebar restore runs **LIFO**, because `removeRow` captures each index against the list as it stands at that moment, so a batch shrinks it one row at a time and front-to-back would turn `[A, B, C]` minus `[A, B]` back into `[B, A, C]`; and `restoreRows` refuses a restore whose `_queryGeneration` no longer matches, rather than splicing the old query's rows into a result set the reader has since replaced — when it does restore, it re-selects the rows so Retry is one press. Selection itself lives in `withLinkedState` reconciled against `ids`, which is what makes a new query empty it, a Load more keep it, and a row leaving by **any** route drop out of it with no second call site — written as plain state, each of those needs its own line and the one that gets forgotten leaves the bar counting rows that are not there. `DataTable` gained `headerSelectAll` (frame `4a` puts select-all in the toolbar, and two controls bound to one state is a defect — below 768 px there is no header row to hold one at all) and a card-branch row checkbox in `CardRow`'s previously unused `[cardRowLead]` slot, so selection exists on a phone rather than silently vanishing there. `DELETE api/conversations/bulk` needs its body **inside the options object**: `HttpClient.delete` drops a body passed any other way and the endpoint binds `[FromBody]`, so the server would see an empty id list and answer 400. A new dumb `DeleteConversationsDialog` (`count`/`open`/`confirmed`) confirms first — inputs rather than a store, unlike its single-row sibling, because it has one invoker and the same "delete N selected rows" shape is what US-908 will want — counting rather than naming, landing initial focus on Cancel, and repairing focus to `#main-content` afterwards because confirming destroys the bar the modal would otherwise return to. The bar itself sits outside the error branch, since a failed _page_ leaves the rows and the selection intact.
- **Acceptance criteria**:
  - Given the list, when any row checkbox is checked, then a floating pill bar appears centred over the bottom of the screen showing "N selected", a Clear action, and a red "Delete selected" action (frame `4a`).
  - Given a bulk delete, when confirmed, then `DELETE /api/conversations/bulk` is called once with all ids and the rows are removed optimistically.
  - Given a bulk delete that fails, when the error returns, then every selected row is restored and one error toast is raised — not one per row.
  - Given the select-all control in the toolbar, when used, then it selects only the rows currently loaded, and the pill bar carries the required copy "Select-all covers the 24 loaded rows only" with that count substituted (frame `4a`).

#### US-705: Understand why conversations cannot be sorted

- **Story**: As a user, I want the app to be honest that it cannot reorder this list, rather than offering a sort that silently reorders one page.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-702
- **Status**: ✅ Done (2026-08-14) — **and deliberately interim: the third criterion is US-706's, and US-706 retires what shipped here.** Regime **B** is now _stated_ rather than left to be inferred from an absence: a muted "Newest first" sits at the far end of the toolbar carrying the explanation as visually-hidden text ("Conversations are listed newest first. This list can't be reordered."), with an info button repeating it as a `Tooltip` flyout for the reader who is looking rather than listening — shown on focus as well as hover and dismissed by Escape (WCAG SC 1.4.13), and given its own short `aria-label` so a screen reader does not announce the whole sentence as a button name. Below 768 px the statement is **clipped, not `display: none`d**: hiding it would take the visually-hidden explanation out of the accessibility tree with it, leaving the criterion unmet on every phone rather than merely unseen. The absence itself is locked by test — no sort control anywhere in the toolbar, and no client-side reordering applied to a partially loaded list across a Load more — so a later story cannot add one without deciding to. `ORDER_EXPLANATION` in `conversations-route.ts` is the single anchor US-706 replaces with a real control issuing `sort=`/`dir=`. Two shared-surface fixes landed alongside, both WCAG SC 1.4.10 Reflow: the toolbar now wraps (five controls with a combined intrinsic width well past a 360 px viewport, and only the search field can shrink), and `BulkActionBar` — `position: fixed`, so no call site can rescue it — caps at `calc(100vw - 24px)`, wraps and centres, letting the note take the second line ahead of the count or the actions. EP-7's four stories added nothing to the initial graph: **663.45 kB raw / 163.11 kB transfer**, level with the US-701 baseline, with the 670 kB warning line and 720 kB ceiling untouched.
- **Acceptance criteria**:
  - Given the conversations screen, when it renders, then no sort control appears anywhere in the toolbar — frame `4a` carries only the name search, the Favorites filter, and select-all — because the list is in server order.
  - Given a partially loaded list, when the user has pressed Load more, then no client-side reordering has been applied to the combined set.
  - Given US-706 has landed, when the screen renders, then a sort control appears and issues `sort=` and `dir=` to the server.

#### US-706: `[enabler]` Accept sort parameters on the paginated list endpoints (B8)

- **Story**: Add `sort` and `dir` query parameters to conversation search, project search, and user search, so paginated lists can be ordered without the client draining every page first. No endpoint accepts a sort parameter today. Unblocks US-705, US-908, and honest sorting on admin users.
- **Priority**: P2 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-20) — every criterion, plus a defect none of them named. New `Enterprise.Gpt.Service/Sorting/` folder, modelled on `Service/Export/ConversationExportFormatNames.cs`: `SortParameters` is the shared parser, and three thin static classes — `ConversationSortKeys`, `ProjectSortKeys`, `UserSortKeys` — each write their accepted tokens down three times over (an enum, a `Supported` list, a case-insensitive lookup), with `SortKeysTests` the only thing keeping the three from drifting apart. Keys: conversations `created|name`; projects `created|updated|name|favorite`; users `name|email|created`. A rejected `sort` or `dir` is keyed by the lowercase query-parameter name it came from rather than a `nameof` — the `format` precedent from the export route — so a client can render the failure against the control that raised it. **Omitting both parameters reproduces the listing's pre-existing `ORDER BY` byte-for-byte**, tie-break included, which for conversations and users was no tie-break at all; asking for any order adds one on `Id`, and it follows the *requested* direction rather than being fixed — a review round caught this as a real defect rather than a style choice: a fixed tie-break made ascending and descending collapse onto the same rows on a screen where many entries share one name, most visibly every untitled conversation. No migration and no new index: the project listing already sorts after its own seek, `UserConfiguration` carries no indexes at all, and US-1205's `permissionId` filter probes the existing filtered grant index rather than adding one. `dir` and `permissionId` joined `RequestLoggingOptions`'s safe-query-parameter allowlist alongside the pre-existing `sort`, because neither identifies a person. `GET /api/users` accepts the identical pair, which makes **US-1201's fourth criterion historical**: "accepts no sort parameter — regime D" described the API this story replaces, not the one it leaves behind; the admin users tab still offers no control over it, but that is now a screen-level gap this enabler unblocks rather than a limit of the endpoint. 75 new unit and 21 new integration tests. See [Conversation Usage and Favourites §5.6](../conversations/usage-and-favorites.md#56-ordering--sort-and-dir-us-706), [Projects §3.6](../projects/project-management.md#36-ordering--sort-and-dir-us-706), and [User Management §2](../users/user-management.md#2-api-surface).
- **Acceptance criteria**:
  - Given `GET /api/conversations/search`, `GET /api/projects`, and `GET /api/users`, when called with `sort` and `dir`, then results are ordered server-side and the `PaginatedResponseDto` envelope is unchanged.
  - Given an unrecognised `sort` value, when the request is handled, then it returns 400 as a validation problem naming the parameter, rather than silently ignoring it.
  - Given the parameters are omitted, when the request is handled, then the current default ordering is preserved byte-for-byte, so existing clients see no change.
  - Given the change, when integration tests run, then each endpoint has a test asserting order for at least one ascending and one descending case.

### EP-8: Attachments, upload, and download

#### US-801: Attach files to a conversation

- **Story**: As a user, I want to attach documents to my conversation so that the assistant can answer questions about them.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-204, US-401
- **Acceptance criteria**:
  - Given the composer, when the user uses the paperclip control or drops files onto the prompt box, then a chip per file shows an extension-appropriate icon, the truncated name, the human-readable size, and a remove control.
  - Given a drag over the prompt box, when it starts, then an overlay at `inset: 4px` with a 2px dashed `--accent` border over `rgba(33,168,216,.07)` reads "Drop file(s)" beside an upload glyph, and it clears on drop or drag-leave (frame `2a`).
  - Given attached files, when the turn is sent, then each is posted to `POST /api/documents/conversations/{conversationId}` and a job id is returned per file.
  - Given `GET /api/documents/file-extensions`, when the picker opens, then it constrains selection to the supported list rather than accepting anything and failing server-side.
  - Given attachment state, when it is held, then chips are immutable records in `UploadStore` — a mutated plain object would not trigger rendering under zoneless change detection.

#### US-802: Watch an upload get processed

- **Story**: As a user, I want to see real progress while my document is ingested so that I know whether to wait.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-801
- **Acceptance criteria**:
  - Given a queued job, when polling starts, then `GET /api/documents/upload-status/{jobId}` is called on a 500ms → 1s → 2s → 4s schedule capped at 5s, not a fixed interval.
  - Given successive polls, when progress is applied, then the displayed value never decreases, and the chip moves through frame `4l`'s states — an `--accent` bar with a mono percentage while uploading, then a `spin` ring with "Generating embeddings… 3 of 12 pages" composed from `message`, `completedUnits`, and `totalUnits`.
  - Given `state` reaches `Succeeded` or `Failed`, when it is observed, then polling stops and the terminal state renders — `Succeeded` as a `--ok` check reading "Ready to use", `Failed` as a `--fail` cross carrying the job's `errorMessage` and a Retry action (frame `4l`).
  - Given a job that succeeds, when it completes, then the returned `documentId` is retained so the document can be downloaded without a further lookup.

#### US-803: Tell an expired upload apart from a failed one

- **Story**: As a user returning to an old tab, I want an expired upload job to say it expired rather than claim my file failed.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-802
- **Acceptance criteria**:
  - Given a 404 from the upload-status route, when it is handled, then the chip renders in `--muted` with a question-mark glyph and the copy "Status is only kept for a limited time." — neutral slate, visibly distinct from a `Failed` job's `--fail` cross (frame `4l`).
  - Given an expired job, when it renders, then polling has stopped and the copy explains that status is retained for a limited window.
  - Given the same 404 shape returned for another user's job, when it is handled, then it renders identically to the expired case — the client must not distinguish them, because the server deliberately does not.

#### US-804: Download a document

- **Story**: As a user, I want to download a file I uploaded so that I can retrieve the original.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-802
- **Acceptance criteria**:
  - Given a document row, when the user clicks download, then the signed URL is requested at that moment and immediately used; a list rendering N documents issues zero download requests until a click.
  - Given a fetched `DocumentDownloadDto`, when the download proceeds, then `downloadUrl` is never written to `localStorage`, `sessionStorage`, a router URL, or any log, and the browser saves the file under the signed `Content-Disposition` name via an `<a download>`.
  - Given a 503 `/problems/storage-not-configured`, when it returns, then a message explains that downloads are unavailable in this deployment rather than showing a generic failure.
  - Given a 404, when it returns, then the row reports the document as unavailable without asserting it was deleted.

#### US-805: Understand why a file was rejected

- **Story**: As a user, I want to know why my file was not accepted so that I can fix it.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-801, US-103
- **Acceptance criteria**:
  - Given a 400 `/problems/upload-too-large`, when it returns, then an error toast states the limit from the `maxBytes` extension as a human-readable size — "Files can be up to 50 MB — this one is 212 MB." — carries the `traceId` in JetBrains Mono and a Retry action, and persists until dismissed (frame `4l`).
  - Given a 403 `/problems/permission-required`, when it returns, then the toast names the missing permission from the `permissions[]` extension and tells the user to ask an administrator (frame `4l`).
  - Given an unsupported extension, when the file is selected, then it is rejected client-side against the `file-extensions` list before any upload is attempted, and the chip names both the rejected extension and the supported set (frame `4l`).

### EP-9: Projects

#### US-901: Browse and search projects

- **Story**: As a user, I want a projects screen listing my projects with a name search so that I can find one quickly.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-104, US-106
- **Status**: ✅ Done (2026-08-14) **with two criteria deliberately narrowed**, per a product-owner decision of 2026-08-14. The card's **favourite star is absent**: `ProjectCard` renders one, but no project favourite exists on the wire until US-909, and a control that can be pressed and never persisted is worse than one that is not there — the call US-308 made about this star's conversation counterpart. Frame `4c`'s **sort select is absent** for the same reason at one remove: no endpoint accepts a sort parameter, so the control only becomes honest over the fully materialised set, which is US-902's whole subject. The drain itself shipped — `ProjectListStore` walks pages with `expand` at `take=100` to a 500 ceiling, and states the shortfall in frame `4g`'s neutral wording, because `4d`'s names sorting and would explain a control the reader cannot see. Search is server-side and URL-driven on US-701's exact contract.
- **Acceptance criteria**:
  - Given the projects route, when it loads, then `GET /api/projects` is called and projects render as a responsive card grid — `col-12` / `col-md-6` / `col-xl-4` — each card carrying the folder glyph, name, description, last-modified, a favourite star, and a kebab (frame `4c`).
  - Given the search field, when the user types, then `name=` is sent debounced and the URL query params reflect the term.
  - Given a user with no projects, when the screen renders, then a designed empty state with a create action shows.
  - Given the list, when it loads, then it drains at `take=100` up to a 500-item ceiling so client-side sorting is offered over a fully materialised set.

#### US-902: Sort projects, honestly

- **Story**: As a user, I want to sort projects by last updated, created, or alphabetically, and to be told when the sort covers only part of my projects.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-901
- **Status**: ✅ Done (2026-08-20) — every criterion, with the third resolved as stale rather than as written: by the day this story built the select, US-909 and US-910 had already landed in the same batch and had already ruled out device-local pins entirely, so the fourth option was **server-flag-only from the moment it shipped** — there was never a build carrying the "Pinned on this device — pins don't sync." variant this criterion describes, the identical call US-908 and US-910 each already made about their own interim states. The board's fourth option is relabelled **Favourites first**, not "Pinned" — device-local pins were never built, and the app says favourite everywhere else, the same inconsistency the conversations toolbar had already decided against. Two further departures from frame `4d`, both because the board predates US-706: **the select is never disabled** past the 500-project load ceiling — that state described a client-side sort over a partial set, and US-706 moved the ordering to the server, so the 500 the ceiling admits are the correct first 500 in the chosen order — and the ceiling line is reworded from a sentence naming sorting as unavailable to a truncation naming the order instead: "Showing the first 500 of 1,284 projects, most recently updated first." The grid's default order changes from `dateCreated` desc to **Last updated**, matching what every card's own "Updated …" label already says. `withClientQuery` — which the store's own doc block had forecast composing once a sort control shipped — is **formally not composed**: the premise it was written against (no endpoint accepting an order) is gone, the grid's search was already server-side, and composing it now would add a second filter over an already-filtered set and a comparator nothing would call. `ProjectLookupStore`, the root store behind the picker and the sidebar's favourite-projects section, now drains with `sort=favorite&dir=desc`, which makes that sidebar section **complete** within the 500-project ceiling rather than best-effort — retiring the interim-behaviours row that had booked exactly this completeness to the US-706/US-902 pair. Neither store re-sorts in place on a write: a star pressed under Favourites first, and a project created under any order, both leave the affected row where it already stood — inserted at the head as a guess for a create, left in place for a favourite — and the true order settles on the next query, judged better than re-draining up to five pages out from under the reader's cursor for one click. History follows the search term's own rule (`shouldReplaceHistory`): turning an order on or off pushes, moving between two non-default orders replaces, because a closed `<select>` fires `change` on every arrow key. See [Projects (UI) §4](../ui/projects.md#4-the-grid-us-901).
- **Acceptance criteria**:
  - Given fewer than 500 projects, when a sort of last updated, created, alphabetical, or pinned is chosen, then the full set is reordered and the choice is reflected in the URL query params (frame `4c`).
  - Given more than 500 projects, when the list hits the ceiling, then the sort select renders disabled and an info line above the grid reads "Sorting is unavailable past the 500-project load ceiling — showing the 500 most recent of N" (frame `4d`).
  - Given the pinned sort option, when US-908 has not landed, then it orders by the device-local pins and is labelled as device-local; when US-908 has landed, it orders by the server flag.

#### US-903: Create, rename, describe, and delete a project

- **Story**: As a user, I want full lifecycle control of a project so that my grouping stays accurate over time.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-901
- **Status**: ✅ Done (2026-08-14). Three things the criteria imply but do not state are now explicit in the code. **The duplicate-name copy is the client's, not the server's**: `ProjectService` answers `A project named 'X' already exists.` and this criterion requires "You already have a project with this name.", so `projectNameMessages` swaps that one message and renders every other `Name` failure verbatim. **Opening the edit form from a card costs a round trip**: `ProjectSummaryDto` omits `instructions` entirely, so a form seeded from a listing row would PUT `instructions: null` over text the user never saw — `beginEdit` accepts either shape and fetches the detail first. And **deleting a project releases its conversations**, which the client must mirror: `ConversationListStore.releaseProject` and `ConversationStore` clear the dead `projectId`, or `toUpdateBody` would echo it and the next rename would take a 404. The dialogs mount on `ProjectsLayout` rather than the shell while both invokers live inside the feature; US-307 adds the third and should move them.
- **Acceptance criteria**:
  - Given the create action, when it is opened from the projects screen, from the composer's project picker, or from a Move-to-project submenu, then one shared modal renders Name, Description, and optional Instructions (frame `4h`); on submit `POST /api/projects` runs and the operation is **not** applied optimistically, because it creates a server id.
  - Given a duplicate name, when submitted, then the inline message "You already have a project with this name." renders against the name field and nothing is created (frame `4h`).
  - Given the modal was opened from the composer, when creation is confirmed, then the composer's draft text and attachments are preserved behind it, the new project becomes the selected one, and the user returns exactly where they were — never to the Projects screen (frame `4h`).
  - Given a rename or description edit, when `PUT /api/projects/{id}` is built, then it carries both `description` and `instructions` — this is a full-representation PUT and omitting a field clears it.
  - Given delete, when confirmed in a modal naming the project, then `projectEvents.deleted(id)` fires and the project list removes it, the conversation list releases its conversations as standalone, the active project navigates away, and the project documents panel clears.

#### US-904: Give a project standing instructions

- **Story**: As a user, I want instructions that apply to every conversation in a project so that I do not repeat context in each chat.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-903
- **Status**: ✅ Done (2026-08-14). It ships the project detail screen the rest of EP-9 hangs off: header, composer slot, and a tab strip whose **tabs are real child routes**, so a panel survives a refresh and `canDeactivate` has something to attach to — which is what makes the unsaved-edit warning fire on a tab change as well as on leaving the project. Frame `4e`'s **Conversations tab is absent** until US-908, and its star until US-909. The save is not optimistic and goes through `toProjectBody`, so the panel — which shows neither the name nor the description — cannot clear either on a full-representation PUT. One case resolves the guard without a modal beyond the obvious ones: a project deleted under the reader, where the edit has nothing left to save into and refusing would strand them on a URL that 404s.
- **Acceptance criteria**:
  - Given the project detail screen, when instructions are edited and saved, then `PUT /api/projects/{id}` carries the new instructions along with the current name and description.
  - Given instructions, when they are saved, then the change is **not** applied optimistically — Save shows a pending state and the saved state renders only once the server confirms (frame `4e`).
  - Given unsaved edits in the instructions field, when the user navigates away, then a warning modal names the unsaved change and requires an explicit discard before the edit is lost (frame `4e`).
  - Given `GET /api/projects/{id}`, when the detail loads, then instructions come from `ProjectDto` — the listing shape omits them by design.

#### US-905: Manage a project's files

- **Story**: As a user, I want to add and remove files on a project so that its conversations share the same reference documents.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-802, US-903
- **Status**: ✅ Done (2026-08-14), and it added no upload code at all — which is what EP-8's `UploadTarget` was for. The transport, the staged poll, the refusal shapes and the download all route through the discriminated pair already, so the panel binds `{ kind: 'project', id }` and reuses the lot; the one change was generalizing `UploadStore.bindConversation` into a private `bindTarget` with a `bindProject` beside it. Two decisions are not derivable from the criteria: **`GET /api/projects/{id}/documents` answers with a bare array**, so `ProjectDocumentsStore` holds a plain list with no pagination and an index-faithful optimistic removal; and **a finished chip becomes a row by re-reading that list**, because the upload route answers with a job id and never a document — the bridge lives on `ProjectDetail`, not the panel, so a file dropped on the composer from another tab still lands.
- **Acceptance criteria**:
  - Given the project files panel, when a file is added, then `POST /api/documents/projects/{projectId}` runs and the same staged polling and progress rules as US-802 apply.
  - Given `GET /api/projects/{id}/documents`, when the panel loads, then each row shows an extension glyph, name, human-readable size, and created date from `ProjectDocumentDto`, with per-row download and remove controls (frame `4f`).
  - Given a file removal, when confirmed, then `DELETE /api/projects/{projectId}/documents/{documentId}` runs and the row is removed optimistically with rollback on failure.
  - Given the user lacks `Upload File`, when the panel renders, then the dashed drop zone and the add control are both **absent** and existing files remain listed and downloadable (frame `4f`).

#### US-906: Start a conversation inside a project

- **Story**: As a user, I want to type a prompt on the project screen and have the conversation created inside that project so that it inherits the instructions and files.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-401, US-903
- **Status**: ✅ Done (2026-08-14) — ⚠ **re-sized S → M**, agreed with the product owner before implementation, the way US-410 was. Frame `4e` puts the _shared_ composer on this screen, but the component lived in `features/chat/` and injected the route-scoped `TurnStore`, and `features → features` is not a direction this app has. The story therefore promoted the composer stack into `shared/composer/` behind a `COMPOSER_HOST` token: `Chat` provides `TurnStore`, the project screen provides `ProjectComposerHost`, and `UploadStore` moved to `core/documents/` alongside. **The project screen does not stream** — the criterion is that the turn streams as it does on the chat route, and the transcript is there — so a send creates the conversation, holds the prompt in a root `PendingPromptStore`, and navigates; `TurnStore.bindRoute` claims it single-shot and runs the ordinary turn. A `no-restricted-imports` rule now enforces the layering that made all this necessary.
- **Acceptance criteria**:
  - Given the project detail composer above the tab strip, when a prompt is sent, then `POST /api/conversations` carries that `projectId` and the turn streams as it does on the chat route (frame `4e`).
  - Given the new conversation, when it is created, then the app navigates to the chat route for it and the project's linked conversation list includes it on return.
  - Given a project that has been deleted in another tab, when a prompt is sent, then the 404 renders as "this project no longer exists" and the user is returned to the projects list.

#### US-907: `[enabler]` Filter conversation search by project (B1)

- **Story**: Add a `projectId` filter to conversation search, or a `GET /api/projects/{id}/conversations` route. `SearchConversationsAsync(name, skip, take, isFavorite)` accepts no project filter today, so the client can only get a project's conversations by draining the whole list. Unblocks US-908 and the sidebar tree in US-910.
- **Priority**: P1 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-15). **A filter on the existing route, not a new one** — the story allowed either, and one filter means one paging contract, one ordering rule and one place the ownership check lives, rather than a second listing to keep in step with the first. Three things about the shape outlive the story. **`EnsureProjectExistsAsync` runs before the page is read**, which is what makes criterion 2 true and is the private method the create and update paths already used; the case that needs it is a **deactivated** project, because `DeactivateProjectAsync` releases its conversations, so without the check the route would answer a cheerful empty page for a project that had rows an hour ago. **Criterion 4 is true by construction rather than by test**: the filter and the ownership check both sit behind one `projectId.HasValue` guard, so an omitted parameter executes nothing new. And **no index was added** — the filtered listing is owner-scoped too, so the engine seeks `(UserId, DateDeactivated, DateCreated DESC)` and gets the ordering off it for free, leaving `ProjectId` a residual predicate over one user's conversations; folding `DateCreated` into `(ProjectId, DateDeactivated)` would cost every conversation write to buy a second ordered path for a query that already has one. The route declares `.ProducesProblem(400)` and `(404)`; the 400 is a binding failure on one of the four typed query parameters, so it carries no `errors` dictionary and is `.ProducesProblem(400)` rather than `.ProducesValidationProblem()`. Landing it in the same batch as US-908 rather than during P3 was still early enough for that story's interim drain ceiling never to reach a build.
- **Acceptance criteria**:
  - Given a `projectId`, when conversation search is called with it, then only that project's active conversations are returned, in the same `PaginatedResponseDto` envelope, scoped to the caller.
  - Given a `projectId` belonging to another user or a deactivated project, when the search runs, then it returns 404 in the same way the single-project route does, so the API cannot be used to probe for project ids.
  - Given both `projectId` and `name`, when supplied together, then both filters apply.
  - Given `projectId` is omitted, when the search runs, then behavior is byte-for-byte unchanged from today.

#### US-908: See and manage a project's conversations

- **Story**: As a user, I want a project's conversations listed on its detail screen with the same actions I have elsewhere so that I can manage them in context.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-903, US-307
- **Status**: ✅ Done (2026-08-15), shipping **criterion 2 and never criterion 1**: US-907 landed in the same batch, so the interim client-side drain, its local `projectId` filter and its "Showing the 500 most recent conversations" notice were live for no build at all, and a spec pins the absence of the notice so nobody reintroduces it from the criterion. Three decisions outlive the story. **The store is provided on the detail route, not on the panel**, so the tab strip's count is readable from the Instructions tab — the same placement, for the same reason, as `ProjectDocumentsStore`. **Its `conversationEvents.updated` handler is asymmetric on purpose, and both halves matter**: a row whose `projectId` no longer matches is **dropped**, which is what makes a move performed from this panel's own kebab remove the row it was invoked from; and a row it does *not* hold whose `projectId` now matches triggers a **re-read**, because the sidebar kebab can move a conversation into the project whose screen the reader is standing on. It re-reads rather than inserting because the row's slot in `dateCreated desc` is the server's to decide. And **the panel is frame `4g` and nothing more** — four columns (name, relative updated time, star, kebab), no search field and no selection, because the board draws none; the row actions are US-307's five routed through `ConversationActionsStore`, with "Remove from project" **unconditional** here, unlike every other kebab, because every row on this panel is in a project by construction. The empty state projects no action, since frame `4g`'s call to action is the project composer already on screen and earlier in reading order.
- **Acceptance criteria**:
  - Given US-907 has not landed, when the panel loads, then the client drains conversation search at `take=100` to the 500-item ceiling and filters by `projectId` locally, and if the ceiling is reached the panel states "Showing the 500 most recent conversations" above the list — this is the interim behavior and must be visible, not silent (frame `4g`).
  - Given US-907 has landed, when the panel loads, then it issues one filtered request and the ceiling notice is gone.
  - Given a listed conversation, when its row menu is opened, then favorite, rename, delete, remove-from-project, and move-to-another-project are all offered (frame `4g`).
  - Given remove-from-project or move, when either is used, then the update goes through `toUpdateBody()` so the name is echoed and only `projectId` changes.
  - Given the panel is empty, when it renders, then it shows an empty state with the project composer as the call to action.

#### US-909: `[enabler]` Support project favorites (B3)

- **Story**: Add a per-user favorite flag to projects, mirroring the conversation favorite design that already works — a dedicated route plus a field on the listing shape. `ProjectSummaryDto` carries no favorite or pin concept today. Unblocks US-910 and the pinned sort in US-902.
- **Priority**: P2 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-20). `Core.Project.IsFavorite` (`bit DEFAULT 0`), migration `20260820050001_AddProjectIsFavorite` — the seventh, settling the open question below in B3's favour without B7 needing one of its own. **No new index**, deliberately: `Core.Conversation` needed one only because its unfiltered listing already seeks an ordered `(UserId, DateDeactivated, DateCreated DESC)` sibling; `IX_Project_UserId_DateDeactivated` carries no `DateCreated`, so the unfiltered project listing already sorts after its own seek, and a favourites index would buy the *filtered* listing an ordering the unfiltered one does not need — paid for on every project write. `PUT /api/projects/{id}/favorite` is its own route rather than a field on the full-representation project `PUT`, for the identical reason the conversation route already is one; `SetProjectFavoriteAsync` runs a set-based `ExecuteUpdateAsync` so `DateModified` does not move, closing criterion 3 the same way the conversation route does. `isFavorite` landed on `ProjectSummaryDto` only, with `ProjectDto : ProjectSummaryDto` inheriting it, and deliberately not on the update mapper — a rename that wrote it would clear the star. `?isFavorite=` filters before `CountAsync`, so `totalCount` describes the filtered set. See [Projects §3.5](../projects/project-management.md#35-favourites--put-apiprojectsidfavorite-and-isfavorite).
- **Acceptance criteria**:
  - Given `PUT /api/projects/{id}/favorite` with `{ isFavorite }`, when called by the owner, then the flag is persisted and 204 is returned, matching the conversation route's shape and status.
  - Given `ProjectSummaryDto`, when returned by any project route, then it carries `isFavorite`.
  - Given the favorite is set, when it is persisted, then `DateModified` is **not** bumped, matching the conversation behavior so client-side optimistic patches stay consistent across both entities.
  - Given `GET /api/projects`, when called with `isFavorite=true`, then only favorited projects are returned.

#### US-910: Pin favorite projects to the sidebar

- **Story**: As a user, I want my favorite projects in the sidebar with their conversations beneath them so that my active work is one click away.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-909, US-907, US-301
- **Status**: ✅ Done (2026-08-20), **shipping criterion 2 and never criterion 1** — US-909 landed in the same batch, so the device-local `localStorage` pins and the "Pinned on this device — pins don't sync." notice were live for no build at all, the call US-908 and US-1002 both already made about their own interim states. Pins were also the wrong place for this story to reach: `local-preferences.ts` holds facts about the *browser*, and a list of favourite project ids is a fact about the *user*. `SidebarFavorites` (`features/shell/sidebar/`) is the section between the nav and the conversation list, its own component because it owns which nodes are expanded and `sidebar.scss` was already at its per-component budget. Its rows derive from the root `ProjectLookupStore`'s new `favorites`/`hasFavorites` computeds — no request of its own, and it inherits that store's 500-project drain ceiling with it — rather than a dedicated `?isFavorite=true` store. `SidebarProjectConversations` provides its own `ProjectConversationsStore` per expanded node, reading the `core/projects/` store US-908's Conversations tab already used — that store moved from `features/projects/detail/conversations/` to `core/` because `features/shell` may not import `features/projects`. The favourite itself is **not** optimistic: `ProjectActionsStore` has no channel into the stores that would need to reflect it, so a fourth event, `projectEvents.favorited`, carries `{ id, isFavorite }` on the 204 only, and three stores (`ProjectListStore`, `ProjectStore`, `ProjectLookupStore`) each patch just that field. Unfavourite or delete from a sidebar row destroys the `<li>` holding the focus the closed menu returned to, so the intent is recorded before the request goes out and an effect moves focus to the section heading once the row is confirmed gone — or, when that was the last favourite, emits `lastFavoriteRemoved` for the sidebar to focus its search field instead. The card star (US-901's deferred criterion) and the project-detail header's star (frame `4e`'s, deferred out of US-904) shipped alongside; `ProjectCard`'s star switched from `[disabled]` to `[attr.aria-disabled]` in the same change, because the pending id is set synchronously with the click and a native `disabled` would blur the button just pressed. The collapsed 60px strip gains a tooltipped `bi-star` entry, present only while a favourite exists, that expands the sidebar and hands focus to the revealed section. See [Shell and Navigation §5.7](../ui/shell-and-navigation.md#57-favourite-projects-in-the-sidebar-us-910) and [Projects (UI) §5.7](../ui/projects.md#57-favourites-us-909-and-why-the-flip-is-not-optimistic).
- **Acceptance criteria**:
  - Given US-909 has not landed, when the sidebar renders, then favorites come from device-local pins in `localStorage` via `UiStore`, under a "Favorite projects" heading carrying the required copy "Pinned on this device — pins don't sync." (the `Sidebar` component).
  - Given US-909 has landed, when the sidebar renders, then favorites come from the server flag and the device-local notice is gone.
  - Given a favorited project, when its sidebar node is expanded, then its conversations render beneath it, sourced from US-907's filtered endpoint where available and from the drained-and-filtered set otherwise.
  - Given a project row in the sidebar, when its menu is opened, then unfavorite, rename, and delete are offered.
  - Given the sidebar is collapsed to the 60px strip, when it renders, then the project tree is replaced by an icon entry with a tooltip.

### EP-10: Documents library

#### US-1001: `[enabler]` List the documents belonging to a conversation (B2)

- **Story**: Expose the documents uploaded into a conversation. `ConversationDocumentDto` exists and `CosmosConversation.Documents` is defined, but no endpoint returns them and the collection is never populated — so the client can upload a document and later download it by id, but can never enumerate what a conversation holds. This blocks the entire Documents screen. Unblocks US-1002 and US-1003.
- **Priority**: P1 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-19) — both routes shipped, closing criterion 4 the strong way: `GET api/conversations/{id:guid}/documents` (a bare `List<ConversationDocumentDto>`, the exact mirror of the project-documents route down to the newest-first order and the indistinguishable 404) **and** a cross-conversation `GET api/documents` scoped to the caller — paginated with `PaginatedResponseDto<UserDocumentDto>` (skip/take clamped 1..100, `DateCreated` desc with `Id` as the tie-break so a page boundary cannot duplicate rows), each item carrying `conversationId` and `conversationName` so the Documents screen drains pages instead of an N+1 fan-out. `ConversationDocumentDto` was widened in place (it had never left the server), `UserDocumentDto : ConversationDocumentDto` adds the name, and the mapper moved to `Service/Mappers/ConversationDocumentMapper` beside its project sibling. One new index, `(UserId, DateDeactivated)`, in migration `AddConversationDocumentUserIndex` — its `Up` also drops the conventional single-column `UserId` index the composite supersedes. The story text's `CosmosConversation.Documents` premise was stale: documents are SQL-only and no Cosmos shape holds them; nothing was populated because nothing needed to be. Review found one real defect the listing would have made visible — a document whose ingestion outlives its conversation's deletion became an active orphan, because the deactivation cascade never re-runs — closed by a post-persist re-check in both `Create*DocumentAsync` paths that deactivates the just-inserted rows set-based (no rowversion involved) and fails the job with the not-found message. 16 new unit and 12 new integration tests.
- **Acceptance criteria**:
  - Given `GET /api/conversations/{id}/documents`, when called by the owner, then it returns the conversation's active documents with at minimum id, name, extension, mime type, size, and created date — matching the field set `ProjectDocumentDto` already provides, so both listings key the same way.
  - Given a conversation belonging to another user or deactivated, when the route is called, then it returns 404, consistent with the rest of the conversation group.
  - Given a conversation with no documents, when the route is called, then it returns an empty list with 200, not 404.
  - Given the Documents screen needs every document across every conversation, when this contract is designed, then it is decided and recorded whether the screen fans out one request per conversation or whether a cross-conversation listing is added instead — the per-conversation route alone forces an N+1 over the conversation list, which is acceptable for the grouped view but not for an unbounded library.

#### US-1002: Browse every document I have uploaded

- **Story**: As a user, I want one screen listing the documents I have uploaded across all my conversations so that I can find a file without remembering which chat it was in.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-1001, US-106
- **Status**: ✅ Done (2026-08-19) — straight to the enabled screen: US-1001 landed first, so frame `4i`'s pre-enabler panel was never built and `DocumentLibraryStore` (route-provided, `features/documents/`) holds zero seeded records by construction, with the skipped criterion recorded in its class doc. The store is `ProjectListStore`'s drain grafted onto the paginated `GET api/documents` — 100 a page to a 500-item ceiling, `expand` with `takeUntil` on sign-out and supersede, and a mid-drain failure that clears the dead pages with the error so Retry shows the skeleton rather than a half-drained set (a review finding; the reference store keeps its partial pages, but there they can be a complete previous result and here they never are). The screen is frame `4j`'s flat view built from custom list rows (`app-document-row`, the `ProjectFiles` anatomy — **not** `DataTable`, which always renders the header row the frame doesn't draw and has no grouping for US-1003 to grow into): `fileGlyph`, `formatBytes`, `formatShortDate`, a hard-coded `Uploaded` `SourceBadge` until US-1004 exists to make it data-driven, the owning-conversation link column, and US-804's click-only downloads through the root `DocumentDownloadStore` with per-row busy state. Frame `4k`'s `ErrorPanel` (trace line, Retry) and the "Nothing uploaded yet" empty state ship with it, and `conversationEvents.updated`/`.deleted` keep the link column and both counters honest. The sidebar gains its Documents entry (`bi-file-earmark-text`, between Projects and Admin — every needed glyph was already in the manifest), and `/documents` joined `AXE_ROUTES` with `documents.a11y.spec.ts` in the same change, deleting `a11y-coverage.spec.ts`'s absence assertion exactly as that test's own comment demanded. Initial bundle 669.93 → 670.62 kB raw against 675 / 720 kB — the route record and `DOCUMENTS_ROUTE` only; the feature rides its own lazy chunk.
- **Acceptance criteria**:
  - Given US-1001 has not landed, when the documents route is opened, then `UnavailablePanel` renders frame `4i` — the ridgeline motif, "Your documents aren't available yet", and a line stating the documents API is not enabled for this deployment — with no toolbar, no filters, and zero rows; `DocumentLibraryStore` holds zero records, and a mock or seeded row fails this criterion.
  - Given US-1001 has landed, when the route is opened, then documents render with an extension glyph, name, size, created date, an `Uploaded` or `Generated` source badge, and the conversation they belong to (frame `4j`).
  - Given a document row, when download is used, then US-804's rules apply — the signed URL is fetched on click only.
  - Given the fetch fails, when the error renders, then frame `4k`'s error state shows — an octagon glyph, "Documents couldn't load", the `traceId` in JetBrains Mono, and a Retry control — visibly distinct from the unavailable panel of frame `4i`.

#### US-1003: Filter documents and group them by conversation

- **Story**: As a user with many documents, I want to filter by name and group by conversation so that I can narrow a long list.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-1002
- **Status**: ✅ Done (2026-08-19) — `withClientQuery` composed over the drained set (its second listing consumer after the model catalog): name-only `searchableText`, which is what "Filters cover file names only." promises; `isAuthoritative: isFulfilled() && isFullyLoaded()`; the ceiling worded through `incompleteSetReason`. The term rides the URL through `withComponentInputBinding` inputs and one `_applyQuery` writer copied from the conversations library — refine replaces history, add/remove pushes, and a deep link filters the first render because `bindQuery(this.name)` takes the signal; it is a `signalMethod`, not an `rxMethod`, because a client-side filter has no request to race. **Grouped is the default** — frame `4j` draws the switch checked — so the URL records only the deviation, as `?view=flat`, and any other value reads as grouped. Groups compute over the *filtered* results in first-seen order (server order makes that newest-document order): collapsible `--surface-2` headers with a chevron button (`aria-expanded`; `aria-controls` nulled while the collapsed body is out of the DOM, since a reference to nothing is an axe violation), `bi-chat-left`, the conversation link and a `'1 file'`/`'N files'` count; collapse state is a screen-local signal that survives a filter change and dies with the route, defaulting all-expanded so a filter never hides its matches behind closed groups. The empty-filter state names the committed term and offers Clear filter — distinct from "Nothing uploaded yet", which wins on a zero-document deep link, because with the filter client-side the two are tellable apart. The toolbar count reads the envelope's `totalCount` (`Showing X of N documents` under a term), the whole toolbar is withheld under the error panel (the filter narrows nothing over a failed load), and the ceiling notice and `resultsPartialReason` never render together. +0.06 kB initial (670.68 kB raw).
- **Acceptance criteria**:
  - Given the documents screen, when a name filter is typed, then the list narrows client-side and the term is reflected in the URL query params.
  - Given the group-by-conversation toggle, when enabled, then documents group under collapsible `--surface-2` headings, each linking to its conversation and showing a per-group file count; when disabled, the owning conversation renders as a link column on every row (frame `4j`).
  - Given a filter matching nothing, when applied, then frame `4k`'s empty-filter state renders, naming the term, stating "Filters cover file names only.", and offering Clear filter — distinct from the never-uploaded state, which reads "Nothing uploaded yet".

#### US-1004: `[enabler]` Distinguish model-created documents (B7)

- **Story**: Introduce a concept for documents the assistant produced, as distinct from documents a user uploaded. No such concept exists anywhere in the data model today. Unblocks US-1005.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-1001
- **Acceptance criteria**:
  - Given a document record, when returned by any document listing, then it carries an origin discriminator distinguishing a user upload from a model-created artifact.
  - Given existing documents, when the discriminator is introduced, then all of them default to the user-upload value and no existing listing response changes shape for a client that ignores the new field.
  - Given the discriminator, when a listing is filtered by it, then only matching documents are returned.

#### US-1005: Filter to documents the assistant created

- **Story**: As a user, I want to see only the files the assistant produced so that I can separate its output from my inputs.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-1004, US-1002
- **Acceptance criteria**:
  - Given US-1004 has not landed, when the documents screen renders, then the assistant-created filter is **hidden** — not shown-and-disabled, and not shown returning zero results (frame `4j`).
  - Given US-1004 has landed, when the filter is enabled, then only documents carrying the `Generated` source badge render and the filter state is in the URL query params (frame `4j`).
  - Given the filter is on and nothing matches, when the list renders, then an empty state explains that the assistant has not created any documents yet.

### EP-11: Response feedback

#### US-1101: `[enabler]` Put a message identity on the transcript and the stream (B4)

- **Story**: Add an id to transcript messages and to the turn's completion signal. `ConversationMessageDto` already carries `id`, `dateCreated`, `htmlContent`, `tokens` and `tokenAccuracy` — the client's actual gap is narrower: the DTO has no `usage`, and the `Finished` frame has no `assistantMessageId`, though the server already mints the id in `PersistTurnAsync` and correlates it on the usage row's `AssistantMessageId`. The `usage` half has no package dependency and could ship on its own; the `assistantMessageId` half was blocked — `AssistantUiEvent` is a sealed record from `Andes.Extensions.AI.UI`, serialized through a source-generated `JsonTypeInfo` that writes exactly its own properties, so there was no supported slot for it on the `Finished` frame until the package added one (tracked upstream: https://github.com/Andes-Software-Solutions/Andes.Extensions.AI/issues/14). Unblocks US-1102 and US-1103, and removes the composite-key workaround.
- **Priority**: P1 · **Estimate**: M · **Depends on**: —
- **Status**: ✅ Done (2026-08-21) — every criterion, and both halves shipped together rather than the `usage` half alone. `Andes.Extensions.AI.UI` 0.8.0 published to nuget.org the day before, 2026-08-20, closing issue #14 by adding an optional `Metadata: IReadOnlyDictionary<string, string>?` to `AssistantUiEvent` and `AssistantStatusSnapshot` — the whole public-surface delta across all four `Andes.Extensions.AI*` packages, all bumped 0.7.0 → 0.8.0 in both `.csproj`s; this story took the release up the next day. The mechanism is worth carrying forward: `Finished` is the middleware's own last event and used to be forwarded straight through; it is now **held back** in `StreamConversationCoreAsync` and re-yielded only after `PersistTurnAsync` — which runs in the `finally` below the model loop — has answered, because the id does not exist until that write does. A cancelled or faulted turn still gets no `Finished` frame at all (unchanged); a completed turn whose transcript write failed or threw gets `Finished` with **no `metadata` object** — the serializer omits nulls — and the write's exception is captured with `ExceptionDispatchInfo` and rethrown *after* the frame, so a completed turn is never downgraded into one the client reads as truncated. The key itself is `IConversationService.AssistantMessageIdMetadataKey` = `"assistantMessageId"`, an opaque identifier clients match verbatim, exactly like the `/problems/` type URIs — renaming it is a breaking API change. `ConversationMessageDto.Usage` (`ConversationMessageUsageDto`: `inputTokens`, `outputTokens`) projects straight off the transcript document's own `usage`, which the storage design had already been writing; it is the **turn's** total, tools included, deliberately not equal to the sibling `tokens` field, and null on every message a turn didn't bill or that predates the field. See [Streaming Contract §4.1.1](../conversations/streaming-contract.md#411-finished-carries-the-persisted-messages-identity-us-1101) and [Transcript Storage §6.1](../conversations/transcript-storage.md#61-writing-a-turn).
- **Acceptance criteria**:
  - Given `GET /api/conversations/{id}/messages`, when it returns, then each message carries `id` and `dateCreated`, and assistant messages carry their `usage`.
  - Given a completed turn, when the `Finished` event is emitted, then it carries `assistantMessageId` matching the id the transcript will return for that message.
  - Given a cancelled or failed turn, when it ends, then no `assistantMessageId` is claimed, consistent with the usage row leaving it null.
  - Given the change, when a client that ignores the new fields consumes either response, then its behavior is unchanged.

#### US-1102: `[enabler]` Record feedback on an assistant message (B5)

- **Story**: Add the endpoint that stores a thumbs up or thumbs down against an assistant message. None exists. The endpoint itself needs nothing from `Andes.Extensions.AI.UI` — it is blocked only transitively, on US-1101's `assistantMessageId`. Unblocks US-1103.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-1101
- **Status**: ✅ Done (2026-08-21) — every criterion, plus two widenings beyond what was written. `POST api/conversations/{id}/messages/{messageId}/feedback`, body `{"rating": "Up" | "Down" | null}`, answers `204`. **First widening: `null` withdraws a rating**, and a withdrawal of a rating nobody recorded writes to *neither* store — the alternative would let a client mint a `Core.MessageFeedback` row per assistant message by posting withdrawals across a transcript it never rated, skewing the "messages considered" denominator a reporting story would read. **Second: the write is dual and one-directional.** The transcript message document is patched first and is what the `204` depends on, exactly like `GET .../messages` already reads it back; `Core.MessageFeedback` — a new table, one migration (`20260821203334_AddMessageFeedback`) — is written second, best-effort, purely so the same fact is queryable outside one conversation's Cosmos partition. A failure to write that row is logged and swallowed rather than thrown, the same trade `ConversationUsage.AssistantMessageId` already documents (rating the message again repairs a stale row). The patch is `Set`, never `Remove` — Cosmos rejects `Remove` against a path the document does not carry, so two withdrawals racing each other would have the loser take a `400` surfacing as a `500` for work already done. The point read checks `TranscriptDocumentTypes.Message` as well as the id, because the header document shares the partition under the conversation's own id and passing it as the message id would otherwise be answered about the header rather than a `404`. A validator closes a hole `JsonStringEnumConverter` leaves open on its own — it accepts integer values by default and does not check them against the enum's members, so `{"rating": 7}` would otherwise bind, pass every check, and come back out of the transcript as a JSON number. See [Transcript Storage §6.5, §13](../conversations/transcript-storage.md#65-rating-a-message-and-why-the-write-goes-to-two-stores-in-one-order-us-1102).
- **Acceptance criteria**:
  - Given `POST /api/conversations/{id}/messages/{messageId}/feedback` with a rating, when called by the conversation's owner, then the rating is persisted and re-submitting replaces the prior value rather than creating a duplicate.
  - Given a message id that does not belong to the conversation, or a conversation not owned by the caller, when the route is called, then it returns 404 consistently with the rest of the group.
  - Given a rating on a user message rather than an assistant message, when submitted, then it returns 400 as a validation problem.
  - Given the transcript response, when it is returned, then any existing rating is included so the UI can render the current state.

#### US-1103: Rate an assistant response

- **Story**: As a user, I want to mark a response as helpful or unhelpful so that the platform learns which answers work.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-1102, US-607
- **Status**: ✅ Done (2026-08-21) — every criterion. Thumbs up/down sit beside Copy in frame `1g`'s settled-turn footer, absent by construction wherever the turn never became a persisted message: a replayed message the server sent no `id` for, and a completed turn whose transcript write failed, which is sent a `Finished` carrying no `metadata` at all (US-1101) — no anchor, no thumbs, with no flag to remember. The rating lives on `TranscriptEntry`'s `assistant` arm (`messageId`, `rating`), and `replaceHistory` rebuilding the whole replayed block from the response is the entire mechanism by which a rating survives a reload — nothing reconciles it separately. Pressing the thumb already selected is read as a withdrawal by `TurnStore.rateMessage`, never by the component: `MessageFeedback` only reports which thumb was pressed, because only the store knows what is currently stored. A rated turn keeps its footer visible at rest — a fourth `:has(.message-feedback--rated)` selector joins the existing reveal rule, with the side effect that Copy and the token counts are visible too on a rated turn. Three channels carry the pressed state — the glyph swaps to its `-fill` variant, `aria-pressed` carries it, colour reinforces — because colour alone fails SC 1.4.1 in dark theme, where `--muted` and `--brand` sit at a 1.06:1 luminance ratio. `aria-disabled`, never the native attribute, with the same refusal enforced again in the component's own `press()`. Ratings are cancelled on a route change (a dedicated `_rateCancel$`, separate from Stop's `_stop$`), not merely forgotten — clearing the pending id without cancelling would leave an orphaned request the re-entry guard can no longer see, letting a second press race it. Two gates widened: `check-tokens.mjs` gained `--brand` on `--surface` and on `--bs-body-bg` (48 → 50 pairs, closing the same kind of gap US-1405 found), and `src/testing/a11y.ts` now enables axe's `target-size` rule — WCAG 2.2 AA, `serious` — which axe-core ships disabled; these thumbs are the first adjacent pair of icon-only toggles in this app measured by it, and every other audit was already clean. Known gaps: the TS `ConversationMessageDto` still ignores `usage`, `htmlContent`, `tokens` and `tokenAccuracy`, so a replayed turn still shows no token counts — US-1101's unbuilt frontend half, not this story's, and unchanged by it; on failure the store reverts to the rating captured when the button was pressed, which a history reload landing mid-request could in principle have superseded, the same small gap `ConversationActionsStore`'s favourite toggle already carries; reading a rating back through `GET .../messages` is proved only against the Cosmos emulator, not by an endpoint integration test, because the fake transcript store used there refuses to implement the query; and nothing here has been exercised against a live API. See [Answer Rendering §9.3](../ui/answer-rendering.md#93-rating-an-answer-us-1103).
- **Acceptance criteria**:
  - Given US-1101 and US-1102 have not landed, when a completed assistant message renders, then the thumbs controls are **absent** — the interim behavior is to ship no feedback affordance at all, because there is no stable anchor and nowhere to send a rating.
  - Given both enablers have landed, when the user hovers a completed assistant message, then thumbs up and thumbs down appear beside the copy control and are reachable by keyboard.
  - Given a rating is submitted, when it succeeds, then the chosen control shows a selected state that survives a reload of the conversation.
  - Given a rating is submitted twice with different values, when the second succeeds, then the displayed state reflects the second and no duplicate is created.
  - Given the request fails, when the error returns, then the control reverts to unselected and an error toast is raised.

### EP-12: Administration

#### US-1201: Find and page through users

- **Story**: As an administrator, I want to search the user directory by name or email so that I can locate an account.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-203, US-104
- **Status**: ✅ Done (2026-08-16) — **shipping every criterion except the Last active column, which has no wire source.** `UserDto` carries no timestamp of any kind and `dateModified` moves only when an administrator edits the row, never when the user signs in, so the column and frame `5c`'s `· last active yesterday` are absent rather than filled with a plausible-looking value; tracked in the interim-behaviours table. Three decisions outlive the story. **The admin area gained a layout route**, so the four tabs are children of one `AdminLayout` carrying `AdminNav.dc.html`'s 190px rail (`PillSubnav` below 768px) — the rail marks the open tab from the router rather than from local state, which is what US-1209 asks for, and it lists only the tabs that exist: a rail entry leading nowhere is the shown-and-disabled affordance US-203 already rejected. **`AdminUsersStore` deliberately does not compose `withOffsetPagination`**, the store's one structural decision: that feature models a running offset for a "Load more" list (`skip` is how many rows are loaded, `hasMore` compares it to the total), while a numbered pager needs `skip = (page − 1) × pageSize` and a page that *replaces* the rows — reinterpreting `skip` would break `hasMore`/`isFullyLoaded` for the four stores that read them as defined, so the page window lives inline, on the rule that two stores sharing a shape is a coincidence and three is a feature. And **`?size=` reads only the three sizes the select offers**, falling back to 25, so the control can never display a value the URL contradicts — the same reasoning that makes `?favorites=yes` read as off in the conversations library. Two controls frame `5a` draws stay absent, each pinned by a spec: no sort control (regime D) and no row-selection column (no endpoint acts on more than one user) — the third, a "Permission: any" filter, shipped with US-1206. `Paginator`, `Offcanvas` and `PermissionBadge` — built by US-106 *for these frames* and unused until now — needed only a page-size select and a `liveSummary` opt-out, the latter because the table already hands the same sentence to a live region and two of them read the count twice.
- **Acceptance criteria**:
  - Given the users tab, when a term is typed, then `GET /api/users?name=` is sent debounced, and because the server matches first name, last name, **and** email with a `LIKE`, an email fragment finds the user — no separate email field is needed.
  - Given results, when they render, then the table shows avatar initials, full name, email, the user's permission set as one badge per grant, last-active, and per-row Edit and Deactivate actions (frame `5a`).
  - Given more results than one page, when the paginator is used, then `skip` and `take` drive the request, the page-size select offers 25, 50, and 100 per page, and the current page is reflected in the URL query params (frame `5a`).
  - Given no sort control, when the tab renders, then none is offered, because user search is server-paged and accepts no sort parameter — regime D (frame `5a`).
  - Given a non-administrator reaching the route directly, when the request is issued, then the 403 `/problems/permission-required` renders naming the missing permission.

#### US-1202: Create a user

- **Story**: As an administrator, I want to provision a user ahead of their first sign-in so that they have the right permissions when they arrive.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-1201
- **Status**: ✅ Done (2026-08-16). The app's third Signal Form, copying `ProjectFormDialog` end to end. Two decisions outlive the story. **Four rejections reach the email field and only three are validation problems**: the fourth — the directory has no user with that address — is a **404** with no `errors` dictionary at all, because `POST api/users` resolves the address through Microsoft Graph before it writes anything. It travels beside `formError` rather than through `serverMessagesFor`, and reaches the field just the same, because a toast would leave the reader guessing which of the two things they filled in was wrong. Both are reported by the declarative `validate` rule only while the field still holds the exact value the server rejected, which is what clears them on the next keystroke. And **the checklist reads `GET api/permissions`, not the criterion's `/all`** — a deliberate, documented departure: `/all` additionally returns *deactivated* permissions, and `ResolveGrantablePermissionsAsync` answers a grant of one with a 404, so offering it would be a checkbox that can only ever fail. Nothing is pre-checked, and a note says why: the server grants every `IsDefault` permission on top of whatever is sent, so seeding them would render them as choices this request cannot take away. On the 201 `adminEvents.userCreated` fires and the directory **re-reads its page** rather than inserting the row, because the order is last name then first name and the new user's slot is the server's to decide. Review added a third: **a failed request names the flow it came from** rather than being routed by whichever surface is open when it lands. Create and edit share one store and a surface can be force-closed mid-flight, so deciding from `formMode()` alone put a save's 404 under a fresh create form's email field, a create's 400 in the edit panel's alert region, and an unrelated 500 in charge of closing a panel whose checkbox edits were unsaved.
- **Acceptance criteria**:
  - Given the create form, when it renders, then it collects a work email address and a permission checklist only — no Role, Status, Password, or Confirm Password fields, because the API has none and users are Entra-backed with a permission set (frame `5b`).
  - Given a valid email, when submitted, then `POST /api/users` runs and the directory resolves the object id and display name.
  - Given an email not present in the directory, or a directory user with no email, when submitted, then the 404 renders against the email field explaining that the directory has no such user.
  - Given an email already belonging to an active user, when submitted, then the 400 validation message renders against the field and nothing is created.

#### US-1203: Edit a user's profile and permissions

- **Story**: As an administrator, I want to change a user's permissions and be told plainly that I am replacing the whole set so that I do not revoke a grant by omission.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-1201
- **Status**: ✅ Done (2026-08-16), with the last-active clause of its first criterion absent for US-1201's reason. Four decisions outlive it. **The panel is not a Signal Form**: the only thing it edits is a *set*, and Signal Forms models fields — a checklist would be a field per permission over a model keyed by server GUIDs, rebuilt whenever the catalogue loads — while the one message the server can return about it belongs to a checkbox rather than to a field. **The PUT echoes the profile back unchanged**, because `UpdateUserActionDto` requires non-empty `firstName`, `lastName` and `email` and frame `5c` edits permissions only: a body of `permissionIds` alone is a 400 on three required fields, not a partial update. No fetch is needed to open it, unlike US-903's `beginEdit` — `GET api/users` already returns the full `UserDto` with its active grants. **MCP-derived rows are lock-marked but stay operable**: the API permits granting and revoking them (`EnsurePermissionIsCustom` guards only edits to the permission *definition*), the board draws them checked and interactive, and disabling them would leave no route to MCP tool access at all — the lock says the row's definition is its server's, not that the grant is frozen. Its caption names the server from `permission.name` rather than a second request, because `McpServerService` creates the permission with the server's name and re-syncs it on rename, and the two share one uniqueness space. And **the self-revocation refusal is recognised by key and identity, never by the server's prose** (`PermissionIds` also carries "Permission ids cannot be empty." from the request validator), then rendered at the Administrator checkbox — matched by id, never by name — in the board's second-person wording; any *other* `PermissionIds` message still surfaces verbatim. Focus returning to the row's Edit button needed no new code: `Offcanvas` already captures and restores the focus origin, and already had the spec for it.
- **Acceptance criteria**:
  - Given the edit form, when it opens, then it renders as a 420px right offcanvas carrying the user's avatar, name, email and last-active in its header, every permission from `GET /api/permissions/all` as a checkbox with the user's current grants checked, and a sticky footer with Cancel and Save changes (frame `5c`).
  - Given the form, when it renders, then a `--warn-bg` panel states "Saving replaces this user's entire permission set." and that unchecking a permission revokes it, because `PUT /api/users/{id}` treats `permissionIds` as the complete desired set (frame `5c`).
  - Given an administrator unchecking their own `Administrator` permission, when they save, then the server's 400 renders as the inline message "You can't remove your own Administrator permission — another administrator must make this change." at that checkbox rather than as a generic toast (frame `5d`).
  - Given MCP-derived permissions, when they render, then each is **lock-marked** — a lock glyph and the caption "Managed by the <server> MCP server" beneath its label — rather than presented as freely grantable (frame `5c`).
  - Given the offcanvas, when it closes, then focus returns to the row's Edit button.

#### US-1204: Deactivate a user

- **Story**: As an administrator, I want to deactivate an account so that a departed employee loses access.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-1201
- **Status**: ✅ Done (2026-08-16). **The request lives in the root `UserActionsStore` and the row removal in the route-scoped `AdminUsersStore`**, split for the reason US-704 records: `rxMethod` binds its subscription to the injector that declares it, so an administrator who confirmed a deactivation and then clicked another admin tab would have had the `DELETE` torn down mid-flight — the row already gone, nothing deleted, nothing restored and nothing said. The undo travels back as a **callback**, because `features → core` is the permitted direction. Removal is optimistic and position-faithful, guarded by a query generation so a restore cannot splice one search's row into another's, and the page is left one row short rather than refetched: pulling a replacement up from the next page reads as if the deactivation had touched two people. **The self-deactivation refusal renders at the control and never as a toast**, recognised the same way US-1203's is — key `Id` plus the target being the caller's own row — and worded as the board words it; the row is restored first, so the message has something to render on. It sits under the name rather than beside the button because the actions column is 160px and the sentence would wrap to four lines there, with `aria-describedby` on the Deactivate control tying the two together. The control is **not** pre-disabled: the criterion requires the server's refusal to be what renders, and the `(you)` marker frame `5d` draws says so before the attempt instead. The confirmation lands initial focus on Cancel and repairs focus to `#main-content` afterwards, because confirming destroys the row it would otherwise return to.
- **Acceptance criteria**:
  - Given a user row, when deactivate is chosen, then a confirmation modal names the user and its destructive action is styled red.
  - Given confirmation, when `DELETE /api/users/{id}` succeeds, then the row is removed from the active list and a success toast is raised.
  - Given an administrator attempting to deactivate themselves, when they confirm, then the server's 400 renders as the inline message "You can't deactivate your own account." at the control with a `--fail` slash glyph — never a generic failure toast — and the row is restored (frame `5d`).

#### US-1205: `[enabler]` Filter user search by permission (B10)

- **Story**: Add a `permissionId` filter to `GET /api/users`. The `name` parameter already covers first name, last name, and email, so email filtering needs nothing — only the permission filter is missing. Unblocks US-1206.
- **Priority**: P2 · **Estimate**: S · **Depends on**: —
- **Status**: ✅ Done (2026-08-20) — every criterion; the frontend half (US-1206) landed the next day, 2026-08-21. `GET /api/users` gained an optional `permissionId`, narrowing the page to active users holding an active grant of that permission, applied **before** `CountAsync` so the numbered pager stays honest, and combinable with `name`. An unrecognised or deactivated `permissionId` is a **`ValidationException` (400)**, deliberately not the `NotFoundException` (404) every other permission-existence check in this service layer raises — a 404 on a list route says the directory itself is gone, while a filter control can render a 400 naming its own parameter. Deactivated permissions are refused alongside absent ones, because the filter this story unblocks is offered from `GET api/permissions`, which returns neither. Omitting the parameter leaves behaviour unchanged, by construction: the filter and its existence check both sit behind one `permissionId.HasValue` guard. See [User Management §2](../users/user-management.md#2-api-surface).
- **Acceptance criteria**:
  - Given `GET /api/users?permissionId=`, when supplied, then only active users holding that grant are returned, paginated in the standard envelope.
  - Given both `name` and `permissionId`, when supplied together, then both filters apply.
  - Given a `permissionId` that does not exist, when supplied, then it returns 400 as a validation problem rather than an empty page.
  - Given the parameter is omitted, when the request is handled, then behavior is unchanged.

#### US-1206: Filter users by permission

- **Story**: As an administrator, I want to list everyone holding a given permission so that I can audit who has access.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-1205, US-1201
- **Status**: ✅ Done (2026-08-21) — every criterion, and the first is deliberately not the one built: US-1205 shipped the day before, so the interim "Filters loaded rows only." caption never rendered, and a spec asserts its permanent absence rather than its retirement. The URL parameter is **`?permission=`**, not the wire's `permissionId` — the same `?size=` rule that spells the URL for the reader rather than proxying the request underneath it. An id the permission catalog does not list reads as no filter at all and is stripped from the URL (`replaceUrl`) rather than sent for the server's 400 to render — the `?size=` invariant ("the control can never display a value the URL contradicts") enforced against a run-time set instead of a constant one. Because settling that needs the catalog, a `?permission=` deep link **withholds the directory request** until `GET api/permissions` answers, so there is one request and no unfiltered flash; a link with no permission parameter is unaffected. If the **catalog request itself** fails, nothing can vouch for anything — the raw id is sent and the server is left the authority, and the select renders the unnameable filter as an option of its own rather than falsely claiming "any". The filter reuses the root `UserActionsStore`'s cached catalog (`ensurePermissions()`, newly exported) rather than fetching a second copy, so the filter and the permission-editing offcanvas cannot disagree about what exists — which is also why `UserActionsStore` grew a `permissionsLoaded` flag distinct from "the catalog has rows": a `200 []` has to read as answered, not as still loading. Turning the filter on or off pushes a history entry; swapping one permission for another **replaces**, because a closed `<select>` commits on every arrow key. See [Administration §4.6](../ui/administration.md#46-the-permission-filter-us-1206).
- **Acceptance criteria**:
  - Given US-1205 has not landed, when the permission filter is used, then it filters only the rows already loaded and the required copy "Filters loaded rows only." renders beside the control — it must not appear to filter the full directory (frame `5a`).
  - Given US-1205 has landed, when the filter is used, then `permissionId` is sent, pagination reflects the filtered total, and the interim label is gone.
  - Given a filter selection, when it changes, then it is reflected in the URL query params.

#### US-1207: Manage the model catalog

- **Story**: As an administrator, I want to add, update, and retire models so that users see an accurate catalog.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-203
- **Status**: ✅ Done (2026-08-16) — every criterion, and the form binds **eleven** fields where the second one lists eight. Six decisions outlive it. **The three extra fields are `isReasoningEnabled` and both prices**, because `ModelMapper` assigns every property unconditionally on a PUT: an edit omitting a price would silently *clear* a figure US-1302's report is built on, and this is the only surface in the app that can set one — blank submits `null` (unpriced), zero submits `0` (genuinely free). Frame `5f`'s caption still binds: no API key, no endpoint URL, no "available to". **`adminEvents.modelCatalogChanged` is `type<void>()` and both observers refetch**, unlike the three user events which carry a DTO and patch in place: a save with `isDefault` demotes a row the response does not describe, and `DELETE` clears `IsDefault` on the row it retires, so no payload could carry either — and its second observer is the root `ModelCatalogStore`, which is what stops the chat picker offering a retired model. **Retired models render muted with a Status column frame `5e` does not draw**, their kebab offering no items at all, because `models/all` returns them and a table that hid them would make Delete look like it did nothing; there is no reactivate route, so Restore is an interim absence rather than an omission. **Regime C, so there is no URL contract** — no route file, no `bindQuery` — and this is `withClientQuery`'s **first production consumer**, with `isAuthoritative` as `isFulfilled() || entities().length > 0` rather than `isFulfilled` alone, because every mutation refetches and the rows on screen are still the whole catalog while the next copy is on the wire. **"Must be a whole number of tokens." is a client-side rule**: the columns are `decimal(18,2)` and the validator carries only `GreaterThan(0)`, so the server would accept `400000.5`. And **the four numeric fields bind as `type="text"` with `inputmode`**, never `type="number"`, which returns `null` for what it cannot parse — discarding the rejected value frame `5f` draws still sitting in its field, and collapsing "blank" into "unparseable". Two smaller ones worth carrying: `formBusy` is set only by the dialog's own submit, because it also drives `Modal`'s Escape and backdrop suppression and a row action setting it would trap the reader in a dialog they opened (WCAG 2.1.2); and no failure closes the dialog, since eleven fields are behind it and `retryInterceptor` retries neither a POST nor a PUT. `provider.ts` gained **Azure AI Foundry** — `Providers.cs` seeds four and the client mirror had three, and there is no `GET api/providers` to fetch instead; it shares Azure OpenAI's dot tone because the palette has no fourth hue, which is flagged for the design owner, as is the `DEFAULT` pill rendering in `--brand` rather than frame `5e`'s `--accent` (2.74:1 on light `--surface`, with the border in the same colour). **No re-baseline**: the initial bundle measures **669.63 kB raw / 168.80 kB transfer** against 670 / 720 kB — **+0.46 kB**, all of it `ModelCatalogStore`'s event handler — with 0.37 kB of headroom left, `styles` unchanged at 63.96 kB, and the admin area still absent from the initial graph.
- **Acceptance criteria**:
  - Given the models tab, when it loads, then `GET /api/models/all` returns the complete list, it renders unpaginated with model name, a provider colour dot, the deployment name in JetBrains Mono, context, max output, and a tools column, and client-side search over provider and model name is fully correct — regime C (frame `5e`).
  - Given the add or edit form, when it renders, then it binds `providerId`, `name`, `deploymentName`, `description`, `contextWindowSize`, `maxOutputTokens`, `isToolEnabled`, and `isDefault` — the last two as toggles carrying `role="switch"` — and shows no API-key, endpoint-URL, or "available to" field, because frame `5f` confirms none of the three exists in this system.
  - Given a model is soft-deleted, when the change lands, then `adminEvents.modelCatalogChanged` fires and the root model catalog store refreshes so the chat model picker cannot offer a retired model.
  - Given a model marked `isDefault`, when saved, then the previous default is reflected correctly in the list after the response.
  - Given a validation failure, when it returns, then each `errors` key maps onto the matching form control — a non-integer context window renders "Must be a whole number of tokens." beneath its field (frame `5f`).

#### US-1208: Manage MCP servers

- **Story**: As an administrator, I want to register and maintain MCP servers so that users can select the right tools.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-203
- **Status**: ✅ Done (2026-08-17) — every criterion, and the third tab copies the second almost exactly: the same two-store seam, **regime C** with no URL contract, and `withClientQuery`'s two shaped values (`isAuthoritative` as `isFulfilled() || entities().length > 0`, an unreachable `incompleteSetReason`) taken verbatim from `AdminModelsStore` rather than re-derived. Nine decisions outlive it. **The form binds five fields where the second criterion lists six**: neither `CreateMcpServerActionDto` nor `UpdateMcpServerActionDto` carries a permission field — `McpServerService` creates the gating permission alongside the server, names it after the server and re-syncs that name on every rename — so the board's "Linked permission" select is a control this API has nothing to bind, and rendering it disabled would be the shown-and-inert affordance US-203 rejected. A note under the Name field states the rule instead, which is also the only way the name-collision 400 makes sense to a reader. **Two auth types, not frame `5h`'s three**: `McpAuthTypes` has `None = 1` and `EntraIdOnBehalfOf = 2`, and **they travel as numbers** because the API registers no `JsonStringEnumConverter` — `IsInEnum` rejects the `0` an empty select parses to, which is why a create seeds `None` rather than a blank option. **Scope is conditional in both directions, because the validator is** — required for `EntraIdOnBehalfOf`, refused outright for `None` — and four mechanisms enforce it: `hidden()` in the schema (a hidden field contributes nothing to validity, which is what stops the `None` arm reaching a disabled Save explained by a control that is not on screen), the `@if` the API recommends alongside it, a clearing effect on an interactive switch, and `toMcpServerBody` normalizing `scope` to `null` rather than refusing — the branch that matters when an edit *seeds* a `None` server that already carries one, which is reachable because `McpServerDto.scope` is nullable and the API forbids the combination only on write. **The one place `ModelActionsStore`'s shape is deliberately not copied is the fourth criterion**: it asks for a `--fail` panel naming the server on a 502 *as well as* on a validation failure, so **any** rejection kept on the dialog's own surface renders there, a validation problem included, and raises no toast — reporting the same fact twice is the defect avoided; it falls back to a toast only once its surface is no longer the one on screen. `McpServerSaveFailure` carries `{ serverName, reason, traceLine }`, names **the row the reader opened** rather than the new name they typed, and `describeRejectedFields` builds the "The server rejected the URL." clause from the `errors` keys, returning null rather than naming a field the reader cannot find. **That criterion's 502 is unreachable on save** and should be recorded as such: `POST`/`PUT api/mcps` attempt no MCP connection, so `/problems/mcp-server-unavailable` arises only from a chat turn — the panel is built to render any error kept on the surface, which satisfies the intent and would cover a 502 the day a validation enabler introduces one. **One void event, `adminEvents.mcpCatalogChanged`**, mirroring `modelCatalogChanged` rather than the third criterion's `mcpServerCreated` naming: the list is ordered by name so a create's slot is the server's to decide, `DELETE` answers 204 with no body while the row must turn muted, a `PUT` renames the **linked permission** too and no response on this route describes it, and the set is unpaginated and small — two observers, `AdminMcpsStore.refresh()` and the root `McpCatalogStore.reload()` (not `ensureLoaded()`, because the memo is already resolved and `reload()` also replaces it). **Deactivated servers render, muted, with no kebab at all**, because `mcps/all` is the only route returning them and there is no reactivate route; the confirmation says what the cascade actually does — `DeactivateMcpServerAsync` deactivates the server, its linked permission **and every grant of that permission** in one atomic save, then invalidates the MCP client cache and every holder's permission-cache entry — carried on the `--warn` surface rather than as an aside, since no other delete in this application revokes other people's access. **The linked-permission badge names the permission from the server's own name**, with no second request, absent only on a deactivated row, and rides with a visually-hidden "Linked permission:" label because below the breakpoint `DataTable`'s badges slot has no column header. Two smaller ones worth carrying: an auth type this build does not know is **rendered** rather than hidden and its edit surfaces an explanation **ungated by `touched`** (the models dialog's provider-404 route, because nobody typed the value and the disabled Save prevents the interaction that would reveal it); and the Auth column is **150px, not frame `5g`'s 90px**, because "Entra ID (on behalf of)" is one of only two values it can hold and a narrower track ellipsises it permanently. The rail entry uses **`bi-plug`**, which is what `AdminNav.dc.html` draws and is already in the 75-glyph sprite. **No re-baseline**: the initial bundle measures **669.78 kB raw / 168.94 kB transfer** against 670 / 720 kB — **+0.15 kB**, all of it `McpCatalogStore`'s event handler and a third of US-1207's price because `withEventHandlers` and the `adminEvents` symbol were already on the graph — with **0.22 kB** of headroom left, `styles` unchanged at 63.96 kB, the lazy `admin-routes` chunk grown to 94.84 kB, and the admin area still absent from the initial graph. **US-1210 later added the sixth field** — Icon — so the form binds six today, still not the board's "Linked permission".
- **Acceptance criteria**:
  - Given the MCP tab, when it loads, then `GET /api/mcps/all` returns the administrative `McpServerDto` list rendered with name, description, url in JetBrains Mono, auth type, scope, a linked-permission badge, and an active/inactive status dot, and client-side search is fully correct — regime C (frame `5g`).
  - Given the add or edit form, when it renders, then it binds name, description, url, auth type, scope, and the linked permission (frame `5h`).
  - Given a server is deactivated, when the change lands, then `adminEvents.mcpServerCreated` or its deactivation counterpart fires so the root MCP catalog and the chat picker refresh across the route-scope boundary.
  - Given a 502 or a validation failure on save, when it returns, then a `--fail` panel at the top of the modal names the server, the failing field renders in its invalid state, and **every** value the administrator typed is retained (frame `5h`).

#### US-1209: Reach an admin tab by URL

- **Story**: As an administrator, I want to link a colleague straight to a specific admin tab so that we do not have to describe how to get there.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-203
- **Status**: ✅ Done (2026-08-17) — every criterion, but most of the work had already landed: US-1201 shipped the layout route, the rail marking the open tab **from the router** and the `/admin` → `/admin/users` redirect, because frame `5a` draws the rail as part of the users screen, and US-1207 and US-1208 each added their own route and rail entry in the same commit. What this story added is the **fourth tab its first criterion names** plus two route-table entries. Five decisions outlive it. **The reports route ships here rather than with US-1302**, resolving a deadlock: the docs page and the code comments had booked the fourth tab to US-1302, which `Depends on: US-1209` — while the rule that put it there ("a tab is added by the story that builds its route, in the same commit, never ahead of it") is unchanged, still binds, and was satisfied, since US-1209 simply *is* the story that built the route. Design frame `5i`, labelled "Reports — unavailable panel (**current state**)" and captioned "Required frame", is the design authority for shipping it in this state. **The tab renders the shared `UnavailablePanel`** with frame `5i`'s copy verbatim — its **first production consumer**, since US-106 built it against frames `4i` and `5i` and only `/ui-kit` had rendered it — which is FR-49 (P0) met: `GET api/reports/usage` does not exist (US-1301 is unstarted, and the API has seven endpoint modules with no `api/reports` prefix), so the tab shows no rows, no totals and no sample chart, and its spec asserts the absence in both directions, counting zero table and list elements **and** verifying against `HttpTestingController` that it issues no request at all. **There is no `ReportsStore`, and that departs from this document's own wording**: US-1302's first acceptance criterion asks for one holding zero records, but with no endpoint such a store would hold no state, expose no method and load nothing, while US-1209's own third criterion — opening a tab instantiates that tab's store and no other — is satisfied most strongly by a tab that instantiates none, and the criterion's *intent* is FR-49, which a storeless component satisfies more strongly than an empty store does; **`ReportsStore` arrives with the endpoint, in US-1302**, and nobody should add one earlier to satisfy that sentence literally. **One departure from frame `5i`, which draws the panel alone in the content pane: an `<h1>Reports</h1>`** — every sibling tab carries one and `UnavailablePanel`'s own heading is a `<p>` bound through `aria-labelledby` rather than a heading role, so without it this would be the only routed screen in the app with no level-one heading, a gap US-1405's axe gate (P7) would find rather than a fidelity win; the centring the frame *does* draw is honoured by making `.admin__content` a **flex column** (inert for the three table tabs — a blockified flex item in a column lays out exactly as the block it already was) rather than by a percentage `min-height`, whose definiteness the Flexbox spec does not guarantee through this chain. And **`/admin/<unknown>` now redirects to `/admin/users` instead of ejecting to `/chat`**, through a `**` child on the layout route: previously an unmatched segment made the router backtrack out of `/admin` entirely and fall to `app.routes.ts`'s own `**`. The consequence outlives the redirect and is the thing to carry forward — the layout is `path: ''` and matches by **prefix**, so **a new admin route must go above that entry** (or above the layout route itself, to render without the rail); added below either it is unreachable, and neither the build nor the linter reports it. The rail entry uses **`bi-graph-up`**, which is what `AdminNav.dc.html` draws and is already among the sprite's 75 glyphs. **No re-baseline, and the initial bundle is unchanged**: **669.78 kB raw / 168.95 kB transfer** against 670 / 720 kB — **+0.00 kB**, because nothing here is root-scoped (no new event on `adminEvents`, no `providedIn: 'root'` store, no new dependency) — with **0.22 kB** of headroom still, `styles` unchanged at 63.96 kB, the lazy `admin-routes` chunk grown 94.84 → **95.94 kB**, and the admin area still absent from the initial graph. One note for US-1302: `admin.routes.ts` names every tab with a static `component:` import, so a charting library would land in the shared `admin-routes` chunk that `/admin/users` also downloads — switching that one route to `loadComponent` is the cheap fix at that point.
- **Acceptance criteria**:
  - Given `/admin/users`, `/admin/models`, `/admin/mcps`, and `/admin/reports`, when each is opened directly, then that tab renders as the active one and the `AdminNav` rail marks it — tabs are routes, not local component state.
  - Given `/admin`, when opened, then it redirects to `/admin/users`.
  - Given a tab is opened, when its chunk loads, then only that tab's store is instantiated, scoped to the route.
  - Given the browser back button after switching tabs, when pressed, then the previous tab is restored.

#### US-1210: Choose a tool server's brand mark

- **Story**: As an administrator, I want to pick the icon a tool server shows so that users recognise it in the composer's picker without reading the name.
- **Priority**: P2 · **Estimate**: S · **Depends on**: US-416, US-1208
- **Status**: ✅ Done (2026-08-22) — a sixth field on the form US-1208 built with five, still not the board's "Linked permission". The interesting case is the one nobody types: a row a **newer** client wrote, carrying a key this build ships no artwork for. The `<select>` has no matching option and so shows blank, but the model keeps the key and the save sends it back unchanged — the note explaining that is ungated by `touched`, exactly as `unsupportedAuthType` is, and Save stays **enabled**, because leaving the value alone is the safe act. Review pinned that end to end rather than trusting it: `<select>` sets `selectedIndex = -1` with no matching option, and it is the one place the value could have been normalised to `''` behind the reader's back. The note is `role="status"`, not the `role="alert"` its sibling fields use — it is explicitly not a blocker, and interrupting the dialog's own announcement to say so would overstate it. `mcpIconLabel` was written for a table column that does not exist and was deleted rather than left as a comment narrating absent behaviour. Both new primitives joined `/ui-kit`, and the axe suites gained four audits — the Tools picker open and the MCP form open, in both themes — because neither surface had been scanned before: 50 audits, up from 46.
- **Acceptance criteria**:
  - Given the add or edit form, when it renders, then a sixth field offers the icons this build ships plus "None", previews the chosen mark beside the select, and sends the stored slug — or null for "None" — on both verbs.
  - Given a row whose stored icon key this build ships no artwork for, when it is opened, then the key is explained and **kept**, not blanked, and Save stays enabled: the value round-trips unchanged, so leaving it alone is the safe act.
  - Given the servers table, when it renders, then each row draws its mark beside the name, and a row with none draws nothing rather than a placeholder logo.
  - Given a saved icon, when the composer's Tools picker is next opened, then it shows the new mark without a reload, through the `adminEvents.mcpCatalogChanged` refresh US-1208 already wired.

### EP-13: Usage reports

#### US-1301: `[enabler]` Expose the usage audit trail over HTTP (B6)

- **Story**: Add a reporting API over the usage data. `ConversationUsage` and `ConversationUsageToolCall` already persist per-call token splits, durations, provider, model, tool names, and outcome — including abandoned turns and the out-of-band naming calls — but there is zero API surface over any of it. Unblocks US-1302.
- **Priority**: P2 · **Estimate**: L · **Depends on**: —
- **Status**: ✅ Done (2026-08-21) — every criterion. `GET api/reports/usage` aggregates `Core.ConversationUsage` behind `PermissionEndpointFilter.Require(PermissionIds.Administrator)`, taking `from`, `to` and `groupBy` (`model | provider | user | day`, default `model`) and answering, in one response, the period totals, the equal-length prior period, the outcome split, a dense UTC day series, the grouped breakdown and a fixed top-ten-models list — one request for the whole dashboard, because four round trips for four cards on one screen would make the range control feel broken. **The window is half-open, `[from, to)`, and refused rather than clamped** when `from >= to` or the span exceeds `Reports:MaxRangeDays` (366 days, configurable) — a 400 keyed `"from"`, on the reasoning every other refusal in this API uses: a client with a range control adjusts the start, not the end. **One deliberate departure from `docs/conversations/usage-and-favorites.md` §7's own outcome query**: every model call counts toward the totals and the outcome split, the out-of-band naming completion included, not only `Kind = 1` chat turns — a naming call that failed is still billed, and a turn count that disagreed with the donut beside it on screen would be a defect, not a nuance. `EstimatedCost` is **null, never zero**, when nothing in a group could be priced, restored from a per-group unpriced count because EF folds an all-null `SUM` to zero through its own `COALESCE`. A new covering index, `IX_ConversationUsage_DateCreated` (migration `20260821224810_AddConversationUsageReportingIndex`), makes the scan seekable; it is an **offline build inside `Database.Migrate()` at startup**, on the fastest-growing table in the schema, and the migration's own remarks record what that costs and the `ONLINE = ON` escape not taken by default. Verified in two halves: `ReportServiceTests` (SQLite) pins only the refusals that throw before a query is built, because SQLite translates no `DateTimeOffset` member and the day-bucketing query does not compile there at all; every aggregate behaviour — plus that the response **serialises camelCase**, which the TypeScript mirror in `domain/api/usage-report.ts` is written against verbatim — runs against real SQL Server 2025 in `ReportEndpointsIntegrationTests`. Backend: **1407 unit and 362 integration tests** (up from 1380/319). See [Usage and Favourites §7](../conversations/usage-and-favorites.md#7-reporting).
- **Acceptance criteria**:
  - Given `GET /api/reports/usage` with `from`, `to`, and `groupBy` across `model | provider | user | day`, when called by an administrator, then aggregated token counts and call counts are returned for the requested grouping.
  - Given a non-administrator, when the route is called, then it returns 403 `/problems/permission-required` naming the Administrator grant.
  - Given the outcome dimension, when results are returned, then completed, cancelled, and failed turns are distinguishable, because "tokens billed for answers nobody read" is the number the audit trail exists to surface.
  - Given a `from` later than `to`, or a range exceeding the configured maximum, when supplied, then it returns 400 as a validation problem naming the parameter.
  - Given the response, when it is produced, then it contains no prompt content, tool arguments, or tool results, consistent with the privacy posture of the data being reported on.
  - Given the dashboard US-1302 renders, when this response shape is designed, then it supplies per-group turn counts split by outcome, token counts, and an estimated cost figure, plus the period totals and prior-period deltas the four KPI tiles show — otherwise frames `5j` and `5k` cannot be built from this contract.

#### US-1302: See what the platform is spending

- **Story**: As an administrator, I want live usage reports so that I can answer what we spent and on what.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-1301, US-1209
- **Status**: ✅ Done (2026-08-21) — every criterion, **inheriting the route from US-1209 exactly as this story's own dependency always implied it would**: the URL, the rail entry, the page title and the `<h1>` are all untouched, and only what the panel occupied changed. `ReportsStore` — absent until now, the one deliberate wording departure the standing note carried against this story's own first criterion — finally exists, provided **on `AdminReports`, never on the layout**, which is what makes the second criterion's "a request on every entry, never cached" a property of DI scope rather than of code a reviewer has to trust. **No charting library**: the area chart, the bar list and the donut are hand-authored SVG and CSS, exactly as frames `5j`/`5k` draw them, which is also what keeps the shared `admin-routes` chunk free of a dependency every other admin tab would otherwise download too. **Two design-board corrections, both forced by contrast gates**: the area chart's line takes `--brand`, not the `--accent` the board draws, which measures 2.74:1 on light `--surface` and is the sole presentation of the daily series; and the KPI delta tints only its arrow, never the figure, because `--ok` at 11.5px measured 3.49:1 and failed as text. `CONTRAST_PAIRS` gained 16 rows for the two. **`?range=` always pushes; `?group=` follows the directory's own push-or-replace rule** — a transition to or from the default grouping pushes, a swap between two non-default groupings replaces. **The table's six numeric columns turn proportional below the desktop edge (1024px)**, retiring US-1403's criterion 5, vacuous for two phases, now asserted by a real browser overflow check at three widths. The footer is the shared `Paginator`, with `[liveSummary]="false"` beside `DataTable`'s own `[summary]`, so there is exactly one live region. Frontend: **2002 tests across 153 files and 46 browser accessibility audits** (up from 1931/146/41); the initial bundle measures **672.06 kB raw / 169.20 kB transfer** against the unchanged 675/720 kB budget — **+0.07 kB**, none of it attributable to this story, since every reports file rides the lazy `admin-routes` chunk (grown 95.9 → 127.13 kB) exactly as the panel it replaced did. None of this has been exercised against a live, Entra-authenticated deployment. See [Administration §12](../ui/administration.md#12-the-reports-dashboard-us-1302).
- **Acceptance criteria**:
  - Given US-1301 has not landed, when the reports tab is opened, then `UnavailablePanel` renders frame `5i` — the ridgeline motif, "Usage reports aren't available yet", and a line stating the reporting API is not enabled for this deployment — and `ReportsStore` holds zero records: no sample chart, no placeholder totals. **(Met by US-1209, 2026-08-17, with no store at all; superseded by the dashboard above, 2026-08-21.)**
  - Given US-1301 has landed, when the tab is opened, then a request is issued on **every** entry and no cached result is displayed, including on a back-navigation to the tab.
  - Given the dashboard renders, when it is compared against frames `5j` and `5k`, then it carries a 7/30/90-day segmented range control, a date-range chip, a Group-by select over Model, Provider, User, and Day, four KPI tiles each with a delta against the prior period, a usage-over-time area chart, a top-10 tokens-by-model horizontal bar chart, a turn-outcome donut labelled with the combined cancelled-and-failed percentage and the tokens spent on those turns, and a paged per-model table of turns, completed, cancelled, failed, tokens, and estimated cost — legible in both themes.
  - Given a date range and grouping control, when either changes, then a new request is issued and both are reflected in the URL query params.
  - Given a range returning no rows, when it renders, then an empty state names the range rather than showing zeroes as data, and no export control is offered.
  - Given the request fails, when the error renders, then the `traceId` is shown with a retry control.

### EP-14: Access and conformance gates

On accessibility this document is authoritative over the design boards. The prototypes carry known gaps — no `aria-live` region for streaming text or toasts, icon-only controls with no accessible name, and clickable `<i>` elements — and the stories below correct them rather than reproduce them.

#### US-1401: Use the whole app from the keyboard

- **Story**: As a keyboard-only user, I want to complete every core task without a mouse so that the app is usable to me.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-606, US-903, US-1201
- **Status**: ✅ Done (2026-08-17) — every criterion, and this is a **conformance** story rather than a build one: `shared/a11y/`, the native `<dialog>` overlays with focus return, the roving-focus `Menu` and the absence of a single clickable `<i>` or `<div>` meant criteria 1–3 were largely already true. What it did was close the two real gaps, turn the claims into gates, and pin the rest. **Gap one: a code block was a keyboard trap** (SC 2.1.1). `_markdown.scss` gives every `.md-code pre` `overflow: auto` and it carried no `tabindex`, so an overflowing line could not be scrolled without a pointer; `renderer.code` now emits `tabindex="0"`, `tabindex` joined `CHAT_ALLOWED_ATTR`, and `pre:focus-visible` takes an inset `--accent` ring. **The block is focusable and deliberately unnamed**, which is the non-obvious half: the obvious `aria-label="ts code block"` is *invalid*, because HTML-AAM maps `<pre>` to `role="generic"` whose name ARIA 1.2 prohibits and axe's `aria-prohibited-attr` fails it — and naming it properly would mean `role="group"` plus a second entry in the sanitizer profile, for a scroll box. `tabindex` alone is what SC 2.1.1 and `scrollable-region-focusable` ask for, and the Copy control beside it already carries "Copy ts code block". **Gap two: five focus indicators failed SC 1.4.11, and one of them was every button in the app.** `--ring` is `rgba(33,168,216,.45)` — `ringpulse`'s colour — and composites to about 1.4:1 on `--surface`; it was drawing the indicator on three composer controls, the empty-state chips and the composer card, while `.form-control:focus` used an 18%-alpha accent glow and `.is-invalid:focus` the same in red. Four rules replace them. A **global `:focus-visible` floor** in the new `src/styles/_focus.scss`, imported after `bootstrap-components` and before `kit` — and **explicitly not the whole answer**, which the first cut got wrong: Bootstrap declares `outline: 0` on `.btn`, `.nav-link` and `.form-check-input` at `(0,2,0)`, which beats a bare `:focus-visible` at `(0,1,0)` whatever the source order, so the floor reached none of them and what they drew instead was off-palette and well under 3:1 (`rgba(55,81,105,.5)` = 2.4:1 light / 1.3:1 dark on the primary button; `rgba(13,110,253,.25)` = 1.41:1 / 1.31:1 on the other two) — that is every dialog footer button, every admin row action, the auth calls to action, the project-detail tabs and every checkbox, and all three are now overridden by name. The floor's real beneficiaries are elements with no rule at all. **The checkbox needs two rules and the order is load-bearing**: Chrome does not match `:focus-visible` on a checkbox focused by pointer, so a `:focus` rule clears Bootstrap's wash and a `:focus-visible` rule *after* it draws the ring; both are `(0,2,0)`, a keyboard-focused box matches both, and reversing them would suppress the ring the second draws. And **the ring is an `outline` with an offset, never a flush `box-shadow` on a filled control** — drawn *on* `--btnP-bg` it measures 2.89:1 in light and **1.00:1** in dark, where the button fill *is* the focus token; offset, it sits on the page behind, which is the surface the gate measures. **Two gates, both in `npm run lint`.** `check-tokens.mjs` measures `--focus-ring` at 3:1 against `surface`, `surface-2`, `bs-body-bg`, `active-bg` and `think-bg` in both themes and `--fail` against `surface` — **38 pairs, up from 26** — plus a has-the-record-fallen-behind guard in *both* directions: every token any `.scss` paints as a background must be measured or excluded with a reason, and every exclusion must still correspond to something painted. The scan follows **variable indirection** (`--anything-bg: var(--x)`), because that is how every Bootstrap fill is set, and matching only the direct form missed the hovered primary button silently. `check-forbidden-apis.mjs` gained a suppressed-indicator pattern (`outline: none|0`, all spellings) and `var(--ring)` outside its keyframe, with a per-line `focus-check-ignore-next-line` hatch over four legitimate sites — **scoped to `escapable` rules**, after an earlier cut applied it to all seven and would have let a comment wave through `bypassSecurityTrustHtml`; caught in review and verified fixed by planting such a call. **One shared testing helper changed**: `src/testing/dialog-polyfill.ts` now dispatches `close` through `queueMicrotask`, as the platform does, because a synchronous shim made `Modal`'s destroy-time focus restore unfalsifiable — deleting that restore now fails `modal.spec.ts`, where before every spec stayed green. **Verification specs cover the criteria that were already true**: a `tabbableWithin` tab-order assertion over the composer's seven controls and their accessible names (Send correctly absent until there is text, the hidden file input excluded), Stop reachable mid-turn as *the same element* Send was with focus still on it, focus returning on modal *destroy*, and rename plus delete driven end to end from the keyboard through the sidebar kebab — activation there is `.click()`, because `MenuItem` sits on native `<button>`s so Enter and Space are the platform's guarantee and jsdom synthesises no click from a key event. A `:has()` coupling is pinned rather than trusted: the composer textarea suppresses its own outline, so the card's `:has(.composer__prompt:focus)` ring is its only indicator, and a class rename would remove it silently with `check:forbidden` green. Four limits are recorded rather than hidden. **The `outline` scan reads `src/` only, not the compiled stylesheet** — which is exactly how Bootstrap's five `outline: 0` declarations passed the gate written to catch them; running it over `dist/` would close it but needs a build step `check:forbidden` does not have. **A focused `.btn` held down briefly regains Bootstrap's wash**, since `.btn:first-child:active:focus-visible` is `(0,3,0)` — transient, cosmetic, and with no persistent case, because `.btn.active`, `.btn.show` and `.btn-check` appear nowhere. **Every code block now costs two tab stops**, Copy and the `<pre>`, unconditionally. And **jsdom cannot verify the top layer, `::backdrop`, light dismiss or native inerting** — browser-only, checked through `/ui-kit`. **1697 tests pass**, lint green. **No re-baseline, and 0.07 kB of headroom left**: the initial bundle measures **669.93 kB raw / 168.91 kB transfer** against 670 / 720 kB, `styles` **64.17 kB** against 65 / 80 kB — the +0.15 kB is entirely global CSS, and dropping Bootstrap's unused `helpers/focus-ring` import paid back most of the override's cost. US-1405 owns budgets and is the right place to re-state that line. The **`--warn` contrast question (§9) is still open and still blocks US-1405's axe gate**: light `#A96A10` is 4.41:1 on white, 4.21:1 on `--bs-body-bg` and 3.99:1 on `--warn-bg`, all under 4.5:1, and `check:tokens` blocks a local override — it must move upstream in `docs/design/project/theme.css`. See [Design System §8.1](../ui/design-system.md#81-focus) and [Answer Rendering §3.5](../ui/answer-rendering.md#35-the-code-block-chrome-and-its-copy-control-us-603).
- **Acceptance criteria**:
  - Given the composer, when navigating by keyboard, then the textarea, attach, model picker, MCP picker, voice, and send/stop are all reachable in a logical tab order, and the pickers open, navigate, and select with arrow keys and Enter.
  - Given a modal or offcanvas, when it opens, then focus moves inside and is trapped, Escape closes it, and focus returns to the invoking control.
  - Given the full paths for send, stop, rename, and delete, when each is performed with the keyboard only, then it completes without a pointer.
  - Given any interactive element, when it receives focus, then a visible focus indicator meets WCAG 2.1 AA non-text contrast at 3:1.

#### US-1402: Follow a streaming answer with a screen reader

- **Story**: As a screen-reader user, I want to be told when the assistant is working and when new content arrives so that I am not left in silence.
- **Priority**: P0 · **Estimate**: S · **Depends on**: US-406, US-501
- **Status**: ✅ Done (2026-08-17) — every criterion, and criterion 2 was already met by US-106's two toast regions, mounted once at `app.ts` and never calling `.focus()`. **Criteria 1 and 3 look contradictory and are reconciled by ARIA's own batching rule**: a polite region over streaming text would read every delta, so the streaming answer's `.assistant-turn__content` is `aria-live="polite"` **and carries its own `aria-busy`**, which accumulates the changes and exposes them once when busy clears — released as an explicit `aria-busy="false"` rather than by removing the attribute, because that release is the single mutation the pattern hangs on. Nothing counts deltas, debounces or samples on a timer. Six decisions outlive the story. **The settled turn carries no `aria-live` at all**, so an answer is announced exactly once; that guard is at the _call site_ in `transcript.html` rather than in the component, which is why it is pinned in `transcript.spec.ts` — the component's own spec flips the input on one instance and structurally cannot prove the template never passes it. **The transcript's `aria-busy` releases on the folded `Finished` frame**, `historyPending() || (inFlight() && !answerComplete())` against the new `answerComplete` (`snapshot.phase() === 'Completed'`), which is one step earlier than `!inFlight()` — criterion 4 names the frame, and reading the frame is also what leaves the live turn mounted at the moment its deferred region is released. **Activity cards and the reasoning region are excluded with `aria-live="off"`** at their call sites: without it, releasing the answer flushes every card's state label, kind, source and growing sub-status list at the reader first, and a second time, since the status region below already narrated each activity live — one attribute covers the recursive nested cards, because a live region covers all descendants and the recursive `<app-activity-card>` declares none of its own. **The running commentary is a separate persistent `role="status"` region in `chat.html`, outside the `aria-busy` container**, because anything inside it is deferred along with the answer; it is last in the document, like the transcript's own two regions, since `visually-hidden` is clipped rather than hidden and a region at the top would put "Andes Test MCP is running" above the conversation title for anyone reading in order. **It speaks the new pure `domain/stream/turn-announcement.ts`**, which satisfies criterion 3 **by construction rather than by throttling**: every branch is a fact that changes only on a status transition, and the one branch reading `snapshot.text` reads it as a boolean, so a thousand deltas produce one transition — over the recorded twelve-event turn it yields seven distinct strings and repeats none. Three decisions inside it: the headline follows the **pre-order-last** activity rather than the innermost running one (both announce every start; only this avoids announcing a parent twice when a nested child settles inside it — the cost, stated plainly, is that only that node's transitions are announced, so a parent's completion is silent once it has any child and an earlier root's once a later root exists, which is safe only because a failure at any depth is carried by the count clause and the answer's arrival takes the headline); failures are **additive rather than ranked**, because first freezes the region on them for the rest of the turn and last buries them, and the clause is a count net of any failure the headline already named, since the snapshot records no order among terminal states; and word forms rather than the em dash ("Andes Test MCP is running"), because at verbose punctuation levels some readers voice "—" as "em dash". **`turnStatus` returns `'Answer ready'` from the folded `Finished` until the settle, and that is a backstop rather than a duplicate**: releasing the answer's region is an `aria-busy` change with **no accompanying content mutation**, so it depends on assistive tech having buffered what it suppressed and replaying it — the spec's intent, but the least reliably implemented corner of the live-region model, and a reader whose AT suppressed-and-forgot would meet exactly the silence this story exists to remove. Two limits are recorded rather than hidden. **The `aria-busy` release itself is not verifiable by any unit test** — jsdom has no accessibility tree — and **has not been run against NVDA, JAWS or VoiceOver**; `'Answer ready'` is what makes shipping it acceptable. And **`answerComplete` trusts `Finished` to be the last thing that matters** — the contract says it "arrives only when the turn ran to completion" ([streaming contract §4.1](../conversations/streaming-contract.md#41-event-kinds)), while the vendored fold enforces nothing and keeps appending text to a `Completed` snapshot: a server framing a `TextDelta` afterwards would leave the answer region live and no longer busy, and every later delta would be announced alone — the alternative drops those deltas, losing answer text to protect an announcement. **No re-baseline**: the initial bundle is unchanged at **669.78 kB raw / 168.97 kB transfer** against 670 / 720 kB, `styles` unchanged at 63.96 kB, with everything on the lazy `chat` chunk because the new domain module is reached only from `TurnStore`. See [Answer Rendering §10](../ui/answer-rendering.md#10-following-a-streaming-answer-with-a-screen-reader-us-1402).
- **Acceptance criteria**:
  - Given a turn in flight, when it starts, then the transcript region carries `aria-busy="true"` and the streaming answer region is `aria-live="polite"`.
  - Given the toast stack, when a toast is raised, then it is announced through an `aria-live` region without stealing focus.
  - Given activity cards appearing during a turn, when they render, then announcements are not so frequent as to make the live region unusable — status changes are announced, individual text deltas are not.
  - Given the turn completes, when `Finished` arrives, then `aria-busy` is cleared.

#### US-1403: Use the app on a tablet or a phone

- **Story**: As a user away from my desk, I want the app usable at tablet and phone widths so that I can work from any device.
- **Priority**: P0 · **Estimate**: M · **Depends on**: US-301, US-1201
- **Status**: ✅ Done (2026-08-18) — and like US-1401 this is a **conformance** story: criterion 1 was already met by US-301/US-401, and criteria 4a and 4b by US-106's `DataTable` and US-1201's `PillSubnav`, so what shipped is criteria 2, 3 and 6 plus the record that makes all of them checkable. **Criterion 5 is vacuous and is recorded as such**: `/admin/reports` is US-1209's `UnavailablePanel` until US-1302, so there are no charts to stack, and US-1302 owns that criterion when the dashboard exists. **One breakpoint record.** Twelve stylesheets and two components carried **six spellings** of the two numbers the boards restructure at, and the mismatched pair is a real defect rather than untidiness: `(max-width: 767px)` beside `(min-width: 768px)` leaves a viewport reported as 767.5px — browser zoom, or the Windows display scaling this app ships on — matching *neither* mode. `src/styles/_breakpoints.scss` (variables plus five mixins, and **it emits no CSS**, which is exactly what makes it safe to `@use` where `_tokens.scss` may never be) and `src/app/shared/layout/breakpoints.ts` now state them once each, because Sass cannot read a TypeScript constant and `matchMedia` cannot read a Sass variable; two new gates keep the pair honest — a `check-forbidden-apis.mjs` rule refusing a hardcoded application breakpoint (component-local widths at 480/575/575.98px are deliberately untouched) and a `check-tokens.mjs` parity check covering both the whole-number minima **and** that every maximum is 0.02px below one, both reading the files comment-stripped because both document the bug by quoting the wrong queries. **`Sidebar` became a controlled component** (`mode` input, `toggled`/`searchRequested` outputs, no `UiStore`), because the same chevron persists a width at desktop and opens a transient overlay at tablet and only the shell can see which. Four shell decisions outlive the story. **The persisted `egpt.sidebar` preference is read and written at desktop only** — at tablet the strip is the viewport's default, which is frame `3g`'s own caption, so expanding there must not change the width the same user meets on a laptop. **The overlay flag is a `linkedSignal` on `Shell` sourced from `mode`, not a field on `UiStore`**: every write to that store goes to `localStorage` and this is a gesture, and deriving it rather than resetting from an `effect` makes the dismissal *atomic with the mode read* — an effect would land one pass late, and for that window a mobile drawer would read as a tablet overlay and put `inert` on `<main>`. **Tablet (frame `3g`) is structural, not arithmetic**: a non-modal absolutely-positioned panel plus backdrop inside the shell, `inert` on `.shell__main` (a backdrop that stops the pointer and not the keyboard is a backdrop that lies), and a `.shell__rail` spacer holding the 60px track the panel vacated — which is what makes "the main content never reflows" a guarantee rather than a sum. **Mobile (frame `3e`) reuses `Offcanvas`** — the native `<dialog>` with `showModal()` — with a new `bare` input that zeroes its padding and **clips** its `<h2>` rather than hiding it, because that heading is the dialog's accessible name. **There is exactly one `<app-sidebar>` declaration, rendered at one of two outlets** through an `<ng-template>`: both of `Sidebar`'s branches emit `<nav id="sidebar-primary" aria-label="Primary">`, so a second instance is a `duplicate-id` violation at serious impact plus two identically named landmarks, and the drawer's copy is additionally `@if`-guarded because `Offcanvas`'s `<dialog>` is unconditionally in the DOM. **The mobile navbar** (`features/shell/mobile-navbar/`, frames `1d`/`5m`) is 54px with a hamburger, brand mark, title and a route-fed trailing slot; the seam is **`ShellChrome`** (`core/ui/shell-chrome.ts`, `@Injectable()` with no `providedIn`, provided at the `Shell` component so it rides the lazy chunk), carrying a title and a `TemplateRef` — `Chat` publishes its star+kebab template and clears on destroy, `AdminLayout` publishes "Admin" — mirroring `COMPOSER_HOST` from US-906, and it **retires US-308's last deferred criterion**. The chat `<h1>` is **clipped, not `display: none`d**, below 768px, the same call `conversations.scss` already makes. **`anchoredPosition` gained a viewport, returns a `width`, clamps `left` and spans full width below 768px** — and the clamp fixes a **pre-existing** defect at *every* width, since nothing clamped `left` before and a 360px picker opened near an edge simply ran off screen; it reads `documentElement.clientWidth` rather than `innerWidth`, because a `position: fixed` panel's containing block excludes the classic scrollbar and sizing from `innerWidth` is the JS spelling of the `100vw` mistake `offcanvas.scss` calls out. **The project-detail tabs below 768px reuse `PillSubnav`** (which gained an optional `count`) rather than frame `4e`'s stacked accordions — a **product-owner-approved departure**: the three tabs are real child routes, one behind a `canDeactivate` guard that can refuse the navigation, so `aria-expanded` on a `routerLink` would announce a state the control neither owns nor can guarantee. **`viewport-fit=cover`** was added to `index.html` plus four `--safe-*` tokens in `_kit.scss`, because `env(safe-area-inset-*)` resolves to 0 without it — consumed by the navbar, the composer, the transcript gutters, the drawer and the toast region. **Two pre-existing bugs fixed**: the projects grid used `row g-3`/`col-*` classes that **never existed** (`bootstrap/scss/grid` is deliberately not imported), so the "grid" had been a block stack at every width, replaced with `repeat(auto-fill, minmax(min(280px, 100%), 1fr))`; and four routed hosts were being **clipped below the fold** by `.shell__main { overflow: hidden }` with no scroll container of their own. See [Responsive layout](../ui/responsive-layout.md) and [Shell and Navigation §5.5](../ui/shell-and-navigation.md#55-three-viewport-modes-us-1403).
- **Acceptance criteria**:
  - Given a viewport at or above 1024px, when the app renders, then the sidebar is persistent — expanded at 260px or a 60px strip — and the conversation column stays centred at a maximum of 820px (frame `3f`).
  - Given a viewport between 768px and 1023px, when the app renders, then the sidebar defaults to the 60px strip and expands as a 260px overlay with a backdrop, the main content **never reflows**, and tapping the backdrop collapses it (frame `3g`).
  - Given a viewport below 768px, when the app renders, then the sidebar is hidden behind a navbar hamburger that opens it as a full-screen overlay with focus trapped and returned to the hamburger on close, the composer is full-width with 16px gutters and safe-area padding, and the picker dropdowns are full-width (frames `3e`, `1d`).
  - Given a viewport below 768px, when an admin table renders, then it collapses to stacked cards carrying the same actions at 44px targets and the admin sub-nav becomes horizontally scrollable pills (frame `5m`), while the project-detail tab strip becomes stacked accordions (frame `4e`).
  - Given a viewport below 1024px, when the reports dashboard renders, then its charts stack into a single column (frames `5j`, `5k`).
  - Given each of the three breakpoints, when the chat, projects, documents, and admin routes are exercised, then no horizontal scrollbar appears on the document body.

#### US-1404: Respect a reduced-motion preference

- **Story**: As a user sensitive to motion, I want animations suppressed when my system says so.
- **Priority**: P1 · **Estimate**: S · **Depends on**: US-106
- **Status**: ✅ Done (2026-08-18) — every criterion, and the story's real output is a **finding**: the global block in `_motion.scss` **already covers every animation site correctly**, verified end-state by end-state rather than assumed. Floored to one iteration of 0.01ms with no `animation-fill-mode`, `spin` settles at `rotate(360deg)` — where it started — and both `ringpulse` sites settle with no box-shadow while losing nothing legible, since the recording pill keeps its tint and its elapsed timer. **So no redundant `animation: none` overrides were added**, and the four sites that *do* carry a local override are exactly the four whose floored end-state would be *wrong*: the `blink` caret would settle invisible, `ridgedash` would leave a blank gap in the ridgeline, the activity spinner keeps a recognisable three-quarter arc, and the toast's drain bar is hidden outright rather than flashed to empty. **Three of the four criteria name motion the application does not have**: there is no typewriter animation, no toast enter/leave slide (`@angular/animations` is not a dependency) and no message-appearance fade-slide. §5's third timing, "200ms for message appearance", **describes an unimplemented affordance** — a message is inserted with no transition and no animation — and `_motion.scss` now records that rather than closing it: adding an animation inside the story whose purpose is to suppress animation is the wrong direction, and a fade-slide on insertion would also disturb US-1402's live-region contract, where the settled turn replacing the streaming one must not re-announce. `--t-slow`'s stated example is therefore the sidebar width, which is real. **What changed in code is two declarations**: `animation-delay: 0s` and `transition-delay: 0s` joined the block, because a delay survives a floored duration and reappears as a late jump; Bootstrap's own reboot omits them, and that omission is a gap rather than a decision, cheap to close while nothing in `src/` declares one. **Two new gates and one spec.** `check-forbidden-apis.mjs` gained a fifth-keyframe rule, word-bounded so `@keyframes spinner` cannot pass as `spin`, and **not** exempting `_motion.scss` — a fifth keyframe is no more welcome there than anywhere else. `check-tokens.mjs` gained **keyframe parity**, holding all four bodies to `docs/design/project/theme.css` with whitespace and the optional trailing semicolon removed, since `_motion.scss` is Prettier-formatted and `theme.css` is minified and those are the only differences that are not a change of behaviour — a rule asserted in prose by four documents and enforced by nothing until now. `src/styles/motion.spec.ts` pins the third leg: the suppression block itself, the **`@import 'motion'` last position in `styles.scss`** that the whole contract rests on, and that no `animation:` in `src/` names a keyframe nobody defines. See [Design System §3.4](../ui/design-system.md#34-motion).
- **Acceptance criteria**:
  - Given `prefers-reduced-motion: reduce`, when a turn streams, then text appears without a typewriter animation and both the `blink` caret and the `ridgedash` thinking indicator render static.
  - Given the same preference, when a toast is raised or dismissed, then it appears and disappears without the slide transition.
  - Given the same preference, when the sidebar collapses, a message appears, the jump-to-latest control shows, or a voice recording starts, then the width and fade-slide transitions are suppressed and neither `spin` nor `ringpulse` animates.
  - Given the preference is not set, when the same interactions occur, then the transition timings stated once in §5 apply, and all four keyframes — `blink`, `spin`, `ringpulse`, `ridgedash` — run as `theme.css` defines them.

#### US-1405: `[enabler]` Gate accessibility and budgets in the build

- **Story**: Turn the accessibility and size criteria into automated gates so they cannot regress silently. Unblocks the success criteria in section 1.
- **Priority**: P1 · **Estimate**: M · **Depends on**: US-108, US-1401
- **Status**: ✅ Done (2026-08-18) — every criterion, and **three of the four were already satisfied**: (b) since US-601 and US-605, whose `check-initial-chunk.mjs` keeps `mermaid` and `katex` out of both the initial and the chat graphs; (c) by `upload-store.spec.ts`'s "replaces records rather than mutating them", which pins the exact failure mode the criterion names ("upload progress and attachment chips"); and (d) by `Andes contract drift`, already a job in `ui-ci.yml`. The work was (a). **A second Vitest target, `test-a11y`**, runs the new `*.a11y.spec.ts` files in headless Chromium (`@vitest/browser-playwright` + `playwright` + `axe-core` as devDependencies), with `browserViewport: "1280x900"` and **`setupFiles: []`**; the main `test` target excludes them. Both of those are load-bearing. **A browser and not jsdom**, because roughly half of what axe checks is computed style, and a jsdom run reports the colour rules as `incomplete` and passes — which reads as a clean bill of health and is not one; it is also what makes "in both themes" mean anything, since the two themes differ only in custom properties. **`setupFiles` empty**, because `setup-dom.ts`'s `matchMedia`/`IntersectionObserver`/`ResizeObserver` fakes would shadow the real browser APIs and answer `false` to every media query. **The viewport line is not cosmetic**: Vitest's browser viewport defaults to 414×896, so without it every audit ran at a phone width where the sidebar, the data tables and the rail are never rendered. `src/testing/a11y.ts` is the harness (`expectNoSeriousViolations`, `applyTheme`, `atViewport`, `expectNoHorizontalOverflow`) and `src/testing/a11y-routes.ts` holds `AXE_ROUTES` separately so the jsdom coverage guard needs no `axe-core`; five spec files, **32 tests** — all 7 existing routes × 2 themes, plus the full shell at three widths × 2 themes. **`shell.a11y.spec.ts` also carries US-1403's criterion 6** — no horizontal overflow at three breakpoints, which jsdom cannot assert at all — and it is the only audit leaving axe's `region` rule enabled, because it is the one that renders real landmarks. `src/testing/a11y-coverage.spec.ts` (jsdom) asserts every `AXE_ROUTES` entry is actually **navigated to** in an audit, not merely mentioned, which a doc comment would satisfy. **The run found two AA failures, and both were fixed upstream in `docs/design/project/theme.css`** — mirrored in `_tokens.scss` and now gated by new `CONTRAST_PAIRS` rows (**46 pairs, up from 38**), since `check:tokens` enforces parity with the bundle and a local override fails by construction. Light `--warn` `#A96A10` → `#9C6210`, **closing the open question in §9** (it measured 4.41 / 4.21 / 3.99:1). Dark `--active-bg` `#173A58` → `#16354D`, where `--brand` measured **4.29:1** — the admin rail's active tab, the sidebar's active nav link and the paginator's current page — found **by the new axe run** rather than by the table, and only after the viewport was pinned to desktop; it had been invisible for three stories because the existing surfaces guard measures `--focus-ring` against every painted background, not the text drawn on it. `check-tokens.mjs` gained an `under:` option so a token that is translucent in one theme and opaque in the other can be measured in both. `check-initial-chunk.mjs` gained an `axe-core` entry, because a spec-only 600 kB dependency is invisible to the budgets and an accidental import would ship an auditor to every user. **Budgets re-baselined, as US-1401 said this story should**: initial warn 670 → **675 kB** against a measured **670.33 kB**, `styles` warn 65 → **66 kB** against **64.47 kB**, error ceilings unchanged at 720 / 80 kB — the previous 0.07 kB of headroom was not headroom. **CI**: two steps in the existing `Lint, test, build` job, **after `Build`** because this is the most expensive step in it, plus a `failure()`-gated upload of the browser-mode screenshots; a new job name would need a branch-protection change nobody would make, so **job names are unchanged and branch protection needs none**. Four limits are recorded rather than hidden: `moderate` and `minor` violations are **discarded, not reported**, and `resultTypes: ['violations']` means `incomplete` is never populated — deliberate, because a gate that prints advisories nobody reads trains people to skim it; **`/documents` is absent from `AXE_ROUTES`** because the route does not exist until US-1002, with the absence asserted so that story adds both together; the overflow check reports only containers that *clip*, since an `auto`/`scroll` region is the design; and automated auditing remains a floor, with US-1402's screen-reader pass still outstanding. **1750 jsdom tests and 32 browser audits pass**, with `format:check`, `lint`, `build` and `check:contract` green. See [Accessibility audit](../ui/accessibility-audit.md).
- **Acceptance criteria**:
  - Given the chat, projects, documents, and admin routes, when the automated axe-core run executes in both light and dark themes, then it reports zero serious or critical violations and fails the build otherwise.
  - Given a build, when the initial chunk is measured, then it is within the configured budget and the build fails if the diagram or math library is present in it.
  - Given the test suite, when it runs with `provideCheckNoChangesConfig({ exhaustive: true })`, then no view depends on a mutated plain object — the two places the old client got this wrong were upload progress and attachment chips.
  - Given the vendored Andes contract, when CI runs, then the diff check from US-107 executes as part of the same pipeline.

### EP-15: Conversation export

#### US-1501: `[enabler]` Expose conversation export over HTTP (B11)

- **Story**: Add a route that renders a conversation to a downloadable document. `DocumentEndpoints.cs` serves only uploaded-document downloads under `api/documents`, and nothing anywhere renders a transcript to a file, so the client has nothing to call. Unblocks US-1502.
- **Priority**: P2 · **Estimate**: L · **Depends on**: —
- **Status**: ✅ Done (2026-08-19) — five formats behind one registered-renderer-per-format registry rather than a branch in the service. `html`, `json` and `md` all **read** what persist time already computed or preserved; `docx` and `pdf` **re-render**, walking each message's markdown once through the same hardened Markdig pipeline that produces `htmlContent` into a shared, renderer-neutral block model (`MarkdownBlockMapper`), so the two binary formats cannot disagree about what a message meant and both inherit the link-scheme allowlist that already guards the stored transcript. The response is the rendered bytes in the body — closing this story's either/or first criterion in favour of a body, since a signed URL would need blob storage behind every export, markdown included, and would leave rendered transcripts for something to clean up. PDFsharp/MigraDoc 6.2.4 (MIT, unlike QuestPDF's revenue-gated Community tier) renders `.docx` and `.pdf`; PDF alone depends on a font, resolved once at startup by `ExportFontResolver`, so a deployment with none serves a typed `503 /problems/export-renderer-not-configured` naming the format rather than a bare 500 or a broken file — closing this story's fifth criterion and the §9 open question below. Images are dropped from `.docx`/`.pdf`, alt text kept: embedding one means the API fetching a URL that model output chose, from its own network position. **1274 unit tests and 284 integration tests** pass (up from 1152/250); `OpenXmlValidator` run against the generated `.docx` caught four schema child-order bugs a round-trip read through the same SDK had missed. See [docs/conversations/conversation-export.md](../conversations/conversation-export.md).
- **Acceptance criteria**:
  - Given an export route on the conversation group with a requested format of `md`, `docx`, or `pdf`, when it is called by the conversation's owner, then the rendered document is returned — either as the response body or as a short-lived signed URL under the same rules `DocumentDownloadDto` already uses — with a signed `Content-Disposition` filename and `Cache-Control: no-store`.
  - Given the rendered document, when its contents are inspected, then it carries the conversation name, the prompts, and the completed assistant answers only — no activity cards, no reasoning text, and no unsaved partial answer, consistent with what the transcript persists.
  - Given a conversation belonging to another user or deactivated, when the route is called, then it returns 404, consistent with the rest of the conversation group.
  - Given an unsupported format value, when it is supplied, then it returns 400 as a validation problem naming the parameter and the supported set.
  - Given the renderer for `docx` or `pdf` is not configured in a deployment, when the route is called for that format, then it returns a typed problem the client can distinguish from a transient failure, rather than a bare 500.

#### US-1502: Download a conversation

- **Story**: As a user, I want to take a conversation out of the platform as a Markdown, Word, or PDF file so that I can attach it to a ticket or share it outside the tool.
- **Priority**: P2 · **Estimate**: M · **Depends on**: US-1501, US-106
- **Status**: ✅ Done (2026-08-19) — one `ConversationDownloadMenu` behind both triggers (frame `2f`): the composer's control row and the conversation header, both reading a root `ConversationExportStore` shaped after `DocumentDownloadStore` with its transport decision reversed — the export response is a `blob` fetched directly rather than a signed URL, because rendering to blob storage first would put every export, markdown included, behind a storage dependency it does not need. The request opts out of the retry interceptor and reads its own error body back by hand (`toAppErrorFromBlobError`), since `HttpClient` parses an error body as JSON only for `responseType: 'json'` requests and an unclassified body would otherwise let an application-typed 503 be retried as if it were a gateway hiccup. The chosen item shows a spin ring and "Preparing `<format>`…" while the other two dim, `stayOpen` keeps the panel up through the click that starts the download, and the menu closes only on a successful settle — a failure leaves it open beside the traceId-bearing error toast. Two menus, one store: the outcome is a replaced record carrying a sequence number rather than an event, so each mounted instance reacts to a settle exactly once. The control is **absent** (`TurnStore.exportTarget()` is `null`) until a transcript holds a persisted message, and **disabled** with a tooltip reason while a turn streams — `Menu` gained a `hint` input for that, and now returns focus to its trigger when a consumer clears its two-way `open` model directly; `Tooltip` no-ops on an empty string. The object URL is revoked ten seconds after the download starts, not immediately — revoking sooner cancels a download some browsers have not begun reading yet. **1830 tests** pass (up from 1788) and **38 browser accessibility audits** (up from 36, auditing the menu open in both themes). Initial bundle **671.59 kB raw / 168.92 kB transfer**, +0.91 kB, inside the unchanged 675/720 kB budget. See [docs/ui/conversation-download.md](../ui/conversation-download.md).
- **Acceptance criteria**:
  - Given US-1501 has not landed, when the composer renders, then the download control is **absent** — not shown-and-disabled and not shown raising an error, because there is nothing to call.
  - Given US-1501 has landed, when the download control is used from either the composer or the conversation header, then one shared menu opens offering Markdown (.md), Word (.docx), and PDF, each with its own file glyph (frame `2f`).
  - Given a format is chosen, when the request is in flight, then that item alone shows a `spin` ring and the label "Preparing <format>…" while the other two dim, the menu **stays open until the browser download starts**, and the footer note reads "The stopped, unsaved answer on screen won't be included." (frame `2f`).
  - Given the export fails, when the error returns, then the menu returns to idle and an error toast naming the `traceId` is raised — the menu does not close on failure.
  - Given a turn is streaming, when the composer renders, then the download control is disabled with the tooltip "Available when the response finishes"; given an empty conversation, when it renders, then the control is absent entirely (frames `2g`, `2f`).

## 8. Milestones & rollout

**Phases**, derived from the epic dependency graph.

| Phase                             | Contents                                                                                                                                                                                                                                                                     | Relative estimate |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| **P1 — Foundation**               | EP-1 in full (US-101 … US-109). US-109 ships the fonts, brand assets, and icon sprite the shared kit composes, and US-106 is an L rather than an M once the design's full component inventory is counted                                                                     | ~2.5 weeks        |
| **P2 — Signed-in shell**          | EP-2 in full, EP-3 US-301 … US-304, US-306, and US-308                                                                                                                                                                                                                       | ~1 week           |
| **P3 — MVP chat**                 | EP-4 P0 stories (US-401 … US-407), EP-5 US-501, EP-6 US-601, US-602, US-606. **This is the minimum viable replacement** — the point at which the rebuilt client can do what the old one could, plus the timeline the old one never could                                     | ~2.5 weeks        |
| **P4 — Chat completeness**        | EP-4 remainder (US-408 … US-410, US-412, US-413), EP-5 remainder, EP-6 remainder, EP-3 US-305                                                                                                                                                                                | ~1.5 weeks        |
| **P5 — Library, files, projects** | EP-7, EP-8, EP-9 excluding US-909 and US-910, plus EP-3 US-307. US-307 sits in EP-3 by surface but depends on US-901, so it schedules here rather than with the rest of its epic. Backend starts US-907 (B1) in parallel with P3 so US-908 lands without its interim ceiling | ~2.5 weeks        |
| **P6 — Administration**           | EP-12 excluding US-1205 and US-1206                                                                                                                                                                                                                                          | ~1.5 weeks        |
| **P7 — Conformance**              | EP-14 in full. Gates run against everything shipped in P1–P6                                                                                                                                                                                                                 | ~1 week           |
| **P8 — Enabled features**         | EP-10, EP-11, EP-13, EP-15, plus US-706, US-902, US-909, US-910, US-1205, US-1206, and the MCP picker batch US-414 … US-418 with US-1210. Each pairs an enabler with the frontend story it releases; the enablers can start any time from P1 onward and the frontend halves land as they complete                           | ~3 weeks          |

The backend enablers form an independent track: US-907 (B1), US-1001 (B2), US-909 (B3), US-1101 (B4), US-1102 (B5), US-1301 (B6), US-1004 (B7), US-706 (B8), US-411 (B9 — **withdrawn 2026-08-22**, superseded by US-414, which deletes the problem type rather than widening it), US-1205 (B10), and US-1501 (B11). US-1102 and US-1004 depend on another enabler; the rest can begin immediately. **US-1101 shipped whole on 2026-08-21**: it was blocked, not on another enabler but on an upstream `Andes.Extensions.AI.UI` 0.8.0 release (issue #14), and that release published to nuget.org the day before, 2026-08-20 — closing the blocker rather than routing around it, so both halves (`usage` projection and `assistantMessageId`) landed together instead of the `usage` half shipping alone as once planned. Scheduling any of them before P8 removes an interim behavior rather than adding one.

**Risks & mitigations.**

| Risk                                                                                                                                                             | Mitigation                                                                                                                                                                                                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The old client is already broken by the framed SSE release, so there is no working frontend during the rebuild                                                   | Treat P3 as the deadline that matters. Nothing before it restores a usable client, and no scope should be added ahead of it                                                                                                                                                                                                     |
| The reference store pattern set in `ConversationListStore` (US-302) is copied by 15 other stores; a wrong pattern is 16 rewrites                                 | Review US-302 as a pattern decision before any second list store is written. The same applies to US-104, which every store composes                                                                                                                                                                                             |
| Streaming performance characteristics are fixed by US-405 and US-602; getting the coalescing or the head/tail split wrong is a rewrite of the chat surface       | Both have unit-level criteria that run without a backend. Write `timeline.ts` and the codec against fixtures before the transport exists                                                                                                                                                                                        |
| `Andes.Extensions.AI.UI` is a package type, not ours — a major upgrade is an API change for every client                                                         | US-107's CI diff check fails the build on drift, so an upgrade surfaces as a red build rather than as a runtime mismatch                                                                                                                                                                                                        |
| MSAL is a two-major jump and the old configuration cannot be ported                                                                                              | ~~US-201 is written against the v6 API from its documentation~~ **Realised and resolved in US-201 (2026-08-10)**: writing against the installed packages rather than from memory is what surfaced three `@azure/msal-angular` defects that make it unusable here, so the client sits directly on `@azure/msal-browser` — see §6 |
| A cancelled turn is billed but transcribed nowhere; appending the partial text to the transcript would make the visible history diverge from the model's context | US-407 makes the detached, explicitly-unsaved card an acceptance criterion rather than a styling choice                                                                                                                                                                                                                         |
| Interim behaviors for the eleven gaps could quietly become permanent                                                                                             | Every interim behavior is an acceptance criterion on a story that also states the post-enabler behavior, so the interim state is visible in the backlog rather than buried in code                                                                                                                                              |
| Client-side sort over a drained set silently misleads at the 500-item ceiling                                                                                    | US-902 and US-908 both require the ceiling notice as an acceptance criterion; US-705 forbids offering a sort the server cannot honor                                                                                                                                                                                            |
| The design bundle and this backlog drift apart again, as they did when `docs/ui/mockups/` was deleted                                                            | Acceptance criteria cite design frames by id (`1b`, `4l`, `5j`, …) rather than paraphrasing them, so a board that changes invalidates a named criterion instead of quietly contradicting prose. `docs/design/` is the single visual authority and this document is authoritative over it only on accessibility                  |

**Rollout & rollback.** There is no incremental rollout path and no rollback to the previous client: the old `enterprise-ui/` is deleted, and it cannot read the current stream regardless. The back-out for any individual capability is the feature flag in `config.json` — the diagram and math chunks are flagged, and the raw-text stream codec stays behind a flag as a fallback for a deployment still running the pre-framing server. Deployment is one artifact promoted across environments with `config.json` overwritten per environment, so a bad configuration is corrected by replacing one file rather than rebuilding. Each phase is independently deployable; P3 is the first phase worth deploying to users.

## 9. Assumptions & open questions

**Assumptions.** Each is a guess a reviewer can veto.

- The eleven backend gaps are in scope for this document as `[enabler]` stories placed inside the feature epic each one unblocks, per the answer given during discovery. Backend engineers are a persona and the phase graph schedules enablers ahead of the stories they release.
- `docs/design/` is treated as the visual authority for every screen, superseding the deleted `docs/ui/mockups/`. Where a board and this document disagree on accessibility, this document wins; on everything else the board wins. The boards' CDN dependencies are assumed to be prototype scaffolding rather than design intent, which is what FR-51 encodes.
- US-106's estimate is raised from M to L in this amendment, because counting the design's actual component inventory roughly doubles the shared kit. This is a sizing judgement, not a measured figure, and it is the only estimate changed here.
- Conversation export (EP-15) is assumed to cover prompts and completed answers only. Activity cards, reasoning text, and the stopped-unsaved answer are excluded, matching what the transcript persists — if exports are expected to carry the assistant's working, US-1501's contract changes.
- The pre-US-1501 behaviour for the export control is assumed to be **absence** rather than an unavailable panel, because the control lives inside the composer where a panel has nowhere to render. Every other capability gap in this document uses `UnavailablePanel`; this is the one deliberate exception.
- Concrete numeric targets not supplied in the request, proposed here for veto: the 500-item drain ceiling and `take=100` page size for regime A; the 200ms Stop responsiveness in US-407; the 2-second copy confirmation; the 4.5:1 and 3:1 contrast ratios (WCAG 2.1 AA). The initial-bundle budget number itself is `TBD` — US-108 requires that a budget exists and fails the build, but the byte figure must be set from the first real build of the scaffold rather than guessed now.
- Phase durations in section 8 are relative sizing derived from the story estimates, not a commitment, and assume no team size — none was specified and none is invented here.
- "Voice input" is assumed to mean browser-native speech recognition with graceful absence where unsupported (US-413), not a server-side transcription service. A server-side transcription requirement would make this a new backend capability and a new enabler.
- The Documents screen is assumed to show only the signed-in user's own documents, consistent with every other resource being owner-scoped and there being no administrative view of another user's data.
- Thumbs up/down is assumed to be a simple binary rating with no free-text comment. A comment field would change US-1102's contract.
- Project favorites (US-909) are assumed to be per-user, mirroring conversation favorites. If projects are ever shared, this becomes a different design.
- The interim device-local project pins (US-910) are assumed acceptable as a stopgap despite not syncing across devices; the alternative is to hide the sidebar project tree entirely until US-909 lands.
- Admin "Reports" is assumed to mean usage and cost reporting over the existing audit trail, since that is the only reporting data the platform persists. If it means something else — adoption, conversation volume, model quality — US-1301's contract is wrong.
- `docs/conversations/streaming-contract.md` is treated as the authority on wire behavior over the planning document.
- `--focus-ring` is an app addition and is deliberately **not** carried back into `theme.css`: `--accent` `#21A8D8` measures only 2.74:1 against `--surface` and fails SC 1.4.11 as a focus indicator on every light surface, so the app records a separate token rather than reusing the accent.
- No telemetry story appears in this document because the client emits no analytics events today and none was requested. The success criteria in section 1 are all measured by tests and build gates rather than by runtime telemetry, which is why the absence does not leave them unmeasurable.

**Open questions.**

- ~~**How should the Documents screen fetch across conversations?**~~ **Answered by US-1001 (2026-08-19, product owner + backend engineer): both routes ship, and the screen never fans out.** The per-conversation `GET /api/conversations/{id}/documents` exists as the B2 contract requires; beside it a cross-conversation `GET /api/documents` scoped to the caller serves the library — paginated with `PaginatedResponseDto` (skip/take, take clamped 1..100), ordered `DateCreated` descending with `Id` as the tie-break so page boundaries cannot duplicate rows, each item carrying `conversationId` and `conversationName` so no client-side name lookup is needed. The client drains pages at `take=100` to `DocumentLibraryStore`'s 500-item ceiling; the response envelope's `totalCount` is what keeps the screen's document count honest when the drain stops short. A `(UserId, DateDeactivated)` index went with it. This closes US-1001's fourth acceptance criterion.
- ~~**What is the initial-bundle budget in bytes?**~~ **Answered at the end of US-101 (2026-08-09); re-stated at the end of EP-1 (2026-08-10), at the end of EP-2 (2026-08-10), after EP-3's US-301–US-303 (2026-08-11), and most recently after EP-6's US-601/602/606 (2026-08-13), where it measures **660.24 kB raw / 161.01 kB estimated transfer** against **665 kB warn / 720 kB error** — the warn line raised by a further 5 kB, the error ceiling still unchanged, and the `styles` budget still 65 / 80 kB against a stylesheet measuring 62.08 kB. The shell is deliberately a **lazy** route rather than an eager component: its sidebar, tooltip, search field and ridgeline are ~24 kB that `/auth`, `/login-failed`, `/session-error` and `/signed-out` would otherwise all download without rendering, and `sessionGuard` has already awaited a round trip by the time the chunk is asked for. Chat is lazy too, and that is what let US-601 put the transcript renderer behind that route rather than in the initial graph: the `chat` chunk is now 179.47 kB, 124.8 kB of it the markdown stack, and none of that is initial.** With EP-5 closed and US-603 landed it measures **661.36 kB raw / 161.19 kB estimated transfer**, still against 665 / 720 kB, with `styles` at 63.19 kB and the `chat` chunk at 198.51 kB — the ~1 kB of initial growth is global CSS for the code-block chrome, since every component and reducer those five stories added rides the lazy route. (At the end of EP-3 the figure was 643.62 kB / 158.05 kB against 660 / 720 kB; it had drifted to 648.75 kB / 157.96 kB by the end of EP-4/EP-5.) The first production build of the scaffold measured **220.34 kB raw / 59.64 kB estimated transfer** against a 240 kB warn / 300 kB error budget, with the router, `HttpClient`, and the US-102/US-103 foundation in but no feature code. With EP-1 complete the same build measured **349.83 kB raw / 88.13 kB estimated transfer** against **360 kB warn / 400 kB error**; Bootstrap's SCSS subset and `app.routes.ts` gaining a real route (so the router is no longer tree-shaken away) drove that jump. With EP-2 complete it measures **629.33 kB raw / 151.96 kB estimated transfer**, and `angular.json` now carries **650 kB warn / 720 kB error** on initial raw, with the `styles` bundle budget unchanged at **65 kB warn / 80 kB error** against a global stylesheet measuring 60.32 kB. **The ~150 kB MSAL estimate above was wrong: MSAL is 248.9 kB of the initial graph** — `msal-browser` 148.7 kB plus `msal-common` 100.2 kB, which the estimate missed — and none of it is deferrable, because `main.ts` must create and `initialize()` the instance before `bootstrapApplication`. Re-baseline and re-state this figure whenever a story adds one. **Re-stated at the end of P7 (US-1405, 2026-08-18), which is the story that owns budgets: it measures **670.33 kB raw** against a warning line moved 670 → **675 kB** and an unchanged **720 kB** ceiling, with `styles` at **64.47 kB** against 65 → **66 kB** warn and an unchanged 80 kB. The warning lines moved because the previous baseline left 0.07 kB of headroom, which is not headroom — it is a build that fails on the next comment. `check-initial-chunk.mjs` also gained an `axe-core` entry, since a spec-only 600 kB dependency is invisible to the budgets and an accidental import would ship an accessibility auditor to every user.**
- ~~**Should the client request the four cheap stream-protocol additions?**~~ **`assistantMessageId` is answered by US-1101 (2026-08-21): shipped, on the metadata bag rather than a fifth stream addition.** `Andes.Extensions.AI.UI` 0.8.0 closed upstream issue #14 by adding an optional `metadata: Record<string, string>` to `AssistantUiEvent` and `AssistantStatusSnapshot`, and the server writes exactly one key onto it — `assistantMessageId`, on a completed turn's `Finished` frame. The remaining three are still open: a leading `event: hello` frame carrying `{protocol, version}` for deterministic codec selection; a terminal failure frame, since "no `Finished`" is still the only truncation signal; `: keep-alive` comments every 15s so intermediaries do not kill long tool calls. The metadata bag is now proven infrastructure rather than a hope, though, and is the natural place for `conversationName` on `Finished` too, if the product wants that addition — _product owner and backend engineer_.
- **Is the 500-item ceiling the right number for regime A?** It trades the number of drain requests against how often a user hits a disabled sort. Should be revisited against real project and conversation counts — _product owner, after P5_.
- ~~**Does adding columns for B3 and B7 require a migration of their own?**~~ **B3 is answered by US-909 (2026-08-20): yes, and it took the seventh** — `20260820050001_AddProjectIsFavorite`, an ordinary `AddColumn` migration, no different in kind from the six before it. B4 was answered earlier and needed none: the usage row's `AssistantMessageId` has existed since `InitialCreate`. **B7 (US-1004) is still open** — _backend engineer, before US-1004 starts_.
- **Should reports be exportable?** US-1302 specifies viewing only, and frame `5j` deliberately carries no export button. CSV or Excel export is a plausible immediate follow-up and would extend US-1301's contract — _product owner_.
- ~~**Which server-side stack renders `.docx` and `.pdf` for US-1501?**~~ **Answered by US-1501 (2026-08-19, backend engineer): PDFsharp/MigraDoc 6.2.4, in process, no headless browser and no hosted service.** MIT-licensed with no revenue threshold, unlike QuestPDF's Community tier, which is source-available and gated on the *company's* revenue — the same reasoning that already keeps FluentAssertions v8 out of this repository. `.docx` is written directly against the `DocumentFormat.OpenXml` SDK and carries no font dependency at all, since a Word document names no glyphs — the reader's own theme supplies them. `.pdf` embeds glyphs and is the one format that can genuinely be unavailable: `ExportFontResolver` decides at startup whether a usable face exists, and a deployment with none serves a typed `503 export-renderer-not-configured` rather than a broken file or a 500. The route returns the response **as a body**, not a signed URL — the PRD's either/or is resolved in the body's favour, because a signed URL would put every export, markdown included, behind a storage dependency it does not otherwise need. See [docs/conversations/conversation-export.md](../conversations/conversation-export.md).
- ~~**Does US-1301's contract cover everything frames `5j` and `5k` render?**~~ **Answered by US-1301 (2026-08-21, backend engineer): yes.** Every dimension the dashboard needed — grouping by model, provider, user and day, the completed/cancelled/failed split, per-model token and estimated-cost figures, and prior-period deltas for all four KPI tiles — shipped in the response `UsageReportDto` describes, and US-1302 built frames `5j`/`5k` directly from it with no contract gap to route around. See [Administration §12](../ui/administration.md#12-the-reports-dashboard-us-1302).
- ~~**Does `--warn` need to change in the design bundle?**~~ **Answered by US-1405 on 2026-08-18: yes, and it changed.** Light `--warn` moved `#A96A10` → **`#9C6210`** in `docs/design/project/theme.css`, mirrored into `_tokens.scss`, and three new `CONTRAST_PAIRS` rows now measure it on `--surface`, `--bs-body-bg` and `--warn-bg` in both themes — at the 4.5:1 text minimum rather than the 3:1 non-text one, because the same token serves icons and running text and the stricter reading is the honest one. The dark half of that third pair needed a new `under:` option in `check-tokens.mjs`, since `--warn-bg` is an `rgba` there and had been reported as unmeasurable. **A second pair went with it, found by the axe run rather than by this table**: dark `--active-bg` `#173A58` → **`#16354D`**, where `--brand` measured **4.29:1** on it — the administration rail's active tab, the sidebar's active navigation link and the paginator's current page. It had been invisible for three stories because the surfaces guard measures `--focus-ring` against every painted background, not the text drawn on it, and it surfaced only once the browser viewport was pinned to a desktop width. Both are the **first changes this project has made to the design bundle**, under this section's own precedence rule, and both had to go there: `check:tokens` enforces parity with `theme.css`, so a local override fails by construction. See [Accessibility audit §6](../ui/accessibility-audit.md#6-the-two-failures-the-run-found) and [Design System §9](../ui/design-system.md#9-corrections-made-against-the-design-boards).
- ~~**Can the UI CI checks be marked required while the workflow keeps its `paths` filters?**~~ **Answered on 2026-08-16 by dropping the filters from both workflows.** The filter moved into a `changes` job whose outputs each gate conditions on, because GitHub's two "skipped" behaviours differ: a workflow skipped by a **path filter** never reports and stays Pending, while a job skipped by a **conditional** reports Success and is safe to require. The conditions are written **fail-open** (`!cancelled() && needs.changes.outputs.* != 'false'`), since a job whose `needs` failed is skipped — and `== 'true'` would therefore hand out a green required check over untested code the moment the filter job broke. The same shape now covers the new `.github/workflows/api-ci.yml` (`Detect API changes` → `Unit tests`, `Integration tests`), the repository's first .NET CI. **One repository setting still has to be got right**: branch protection must list **all three** check names per workflow — `Detect API changes` + `Unit tests` + `Integration tests`, and `Detect UI changes` + `Lint, test, build` + `Andes contract drift` — because requiring only the gates leaves a window where a `changes` job that dies after writing `false` lets its dependents skip to Success. See [docs/ci/pull-request-checks.md](../ci/pull-request-checks.md) — _whoever configures branch protection_.
- ~~**Does the initial bundle still fit under budget after Mermaid and KaTeX?**~~ **Answered at the end of EP-6 (2026-08-14): neither library is in the initial graph, and neither is in the chat chunk either.** Both are reached only through `await import(…)` from `LazyRendererLoader`, behind flags that are a per-environment switch, and `scripts/check-initial-chunk.mjs` now enforces both halves of that — a second traversal rooted at the chat chunk, because a static import there would be off the initial graph and still downloaded by every reader who opens a conversation, and a positive presence assertion, because every other check in the file passes when a library is simply absent. With US-701's screen added the initial bundle measures **663.30 kB raw / 163.25 kB estimated transfer**, the warning line re-stated 665 → **670 kB** and the error ceiling still **720 kB**; `styles` is 63.80 kB against 65 / 80 kB and the `chat` chunk is 177.02 kB. Mermaid contributes about 1.5 MB across a dozen chunks of its own, since its core build loads each diagram type on demand, and KaTeX about 280 kB plus a 24 kB stylesheet and 20 woff2 faces served from `public/vendor/katex` — none of it fetched until matching content appears with the flag on.
- ~~**Does the initial bundle still fit under budget after `ngx-markdown`?**~~ **Answered at the start of US-601 (2026-08-13): the transcript renderer moved behind the lazy chat route, so the initial bundle never carries it.** `provideChatMarkdown()` is provided at the `Chat` component, and `scripts/check-initial-chunk.mjs` gained a FORBIDDEN entry for `ngx-markdown|marked|prismjs|dompurify`, so the decision is enforced by the build rather than by convention — a static import from eagerly reachable code fails it, naming the library and the chunk. The whole **124.8 kB** stack (marked 42.5, Prism 40.8, DOMPurify 29.0, `ngx-markdown` 12.5) is in the `chat` chunk, which grew 48.60 → 179.47 kB. The initial graph still grew **~11.5 kB**, to 660.24 kB, but not from those libraries: `MarkdownService` injects Angular's `DomSanitizer`, whose implementation lands in a chunk shared between initial and lazy code, plus ~1.6 kB of global CSS — unavoidable short of dropping `ngx-markdown`, and the reason the warn line moved 660 → 665 kB while the 720 kB ceiling held.
