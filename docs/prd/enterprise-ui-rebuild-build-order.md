# Build order: Enterprise GPT frontend rebuild

A dependency-resolved execution sequence for the 94 stories in [`enterprise-ui-rebuild.md`](enterprise-ui-rebuild.md), for tracking progress against.

**The PRD is authoritative.** This file adds no requirements and changes no scope. It derives one linear order from §8's phase table plus each story's `Depends on` field, because §7 is grouped by surface and its numbering is not a valid build order — four stories depend on a later-numbered one (US-105→US-109, US-106→US-109, US-307→US-901, US-401→US-405). Where this file and the PRD disagree, the PRD wins and this file is wrong.

Status here mirrors the `Status` field on each PRD story. Update both, or update the PRD and re-derive this.

**Progress: 88 / 94 done.** Current position: **P8 is under way — US-706, US-902 and US-1205 landed on 2026-08-20, right after US-909 and US-910 landed together earlier the same day (rows 87–88), and US-1101, US-1102, US-1103 and US-1206 landed the day after, on 2026-08-21, closing EP-11 entirely**. Rows 89–92 are the sort enabler, the two screens it releases, and the admin permission filter's enabler-and-screen pair — the same shape US-909/US-910 set two rows above them (land the enabler and the story that releases it, so no interim behaviour reaches a build), though this pair split across two days rather than landing together: US-1205 on 2026-08-20, US-1206 the day after. `sort=`/`dir=` now ride `GET api/conversations/search`, `GET api/projects` and `GET api/users` alike, refusing an unrecognised value as a validation-error 400 naming the parameter and preserving each listing's prior order byte-for-byte when neither is supplied — an omission every client written before this release depends on. Review caught a real defect in the tie-break every requested order now gets: it follows the *requested* direction rather than a fixed one, because a fixed tie-break made a reversed sort collapse onto the same rows wherever many entries share one name (every untitled conversation, most visibly). The conversations library traded US-705's stated "Newest first" caption for a working select (Newest first, Oldest first, Alphabetical), and the projects grid traded its ceiling-disabled forecast for one too (Last updated, Recently created, Alphabetical, Favourites first) — with the grid's own default order now **Last updated** rather than newest-created, matching what its cards already say, and its 500-project ceiling notice naming the order it truncated rather than claiming a recency that order may not hold. `ProjectLookupStore`, behind the picker and the sidebar's favourite-projects section, now drains `sort=favorite&dir=desc`, closing the interim-behaviours row that had booked that section's completeness to this pair. `GET api/users` also gained `?permissionId=` (US-1205), and the admin users tab wired it the next day (US-1206, 2026-08-21): `?permission=` in the URL, the whole directory narrowed rather than the loaded page, and the board's interim "Filters loaded rows only." caption never rendered because its enabler was already done — an id the permission catalog cannot vouch for reads as no filter and is repaired out of the URL, and a catalog that itself fails to load leaves the raw id on the wire with the server as the last authority. The sort select still has not reached the admin users tab, though — that remains a screen-level gap rather than an API one (a follow-up). US-1101 (B4) shipped the next day, on its own track: `Andes.Extensions.AI.UI` 0.8.0 published to nuget.org on 2026-08-20 and closed the upstream blocker (issue #14) by adding an optional `metadata` bag to `AssistantUiEvent`; the story took the release up on 2026-08-21, so both halves — the `usage` projection and the `Finished` frame's `assistantMessageId` — landed together rather than the `usage` half alone as once planned; the frame is now held back until the transcript write it names has answered. **US-1102 and US-1103 shipped the same day, closing EP-11 entirely.** `POST api/conversations/{id}/messages/{messageId}/feedback` patches the transcript message document first — the same one `GET .../messages` reads back — and writes a new relational projection, `Core.MessageFeedback` (one migration, `20260821203334_AddMessageFeedback`), second and best-effort, logging rather than failing the request if that write fails; two widenings went beyond the written criteria, both recorded on the stories themselves: `{"rating": null}` withdraws a rating, and a withdrawal of one never recorded touches neither store. The client puts frame `1g`'s thumbs beside Copy in the settled-turn footer, absent by construction wherever the turn never became a persisted message, surviving a reload because `replaceHistory` rebuilds the whole replayed block from the response rather than reconciling a rating separately. After this batch, backend totals **1380 unit and 319 integration tests** (up from 1367/305) and frontend totals **1931 tests across 146 files and 41 browser a11y audits** (up from 1904/39); the initial bundle measures **671.99 kB raw / 169.10 kB transfer** against 675/720 kB, `styles` and the lazy `admin-routes` chunk unchanged — no re-baseline needed. Before them, **US-1001, US-1002, US-1003, US-1501 and US-1502 all landed on 2026-08-19**, opening EP-10 and closing EP-15. The backend now lists a conversation's documents and, beside it, everything the caller owns: `GET api/documents` is paginated (`take` clamped to 100, `DateCreated` desc with an `Id` tie-break so a page boundary cannot duplicate rows) and each row carries its conversation's id **and name**, which is the §9 fan-out question answered by adding the route rather than accepting the N+1. `/documents` is a real screen: a route-provided `DocumentLibraryStore` drains 100-a-page to a 500-item ceiling with the envelope's `totalCount` keeping the count honest, rows are the `ProjectFiles` anatomy rather than `DataTable` (no header row in frame `4j`, and grouping would not fit), the source badge reads `Uploaded` on every row until US-1004 exists, downloads are US-804's click-only signed links, the name filter is client-side with the term in the URL, and the grouped view — the default, as the board draws it — collapses under `--surface-2` headers that link into the chat. The review rounds closed a real backend defect the new listing would have made visible (a document whose ingestion outlives its conversation's deletion is now deactivated to match by a post-persist re-check in both ingest paths) and a frontend one (a mid-drain failure now clears its dead pages, so Retry shows the skeleton rather than a half-drained set). **EP-15 closed the same day.** Conversation export grew from two formats to five behind one registered-renderer-per-format registry rather than a branch in the service: `html`, `json` and `md` all read what the transcript already computed or preserved, while `.docx` and `.pdf` re-render, walking each message's markdown once through the same hardened pipeline that produces `htmlContent` into a shared, renderer-neutral block model, so the two document formats cannot disagree about what a message meant. PDFsharp/MigraDoc 6.2.4 (MIT, unlike QuestPDF's revenue-gated Community tier) renders both; PDF alone depends on a font resolved once at startup, so a deployment with none serves a typed `503 export-renderer-not-configured` naming the format rather than a 500 or a broken file, closing the long-open renderer-stack question. The response is bytes in the body, not a signed URL, settling that either/or in the body's favour — a signed URL would put every export, markdown included, behind a storage dependency it does not need. Images are dropped from `.docx`/`.pdf` and their alt text kept, because embedding one means the API fetching a URL model output chose. The client got one `ConversationDownloadMenu` behind two triggers — the composer and the conversation header — reading a store shaped after `DocumentDownloadStore` with its transport reversed: a `blob` fetched directly, retried never (an unclassifiable error body would otherwise let a typed 503 be retried as a gateway hiccup), and its object URL released ten seconds after the download starts rather than immediately, which would cancel it. Backend: **1274 unit and 284 integration tests** (up from 1152/250), with `OpenXmlValidator` against the generated `.docx` catching four schema-ordering bugs a round trip had missed. Frontend: **1830 tests** (up from 1788) and **38 browser audits** (up from 36); the initial bundle grows **0.91 kB to 671.59 kB raw / 168.92 kB transfer**, inside 675/720 kB. See [Conversation Export](../conversations/conversation-export.md) and [Conversation Download](../ui/conversation-download.md). Before this, **P7 closed on 2026-08-18 — US-1403, US-1404 and US-1405**, the conformance phase. The app now has three viewport modes rather than two widths (a tablet overlay that leaves the content where it was, a phone drawer behind a 54px navbar, and the chat header's controls moving into that navbar rather than vanishing with it), a reduced-motion contract that was already correct and is now gated rather than asserted in prose, and an **axe-core run in a real browser** over all seven routes in both themes that fails the build on anything serious or critical. That run found the phase's only genuine defect nobody had noticed: dark `--active-bg` at **4.29:1** under `--brand`, on the admin rail's active tab, the sidebar's active nav link and the paginator's current page. Both it and the long-open `--warn` question were fixed **upstream in `docs/design/project/theme.css`** — the first changes this project has made to the design bundle. Three build gates are new (a hardcoded application breakpoint, a fifth keyframe, and keyframe plus breakpoint parity), `CONTRAST_PAIRS` is at 46, and the budgets are re-baselined at **670.33 kB raw against 675 / 720 kB** with `styles` at 64.47 kB against 66 / 80 kB. P7 also retired US-308's last deferred criterion and fixed two pre-existing defects it found on the way: a projects grid written against Bootstrap classes this app does not ship, and four routed screens clipped below the fold at every width. The administration area behind all of this has a layout route, a rail that marks the open tab from the router, a working user directory (server-paged search, a create modal, a permission-editing offcanvas and a deactivate confirmation), a model catalog (the whole set in one request, filtered in memory, an eleven-field create-and-edit dialog, Set as default, and a retire confirmation whose event also refreshes the chat picker's catalogue), an MCP server registry (the same regime again, a five-field dialog whose Scope is conditional in both directions, and a deactivate confirmation that names the permission cascade) and a **reports tab that is a URL and frame `5i`'s unavailable panel**, because the API behind it does not exist — US-1302 fills it in, in P8, and inherits the route rather than creating one. **Within P8 the remaining enablers stay a parallel track** — none of those still open waits on another enabler now that US-1102 has landed, and US-1004 has its dependency (US-1001) in hand. Ten things are outstanding against shipped stories: a backend enabler for the Last active column US-1201 could not build, a backend enabler for restoring a retired model, the same for reactivating a deactivated MCP server, US-1402's two — a screen-reader pass against real assistive technology, and the post-`Finished` delta the fold does not refuse — US-1401's three: the `outline` scan reading `src/` rather than the built stylesheet, Bootstrap's transient active-state wash, and two tab stops per code block — and US-1403/1405's two: the vacuous reports-charts criterion and the project-detail pill departure.

Within a phase, stories on the same table row have no ordering constraint between them and can be taken in any order or in parallel. `→` in the notes column means "must follow".

---

## P1 — Foundation · ✅ complete

| Order | Story                                                                     | Depends on             | Pri | Status        |
| ----- | ------------------------------------------------------------------------- | ---------------------- | --- | ------------- |
| 1     | US-101 `[enabler]` Scaffold the zoneless Angular 21 workspace             | —                      | P0  | ✅ 2026-08-09 |
| 2     | US-102 `[enabler]` Load configuration at runtime before bootstrap         | US-101                 | P0  | ✅ 2026-08-09 |
| 3     | US-103 `[enabler]` Type and normalize every API error                     | US-101                 | P0  | ✅ 2026-08-09 |
| 4     | US-104 `[enabler]` Build the reusable signal-store features               | US-101                 | P0  | ✅ 2026-08-09 |
| 5     | US-107 `[enabler]` Vendor the Andes streaming contract with a drift check | US-101                 | P0  | ✅ 2026-08-10 |
| 6     | US-108 `[enabler]` Enforce lint rules and bundle budgets in the build     | US-101                 | P0  | ✅ 2026-08-10 |
| 7     | US-109 `[enabler]` Ship the brand assets, type scale, and icon set        | US-101                 | P0  | ✅ 2026-08-10 |
| 8     | US-105 Switch between light and dark without a flash                      | US-101, US-109         | P0  | ✅ 2026-08-10 |
| 9     | US-106 `[enabler]` Build the shared UI kit                                | US-101, US-105, US-109 | P0  | ✅ 2026-08-10 |

US-109 sits ahead of US-105 and US-106 despite its number: it ships the assets both of them consume.

## P2 — Signed-in shell · ✅ complete

| Order | Story                                                                             | Depends on     | Pri | Status                                                              |
| ----- | --------------------------------------------------------------------------------- | -------------- | --- | ------------------------------------------------------------------- |
| 10    | US-201 Sign in with Entra ID                                                      | US-102         | P0  | ✅ 2026-08-11                                                       |
| 11    | US-202 Bootstrap the session from the current user                                | US-201, US-104 | P0  | ✅ 2026-08-11                                                       |
| 12    | US-203 Keep the admin area off non-admin devices                                  | US-202         | P0  | ✅ 2026-08-11                                                       |
| 13    | US-204 Gate upload affordances on the Upload File permission                      | US-202         | P1  | ✅ 2026-08-11 — primitives only; frame `2h` re-asserted by US-801, 2026-08-14 |
| 14    | US-205 Sign out and leave nothing behind                                          | US-202, US-104 | P0  | ✅ 2026-08-11                                                       |
| 15    | US-302 See my recent conversations                                                | US-202, US-104 | P0  | ✅ 2026-08-11 — with the shell that hosts the sidebar               |
| —     | ⛔ **Pattern gate** — review `ConversationListStore` before any second list store |                |     | ✅ reviewed 2026-08-11                                              |
| 16    | US-301 Collapse the sidebar and have it stay collapsed                            | US-106         | P0  | ✅ 2026-08-11                                                       |
| 17    | US-303 Start a new conversation without creating one                              | US-302         | P0  | ✅ 2026-08-11 — prompt chips deferred to US-401                     |
| 18    | US-304 Rename a conversation without unlinking it                                 | US-302, US-106 | P0  | ✅ 2026-08-11 — first Signal Form; row kebab ships Rename-only      |
| 19    | US-306 Delete a conversation                                                      | US-302, US-106 | P0  | ✅ 2026-08-11 — optimistic removal, navigation on the 204           |
| 20    | US-308 Act on the open conversation from its header                               | US-302         | P1  | ✅ 2026-08-11 — ⚠ shipped per the note below                        |

US-302 leads the phase on two counts: US-303, US-304, US-306, and US-308 all hang off it, and the PRD's risk table requires it be reviewed as a _pattern decision_ before the 15 stores that copy it exist. US-301 needs only US-106, so it can run alongside.

⚠ **US-308 was scheduled ahead of its own criteria, and shipped that way.** §8 places it in P2, but its header star routed through US-305 (P4) and two of its four kebab items through US-307 (P5); the 52px header bar shipped with those controls absent, per this note. Its sub-768px mobile-navbar criterion was additionally deferred to US-1403 on 2026-08-11 — frame `1d`'s hamburger opens frame `3e`'s sidebar overlay, which does not exist until the responsive shell does. The star was retired on 2026-08-13 when US-305 was pulled forward, the two project items on 2026-08-15 when US-307 landed, and the mobile-navbar collapse on 2026-08-18 when US-1403 landed — so **nothing deferred out of US-308 is outstanding any more**, and the controls move *into* frame `1d`'s navbar rather than disappearing with the bar that used to hold them.

## P3 — MVP chat · ✅ complete — the minimum viable replacement, and the first phase worth deploying

| Order | Story                                                                                | Depends on     | Pri | Status                                                                                                               |
| ----- | ------------------------------------------------------------------------------------ | -------------- | --- | -------------------------------------------------------------------------------------------------------------------- |
| 21    | US-404 `[enabler]` Decode the SSE frame stream                                       | US-107         | P0  | ✅ 2026-08-12                                                                                                        |
| 22    | US-405 `[enabler]` Stream over an abortable fetch that reads errors first            | US-103, US-404 | P0  | ✅ 2026-08-12                                                                                                        |
| 23    | US-402 Choose the model for a turn                                                   | US-202         | P0  | ✅ 2026-08-12 — send ships inert until US-401                                                                        |
| 24    | US-403 Select MCP servers, and be stopped from selecting them where they cannot work | US-402         | P0  | ✅ 2026-08-12 — ⚠ two deviations, see below                                                                          |
| 25    | US-401 Send a first prompt and have the conversation created around it               | US-303, US-405 | P0  | ✅ 2026-08-12 — ⚠ URL via `router.navigateByUrl` + `replaceUrl`, a documented deviation; chips from US-303 ship here |
| 26    | US-406 Watch the answer arrive                                                       | US-405, US-104 | P0  | ✅ 2026-08-12 — also retires US-403's tool-server-unavailable panel                                                  |
| 27    | US-407 Stop a turn and keep what it produced                                         | US-406         | P0  | ✅ 2026-08-12                                                                                                        |
| 28    | US-501 See what the assistant is doing, in the order it happened                     | US-404, US-406 | P0  | ✅ 2026-08-12 — flat cards; US-502 nests them                                                                        |
| 29    | US-601 Render model output as markdown that cannot execute                           | US-108         | P0  | ✅ 2026-08-13 — gate resolved, see below                                                                             |
| 30    | US-602 Read a streaming answer without the page slowing down                         | US-601, US-406 | P0  | ✅ 2026-08-13 — split takes a third boundary the criteria omit                                                       |
| 31    | US-606 Stay with the latest message, or stay where I am reading                      | US-406         | P0  | ✅ 2026-08-13 — following driven by a `ResizeObserver`, not the turn's signals                                       |

Transport first, then the composer controls, then send, then rendering. US-402 precedes US-401 by practical necessity rather than by its `Depends on` field — a turn needs a `modelId`.

✅ **Gate at US-601, resolved 2026-08-13 — the renderer moved rather than the budget.** `provideChatMarkdown()` is provided at the `Chat` component, so the whole 124.8 kB markdown stack rides the lazy chat chunk (48.60 → 179.47 kB) and none of it is in the initial graph; `check-initial-chunk.mjs` now fails the build on any static path from `main.ts` to `ngx-markdown`, `marked`, `prismjs` or `dompurify`. The baseline was re-measured anyway: **660.24 kB raw / 161.01 kB transfer** initial, with the warn line re-stated 660 → **665 kB** and the ceiling held at 720 kB. The ~11.5 kB of initial growth is Angular's own `DomSanitizerImpl` — `MarkdownService` injects `DomSanitizer` — plus ~1.6 kB of global CSS, not the libraries.

Also fixed here, per the risk table: the coalescing and head/tail split in US-405 and US-602. Both have unit-level criteria that run against fixtures with no backend; getting them wrong is a rewrite of the chat surface.

## P4 — Chat completeness

| Order | Story                                                           | Depends on     | Pri | Status                                                                                     |
| ----- | --------------------------------------------------------------- | -------------- | --- | ------------------------------------------------------------------------------------------ |
| 32    | US-305 Favorite a conversation                                  | US-302         | P1  | ✅ 2026-08-13 — taken early, with P3's rendering stories; retires US-308's header star     |
| 33    | US-408 Recover when the conversation is already busy            | US-405         | P1  | ✅ 2026-08-13 — frame `1h`'s row variant; a focus defect on repeated Retry fixed with it   |
| 34    | US-409 See the name the server gave my conversation             | US-406, US-302 | P1  | ✅ 2026-08-13 — taken after US-410, see below                                              |
| 35    | US-410 Resume a conversation with the settings it last used     | US-402, US-403 | P1  | ✅ 2026-08-13 — ⚠ re-sized S → M: history replay shipped here                              |
| 36    | US-412 Understand an MCP consent requirement                    | US-403, US-103 | P1  | ✅ 2026-08-13 — interim variant; consent action still waits on US-411                      |
| 37    | US-413 Dictate a prompt                                         | US-401         | P2  | ✅ 2026-08-13                                                                              |
| 38    | US-502 See nested work inside the activity that caused it       | US-501         | P1  | ✅ 2026-08-13 — render-side only; the filter is root membership, not `parentScopeId`       |
| 39    | US-503 Read the model's reasoning as it streams                 | US-501         | P1  | ✅ 2026-08-13 — the duration is measured client-side; the wire carries none                |
| 40    | US-504 See what a turn cost                                     | US-501         | P1  | ✅ 2026-08-13 — footer always visible until US-607; the activity strip shows what is known |
| 41    | US-505 Understand an activity that failed                       | US-501         | P1  | ✅ 2026-08-13 — mostly already structural; taken after US-502, which made it observable    |
| 42    | US-603 Copy a code block                                        | US-601         | P1  | ✅ 2026-08-13 — custom renderer + one delegated listener; `div`/`button` join the profile  |
| 43    | US-604 Read code in a theme that matches the page               | US-105, US-603 | P1  | ✅ 2026-08-14 — ⚠ no stylesheet swap; verification and gates instead, see below            |
| 45    | US-607 Copy a prompt or a response                              | US-601         | P1  | ✅ 2026-08-14 — retires US-504's always-visible footer                                     |
| 44    | US-605 Render diagrams and math without paying for them upfront | US-601, US-108 | P2  | ✅ 2026-08-14 — the custom-renderer branch again; diagrams appear at settle                |

US-305 was pulled forward and landed with P3, which is what unblocked the header star deferred out of US-308; the rest of the phase is untouched.

**EP-6 completed 2026-08-14, in the order US-604 → US-607 → US-605.** They are sequential rather than parallel for one mechanical reason: US-607 and US-605 both edit `assistant-turn.html`, and taking the smallest first meant the code-block chrome was settled before anything else touched it. US-701 shares no file with any of the three and was taken alongside them.

⚠ **US-604 does not swap a stylesheet, and will not.** The board fixes `--code-bg` and `--code-head` dark in _both_ themes and `check-tokens.mjs` enforces that parity, so there is one token-driven Prism palette and nothing to swap to — which is also why the story's third criterion is free. Agreed with the product owner before implementation. What the story shipped instead is the enforcement its fourth criterion needed: 26 measured code-surface pairs, including the head bar's hovered state and `--accent` at SC 1.4.11's 3:1, plus a check that the pair list has not fallen behind the stylesheets and a ban on importing a Prism theme — in `angular.json` as well as `src/`, since the `styles` array is the route a theme would most likely arrive by.

⚠ **US-605 renders a diagram at settle, not as it streams.** The tail's fences have had their info strings stripped, and re-laying-out an SVG on every flush of the head would fight both the batch cadence and US-606's `ResizeObserver`. A mermaid fence reads as a `mermaid`-labelled code block while the answer arrives — the same contract US-602 set for highlighting. It also declines `ngx-markdown`'s own integrations, as US-603 declined its clipboard directive: theirs mutate the shared renderer globally, interpolate the source unescaped, and offer no error hook, which puts the story's fourth criterion out of reach.

**EP-4 completed 2026-08-13, in two lanes rather than the numbered order.** US-408 and US-412 are two variants of one component and were taken together; US-410 preceded US-409 because both extend `TurnStore` and `ConversationStore`, and US-409's "was this the conversation's first turn" reads cleanest once replayed history exists to answer it. US-413 touches only the composer and was independent of all four.

⚠ **US-410 was an S that turned out to be an M.** Its third criterion is about a _refetched_ transcript, and nothing in the client had ever fetched one — reopening a conversation showed a blank screen. History replay shipped inside the story rather than becoming a new one, agreed with the product owner before implementation.

**EP-5 completed 2026-08-13, in the order US-502 → US-505 → US-504 → US-503, not the numbered one.** The three card stories share `ActivityCard`, so they are strictly sequential whatever order they take; US-505 follows US-502 because nesting is what makes "a failed child does not fail its parent" something a spec can see, and US-504's chevron is easier to place once the card's header is final. US-603 shares no code with any of them, but it adds a delegated listener to `Transcript` and cases to `transcript.spec.ts`, which is why it ran after rather than beside them — the only two files a parallel worktree would have collided on.

**Three of the five had no data behind part of their design**, and each resolved differently rather than by one rule: US-503 measures the reasoning duration client-side because a plausible number exists to measure; US-504 renders the activity strip with the duration alone, because that keeps the chevron live and the token columns arrive with the field; US-505 leaves frame `1f`'s failure-reason line unbuilt, because nothing on the wire could fill it and inventing copy is the fabricated status line US-406 refuses.

**The review round changed US-503 twice over.** Its first cut opened the timing window on the turn's _first_ `ReasoningDelta` — which is the server's seed, so the pill reported stream latency as thinking time — and hung the region inside a component that `showLiveTurn` keeps off screen until the first token, so a story called "read the model's reasoning **as it streams**" showed it only once streaming was over. Both are fixed, and the fix that resolved them together was to treat the seeded delta as what it is: a request-level status line, which US-406 already decided this app does not render. It is now measured out of the text by length and skipped by the clock, and the ridgeline hands the gap to the region exactly when the model starts reasoning in its own words.

## P5 — Library, files, projects · ✅ complete

| Order | Story                                                 | Depends on     | Pri | Status                                                                              |
| ----- | ----------------------------------------------------- | -------------- | --- | ----------------------------------------------------------------------------------- |
| 46    | US-701 Find a conversation by name                    | US-302         | P0  | ✅ 2026-08-14 — the screen and the URL contract; the `name=` search already existed |
| 47    | US-702 Page through my conversations                  | US-701         | P1  | ✅ 2026-08-14 — a query replaces rows, a page appends; two guards keep them apart   |
| 48    | US-703 Filter to my favorites                         | US-701, US-305 | P1  | ✅ 2026-08-14 — `?favorites=1`; an unstarred row leaves the filtered list           |
| 49    | US-704 Delete several conversations at once           | US-702, US-106 | P1  | ✅ 2026-08-14 — the request lives in the root actions store, not this route's       |
| 50    | US-705 Understand why conversations cannot be sorted  | US-702         | P2  | ✅ 2026-08-14 — the order is stated; interim until US-706                           |
| 51    | US-801 Attach files to a conversation                 | US-204, US-401 | P1  | ✅ 2026-08-14 — uploads start as early as an id exists; Send gates on them          |
| 52    | US-802 Watch an upload get processed                  | US-801         | P1  | ✅ 2026-08-14 — staged poll 500ms→5s; the POST is raw `XMLHttpRequest` (see below)  |
| 53    | US-803 Tell an expired upload apart from a failed one | US-802         | P1  | ✅ 2026-08-14 — a 404 is `unknown`, never a failure, and offers no Retry            |
| 54    | US-804 Download a document                            | US-802         | P1  | ✅ 2026-08-14 — signed link fetched on click, handed to a detached `<a download>`   |
| 55    | US-805 Understand why a file was rejected             | US-801, US-103 | P1  | ✅ 2026-08-14 — four refusal shapes, not two; ⚠ two board departures, see below     |
| 56    | US-901 Browse and search projects                     | US-104, US-106 | P1  | ✅ 2026-08-14 — server-side search, a drained set, a stated ceiling; no sort, no star |
| 57    | US-903 Create, rename, describe, and delete a project | US-901         | P1  | ✅ 2026-08-14 — one modal for create and edit; a delete releases its conversations    |
| 58    | US-904 Give a project standing instructions           | US-903         | P1  | ✅ 2026-08-14 — the detail screen and its tab routes; the warning rides canDeactivate |
| 59    | US-905 Manage a project's files                       | US-802, US-903 | P1  | ✅ 2026-08-14 — no new upload code at all; EP-8's `UploadTarget` is why               |
| 60    | US-906 Start a conversation inside a project          | US-401, US-903 | P1  | ✅ 2026-08-14 — ⚠ re-sized S → M: the composer moved to `shared/` behind a token      |
| 61    | US-307 Move a conversation into or out of a project   | US-304, US-901 | P1  | ✅ 2026-08-15 — deferred out of EP-3; one picker, five invokers, and not a `Menu`   |
| 62    | US-908 See and manage a project's conversations       | US-903, US-307 | P1  | ✅ 2026-08-15 — one filtered request; its interim ceiling never shipped             |

**EP-7 completed 2026-08-14, in the numbered order.** The four stories all edit the same three files, so they are sequential whatever order they take, and each one's shape depends on the one before it: US-704's selection has to survive a Load more, and US-705's "no client-side reordering" is only assertable once a partially loaded list exists. US-307 later added the project chip and the per-row kebab the frame also draws (row 61); US-706 remains a backend enabler on the parallel track (row 89).

**EP-8 completed 2026-08-14, in the numbered order.** The five stories are one feature — every one of them is a state the same chip can be in — so they were taken as a unit rather than sequenced. Documented in [docs/ui/file-attachments.md](../ui/file-attachments.md). Two decisions in it were not derivable from the criteria and are recorded there in full: **uploads start as early as a conversation id exists** and `TurnStore` gates the stream on `UploadStore.settled$()`, because retrieval attaches `document_search` from what the conversation _already_ holds and a file posted as the stream opens is invisible to the very turn that asked about it; and **the upload POST is raw `XMLHttpRequest`**, because `HttpEventType.UploadProgress` is emitted by `HttpXhrBackend` only and the app runs `provideHttpClient(withFetch())` — so frame `4l`'s transfer percentage is unobtainable through `HttpClient` here. That transport restates the auth interceptor's single forced-refresh replay via `authErrorDecision` and deliberately does **not** restate `retryInterceptor`, which would queue the file twice. Concurrency is capped at three transfers, because the origin's six connections are shared with the SSE stream and the status polls.

⚠ **Two deliberate departures from frame `4l`**, both because the board is wrong about the API rather than because the app disagrees with the design. The board's example copy names XLSX and CSV, which the API does not accept, and omits PPTX, which it does — so the supported set is built from `GET api/documents/file-extensions` and no string is quoted from the board. And the board draws Retry on its **unsupported** chip, which this app does not: re-posting a `.mov` produces the same refusal, the same reasoning `canRetry` in `core/errors` already records. Retry stays on the `failed` arm, where a second attempt can succeed. Neither story's criteria are weakened — US-802 ties Retry to `Failed`, and US-805 asks only that the unsupported chip name the extension and the supported set.

**No re-baseline.** The initial bundle measures **663.92 kB raw / 163.32 kB transfer** against 670 kB warn / 720 kB error. Everything EP-8 added rides the lazy `chat` chunk or `core/` code only chat imports, and the feature adds no dependency of any kind.

**EP-9's first five stories completed 2026-08-14, in the numbered order — the one epic in P5 that had to be.** US-903 needs a grid to be invoked from; US-904 builds the detail screen; and US-905 and US-906 both add to that screen's tab strip and its composer slot, which frame `4e` draws on one page. Two worktrees would have collided on the same three files for no gain. US-908 closed the epic on 2026-08-15, after US-307.

**A prep step came first, and it is the largest thing in the epic.** Frame `4e` puts the _shared_ composer on the project screen, but the component lived in `features/chat/` and injected the route-scoped `TurnStore` — and dependencies run one way. So `UploadStore` moved to `core/documents/`, the composer stack moved to `shared/composer/`, and what executes a prompt became the `COMPOSER_HOST` token: `TurnStore` on `/chat`, `ProjectComposerHost` on a project. Neither move touches the initial graph — the composer now sits in a chunk the two lazy routes share — and a `no-restricted-imports` rule was added so the layering that forced all this is machine-checked rather than remembered.

**Two decisions were not derivable from the criteria.** `beginEdit` **fetches the project before opening the form**, because `ProjectSummaryDto` omits `instructions` entirely and a form seeded from a card would clear them on a full-representation PUT. And a project delete **releases its conversations client-side** (`ConversationListStore.releaseProject`, and the same clear on `ConversationStore`), because the server does exactly that and `toUpdateBody` echoes `projectId` — a row left holding a deleted id would take a 404 on its next rename, a failure with nothing to do with projects.

**No re-baseline.** The initial bundle measures **667.05 kB raw / 168.02 kB transfer** against 670 kB warn / 720 kB error. The +3.1 kB is the events plugin's handler API reaching `ConversationListStore`, which is on the initial graph; everything else rides the lazy `projects` chunk or the composer chunk it shares with `chat`.

**US-307 and US-908 closed the phase on 2026-08-15, in that order, which was not optional.** US-908's row menu *is* US-307's five actions and its panel opens US-307's picker; the other order would have meant building the menu twice. Three decisions in them were not derivable from the criteria and are recorded in [docs/ui/projects.md](../ui/projects.md) in full. **The project picker is not a `Menu`** — `Menu` renders its own trigger, its panel is `role="menu"` (whose only valid children are `menuitem*`), and its keyboard handling swallows the arrows and closes on Tab, which is three collisions with a search field; it is a non-modal `role="dialog"` holding a listbox, borrowing only placement and dismissal, which were extracted so both share them. **A root `ProjectLookupStore` drains every project to the 500 ceiling**, because `ConversationDto` carries a `projectId` and no name, so no chip anywhere could render one without it — and unlike the projects grid it **keeps** the pages already loaded when a drain fails, since a lookup that can still name 200 of 500 projects beats one that names none. And **the project dialogs moved from `ProjectsLayout` to `Shell`**, which US-903 forecast: the picker's "New project" is the first invoker outside the projects feature.

**US-907 (B1) landed with them rather than during P3**, which is where the PRD scheduled it — earlier would have been better, but landing it in the same batch was enough to keep US-908's interim 500-item drain ceiling out of every build. See the interim-behaviours table below.

**No re-baseline, and 1.1 kB of headroom left.** The initial bundle measures **668.90 kB raw / 168.23 kB transfer** against 670 kB warn / 720 kB error. The +1.85 kB is `ProjectLookupStore`, which is root-scoped and released by `SessionBootstrap` — the same shape as EP-9's +3.1 kB, and the same lesson: **the next root-scoped store trips the warning line.** The picker, the dialogs and every chip they feed ride lazy chunks.

## P6 — Administration · ✅ complete — four tabs, the fourth of them a URL waiting on an API

| Order | Story                                         | Depends on     | Pri | Status                                                                            |
| ----- | --------------------------------------------- | -------------- | --- | --------------------------------------------------------------------------------- |
| 63    | US-1201 Find and page through users           | US-203, US-104 | P1  | ✅ 2026-08-16 — the layout route and the rail; no Last active column, see below   |
| 64    | US-1202 Create a user                         | US-1201        | P1  | ✅ 2026-08-16 — four rejections at one field, and only three are validation ones  |
| 65    | US-1203 Edit a user's profile and permissions | US-1201        | P1  | ✅ 2026-08-16 — not a Signal Form; the PUT echoes the profile it does not edit    |
| 66    | US-1204 Deactivate a user                     | US-1201        | P1  | ✅ 2026-08-16 — root store owns the request, the route owns the row               |
| 67    | US-1207 Manage the model catalog              | US-203         | P1  | ✅ 2026-08-16 — eleven fields where frame `5f` draws eight; a void event, and both observers refetch |
| 68    | US-1208 Manage MCP servers                    | US-203         | P1  | ✅ 2026-08-17 — five fields where frame `5h` draws six; every rejection kept on the surface renders in the panel |
| 69    | US-1209 Reach an admin tab by URL             | US-203         | P1  | ✅ 2026-08-17 — the fourth tab, shipped as frame `5i`'s unavailable panel; no store, by decision |

**EP-12's first four completed 2026-08-16, in the numbered order, which was not optional.** US-1202, US-1203 and US-1204 all hang off US-1201 and all three write the same three files — `AdminUsersStore`, `UserActionsStore` and `admin-users.html` — so two worktrees would have collided on every one of them, exactly as EP-7 and EP-9 did. Within that, the create modal came before the edit panel because the shared `PermissionChecklist` is simpler in frame `5b` (an inline caption, no lock) than in frame `5c` (a lock glyph, a block caption and a per-row refusal slot), and settling the smaller variant first is the same reasoning EP-6 used for the code-block chrome.

**The shared kit had been waiting for this epic.** `Paginator` ("the numbered pager of frame `5a`"), `Offcanvas` ("the admin detail drawer of frame `5c`", already defaulting to 420px with a sticky footer and focus return) and `PermissionBadge` ("in the admin user list and detail drawer") were all built by US-106 *against these frames* and had no consumer but `/ui-kit` until now. Between them they needed one page-size select, one `liveSummary` opt-out and nothing else. Two things did have to be built: `AvatarInitials`, whose four-tone ramp lives in the component rather than in `_tokens.scss` because it is the only consumer and that file is global; and the `AdminLayout` rail, which lists only the tabs that exist.

**US-1209 was smaller than it looked, and it closed the phase.** The layout route, the rail marking the active tab from the router, and the `/admin` → `/admin/users` redirect all landed with US-1201, because frame `5a` draws the rail as part of the users screen. US-1207 then added the models route and its rail entry in the same commit, and US-1208 the MCP route and `bi-plug` in the same commit again, on the rule that a tab is added by the story that builds it. What was left was the **fourth** tab — and that is where a deadlock had to be broken: this file and the docs page had both booked it to US-1302, which `Depends on: US-1209`. The rule is unchanged and still binds; US-1209 simply is the story that built the route, and frame `5i` ("Reports — unavailable panel (**current state**)", captioned "Required frame") is the design authority for shipping it that way. `/admin/reports` therefore renders an `<h1>` and the shared `UnavailablePanel`, with **no `ReportsStore` and no request** — a deliberate departure from US-1302's first criterion, which asks for a store holding zero records: with no endpoint it would hold no state and load nothing, and FR-49 is met more strongly by a tab that instantiates none. It is the panel's first production consumer; only `/ui-kit` had rendered it since US-106. Two smaller things outlive the story: the tab carries an `<h1>` frame `5i` does not draw, because `UnavailablePanel`'s own heading is a `<p>` bound through `aria-labelledby` and this would otherwise be the app's only routed screen with no level-one heading (US-1405's axe gate would find it), and **`/admin/<unknown>` now redirects to `/admin/users`** through a `**` child rather than backtracking out of the area to `/chat` — which means **a new admin route has to be added above that entry**, or it is unreachable with nothing to report it.

**US-1207 is the second tab, and it copies almost none of the first.** The two-store seam is the same — a root-scoped `ModelActionsStore` for the requests, a route-scoped `AdminModelsStore` for the rows — but the endpoint decides everything else: `GET api/models/all` is unpaginated, so the screen is **regime C** with no URL contract, no `bindQuery` and a filter that runs in memory, making it `withClientQuery`'s first production consumer (`ProjectListStore` had deferred it to US-902). Its `isAuthoritative` is `isFulfilled() || entities().length > 0` rather than `isFulfilled` alone, because every mutation refetches and the rows on screen are still the whole catalog while the next copy is on the wire. US-1208's tab is the one that copies **this** screen, not `AdminUsers`.

**US-1208 is the third tab, and it copies the second almost exactly.** Same seam (a root-scoped `McpServerActionsStore`, a route-scoped `AdminMcpsStore`), same **regime C** with no URL contract, same two shaped `withClientQuery` values including `isAuthoritative: isFulfilled() || entities().length > 0` — the second consumer of that feature was a copy rather than a re-derivation, which is what §10.2 of the docs page was written down for. It departs in exactly one place, and deliberately: **every rejection kept on the dialog's own surface renders in the modal's `--fail` panel, a validation problem included**, because the criterion asks for a panel that names the server on a 502 as well as on a 400 — a non-validation failure therefore raises no toast, since reporting the same fact twice is the defect. Four things about the API decide the rest: the write DTOs carry **no permission field** (the server creates and renames that permission itself), `McpAuthTypes` has **two** members where frame `5h` draws three and they travel as **numbers**, `Scope` is conditional in **both** directions, and `DELETE` cascades to the linked permission and every grant of it. `adminEvents.mcpCatalogChanged` is `type<void>()` and both observers refetch, mirroring `modelCatalogChanged`. **The criterion's 502 is unreachable on save** — `POST`/`PUT api/mcps` attempt no MCP connection — and the panel is built to render any error anyway.

**No re-baseline.** The initial bundle measures **669.78 kB raw / 168.95 kB transfer** against 670 kB warn / 720 kB error, `styles` unchanged at 63.96 kB and the lazy `admin-routes` chunk grown 70.23 → **95.94 kB** — **0.22 kB of headroom left**. It got there in three paying steps and then stopped: **+0.27 kB** for US-1201–1204 (the shared kit's), **+0.46 kB** for US-1207 (all of it `ModelCatalogStore`'s `modelCatalogChanged` handler, since that store is root-scoped and genuinely initial), **+0.15 kB** for US-1208 (`McpCatalogStore`'s handler, a third the price because `withEventHandlers` and the `adminEvents` symbol were already on the graph) and **+0.00 kB** for US-1209, whose whole tab — 1.10 kB of it — landed in the lazy chunk, because it introduces no event, no root-scoped store and no dependency. Everything else rides that chunk too, including all three root-scoped action stores: `providedIn: 'root'` is a DI scope, not a chunk assignment, and nothing eagerly reachable imports any of them. `check-initial-chunk.mjs` still reports the admin area absent from the initial graph. **One note for US-1302**, which is the next story to touch this chunk: `admin.routes.ts` names every tab with a static `component:` import, so a charting library imported by the reports screen would be downloaded by everyone who opens `/admin/users` — switching that one route to `loadComponent` is the cheap fix, and it is cheap at that point rather than now.

## P7 — Conformance · gates run against everything shipped in P1–P6

| Order | Story                                                           | Depends on              | Pri | Status    |
| ----- | --------------------------------------------------------------- | ----------------------- | --- | --------- |
| 70    | US-1401 Use the whole app from the keyboard                     | US-606, US-903, US-1201 | P0  | ✅ 2026-08-17 — a conformance story: two real gaps closed, the rest turned into gates |
| 71    | US-1402 Follow a streaming answer with a screen reader          | US-406, US-501          | P0  | ✅ 2026-08-17 — polite **and** busy; the commentary lives outside the container that would defer it |
| 72    | US-1403 Use the app on a tablet or a phone                      | US-301, US-1201         | P0  | ✅ 2026-08-18 — three modes, one breakpoint record, and two pre-existing bugs found on the way |
| 73    | US-1404 Respect a reduced-motion preference                     | US-106                  | P1  | ✅ 2026-08-18 — the block was already sufficient; the story gated it and closed the delay gap |
| 74    | US-1405 `[enabler]` Gate accessibility and budgets in the build | US-108, US-1401         | P1  | ✅ 2026-08-18 — axe in a real browser; it found a failure three stories had missed |

**US-1402 opened the phase, and it was smaller in code than in reasoning.** Its first and third criteria look contradictory — a polite live region over streaming text reads every delta — and are reconciled by ARIA's batching rule rather than by a throttle: the streaming answer's `.assistant-turn__content` is `aria-live="polite"` **and carries its own `aria-busy`**, released as an explicit `false` when the folded `Finished` lands, so the region accumulates and is exposed once. The settled turn that replaces it carries no `aria-live` at all — the guard is at the call site in `transcript.html`, which is why `transcript.spec.ts` pins it and the component's own spec cannot. The transcript's `aria-busy` moved to `historyPending() || (inFlight() && !answerComplete())`, one step ahead of `!inFlight()`, because criterion 4 names the **frame** and reading the frame is what leaves the live turn mounted while its region is released. Two things that look like extras are the load-bearing parts: the activity cards and the reasoning region are `aria-live="off"`, or releasing the answer would flush every card's state, kind, source and sub-status list at the reader first — and a second time, since the status region already narrated them live; and that status region is rendered by `Chat` **outside** the `aria-busy` container, because anything inside it is deferred with the answer. It speaks the new pure `domain/stream/turn-announcement.ts`, which meets criterion 3 by construction — every branch changes only on a status transition, the one branch reading `snapshot.text` reads it as a boolean, and the recorded twelve-event turn reduces to seven distinct strings with none repeated. Two interim behaviours came out of it and are in the table below. Criterion 2 needed no work: US-106's two toast regions already covered it. **No re-baseline** — the initial bundle is unchanged at **669.78 kB raw / 168.97 kB transfer** against 670 / 720 kB, `styles` unchanged at 63.96 kB, everything on the lazy `chat` chunk.

**US-1401 was a conformance story, not a build one, and its two gaps were the only code it had to write.** `shared/a11y/`, the native `<dialog>` overlays with focus return, the roving-focus `Menu` and the absence of a single clickable `<i>` or `<div>` meant three of the four criteria were largely already met; what they lacked was anything that would notice them breaking. **Gap one** was a code block that could not be scrolled without a pointer — `overflow: auto` with no `tabindex` (SC 2.1.1) — closed by emitting `tabindex="0"` from `renderer.code`, admitting `tabindex` to the sanitizer profile, and leaving the block **deliberately unnamed**, because `<pre>` maps to `role="generic"` whose name ARIA prohibits, and naming it properly costs a role and a wider profile to announce a scroll box. **Gap two** was five focus indicators under 3:1, one of which was every button in the app: `--ring` is `ringpulse`'s 45%-alpha colour and composites to about 1.4:1, and it was drawing the indicator on three composer controls, the empty-state chips and the composer card, beside two form-field rules using an 18%-alpha wash. A global `:focus-visible` floor in the new `_focus.scss` fixes the elements with no rule at all — and **explicitly not the three Bootstrap components**, which declare `outline: 0` on a class and out-specify a bare `:focus-visible` whatever the source order; the first cut of the work missed that and reached none of them. They are overridden by name, the checkbox with two rules whose order is load-bearing, and the ring is an `outline` with an offset rather than a flush shadow, because drawn on `--btnP-bg` it measures **1.00:1** in dark, where the button fill *is* the focus token. Both claims are now gates inside `npm run lint`: `check:tokens` measures **38** contrast pairs (up from 26) and refuses to fall behind the stylesheets in either direction, following variable indirection because that is how every Bootstrap fill is set; `check:forbidden` refuses a suppressed indicator and `var(--ring)` outside its keyframe, with a per-line hatch **scoped to those two rules** after a review round found an earlier cut applying it to all seven. One shared helper changed with it — `dialog-polyfill.ts` now queues its `close` event as the platform does, which is what makes `Modal`'s destroy-time focus restore falsifiable. Three limits came out of it and are in the table below. **No re-baseline, and 0.07 kB of headroom left**: the initial bundle measures **669.93 kB raw / 168.91 kB transfer** against 670 / 720 kB and `styles` **64.17 kB** against 65 / 80 kB, the +0.15 kB entirely global CSS — dropping Bootstrap's unused `helpers/focus-ring` import paid back most of the override's cost. **US-1405 owns budgets and is where that line should be re-stated**; this story did not move it.

**US-1403 was the phase's only build story, and half of it was a record rather than a screen.** Criterion 1 already held, and so did criteria 4a and 4b — `DataTable` collapses to cards and `PillSubnav` exists — while **criterion 5 is vacuous**, since `/admin/reports` is an unavailable panel until US-1302 and has no charts to stack. What shipped is frames `3g`, `3e` and `1d`, plus the thing that makes any of them checkable: **twelve stylesheets carried six spellings of the two numbers the boards restructure at**, and the mismatched pair (`767px` beside `768px`) left a 0.98px band matching neither mode — reachable by browser zoom or by the Windows display scaling this app ships on. The two edges are now stated once per language, with a `check:forbidden` rule refusing a third copy and a `check:tokens` parity check covering both the minima and the fractional maxima. `Sidebar` became a **controlled component** because the same chevron persists a width at desktop and opens a transient overlay at tablet; the persisted preference is therefore desktop-only, and the overlay is a `linkedSignal` on the shell rather than a field on `UiStore`, since every write there goes to `localStorage` and deriving it makes the dismissal atomic with the mode read (an `effect` would land one pass late, and a mobile drawer would momentarily read as a tablet overlay with `inert` on `<main>`). Frame `3g`'s "the main content never reflows" is **structural**: the panel leaves the flow and a rail spacer holds the 60px track, so the flex row is the same open or closed. Frame `3e` reuses `Offcanvas` with a new `bare` input that **clips** its heading rather than hiding it, because that heading is the dialog's accessible name — and there is exactly **one** `<app-sidebar>` declaration rendered at one of two outlets, because both of its branches emit the same `id` and the same landmark name. The mobile navbar's seam is `ShellChrome`, mirroring `COMPOSER_HOST`, and it **retires US-308's last deferred criterion**. `anchoredPosition` gained a viewport and a clamp, which fixes a **pre-existing** defect at every width. The project-detail tabs became the pill strip rather than frame `4e`'s accordions — a product-owner-approved departure, because those tabs are real routes and one can refuse to navigate. Two pre-existing bugs were fixed on the way and are worth naming: the projects grid used Bootstrap `row`/`col-*` classes that **never existed** in this build, so it had been a block stack at every width, and four routed hosts were being clipped below the fold by `.shell__main { overflow: hidden }`.

**US-1404's output was a finding, not a change.** The global block in `_motion.scss` **already covers every animation site correctly**, verified end-state by end-state: floored to 0.01ms, `spin` settles where it started and both `ringpulse` sites settle with no shadow while losing nothing legible. **No redundant `animation: none` overrides were added**, and the four sites that carry a local override are the four whose floored end-state would be wrong. Three of the four criteria name motion the app does not have — no typewriter, no toast slide (`@angular/animations` is not a dependency), no message-appearance fade-slide — and §5's "200ms for message appearance" **describes an unimplemented affordance**, which `_motion.scss` now records rather than closing: adding an animation inside the story whose purpose is to suppress animation is the wrong direction, and a fade-slide on insertion would disturb US-1402's live-region contract. The code change is two declarations — `animation-delay: 0s` and `transition-delay: 0s`, because a delay survives a floored duration and reappears as a late jump, a gap Bootstrap's reboot leaves open. Two gates and one spec now hold the contract: no fifth keyframe (word-bounded, so `@keyframes spinner` cannot pass as `spin`), keyframe **parity** against `theme.css`, and `motion.spec.ts` pinning the block, the `@import 'motion'` last position in `styles.scss` that everything rests on, and that no `animation:` names an undefined keyframe.

**US-1405 closed the phase, and three of its four criteria were already met** — the initial-chunk guard since US-601/US-605, the mutated-object criterion by `upload-store.spec.ts`, and the contract diff by an existing CI job. The work was the axe run, and two of its configuration lines are the whole story. **A real browser and not jsdom**, because jsdom computes no style and reports every colour rule as `incomplete` — a suite that passes and means nothing. And **`setupFiles: []`**, because `setup-dom.ts`'s `matchMedia` fake would shadow the real one and answer `false` to every media query. A third, `browserViewport: "1280x900"`, is why the run found anything at all: Vitest defaults to 414×896, where the sidebar, the tables and the rail are not rendered. **Two AA failures came out of it**, both fixed **upstream in `docs/design/project/theme.css`** — the first changes this project has made to the design bundle, and necessarily so, since `check:tokens` enforces parity and blocks a local override. Light `--warn` `#A96A10` → `#9C6210` closes the §9 question; dark `--active-bg` `#173A58` → `#16354D` was found by the run rather than the table, at **4.29:1** for `--brand` on the admin rail's active tab, the sidebar's active nav link and the paginator's current page — invisible for three stories because the surfaces guard measures the focus ring against a background, never the text on it. `CONTRAST_PAIRS` is at **46**, with a new `under:` option so a token translucent in one theme and opaque in the other is measurable in both. **Budgets re-baselined, as US-1401 said this story should**: **670.33 kB raw** against 675 / 720 kB, `styles` **64.47 kB** against 66 / 80 kB — the old 0.07 kB of headroom was a build that fails on the next comment. CI runs it as **two steps inside `Lint, test, build`**, after `Build` because it is the most expensive thing in the job, so **no check name changed and branch protection needs no change**. Four interim behaviours came out of the phase and are in the table below.

## P8 — Enabled features · each enabler releases a frontend story

| Order | Story                                                                            | Depends on             | Pri | Status                                    |
| ----- | -------------------------------------------------------------------------------- | ---------------------- | --- | ----------------------------------------- |
| 75    | US-1001 `[enabler]` List the documents belonging to a conversation (B2)          | —                      | P1  | ✅ 2026-08-19 — both routes; the fetch question answered by adding `GET api/documents` |
| 76    | US-1002 Browse every document I have uploaded                                    | US-1001, US-106        | P1  | ✅ 2026-08-19 — straight to the enabled screen; frame `4i`'s panel never shipped |
| 77    | US-1003 Filter documents and group them by conversation                          | US-1002                | P2  | ✅ 2026-08-19 — grouped is the default; the URL records only `?view=flat` |
| 78    | US-1004 `[enabler]` Distinguish model-created documents (B7)                     | —                      | P2  |                                           |
| 79    | US-1005 Filter to documents the assistant created                                | US-1004, US-1002       | P2  |                                           |
| 80    | US-1101 `[enabler]` Put a message identity on the transcript and the stream (B4) | —                      | P1  | ✅ 2026-08-21 — `Andes.Extensions.AI.UI` 0.8.0 published the day before, closing issue #14; `Finished` now held back until the transcript write answers |
| 81    | US-1102 `[enabler]` Record feedback on an assistant message (B5)                 | US-1101                | P1  | ✅ 2026-08-21 — dual write to Cosmos and SQL, withdrawal added beyond the written criteria |
| 82    | US-1103 Rate an assistant response                                               | US-1102, US-607        | P2  | ✅ 2026-08-21 — landed the same day as its enabler; a fourth footer-reveal selector |
| 83    | US-1301 `[enabler]` Expose the usage audit trail over HTTP (B6)                  | —                      | P2  | verify against `ConversationUsage` first  |
| 84    | US-1302 See what the platform is spending                                        | US-1301, US-1209       | P2  | inherits the route; creates `ReportsStore` |
| 85    | US-1501 `[enabler]` Expose conversation export over HTTP (B11)                   | —                      | P2  | ✅ 2026-08-19 — PDFsharp/MigraDoc, response body not a signed URL |
| 86    | US-1502 Download a conversation                                                  | US-1501, US-106        | P2  | ✅ 2026-08-19 — one menu, two triggers, absent-then-disabled |
| 87    | US-909 `[enabler]` Support project favorites (B3)                                | —                      | P2  | ✅ 2026-08-20 — seventh migration, no new index |
| 88    | US-910 Pin favorite projects to the sidebar                                      | US-909, US-907, US-301 | P2  | ✅ 2026-08-20 — device-local pins never built; server flag from the start |
| 89    | US-706 `[enabler]` Accept sort parameters on the paginated list endpoints (B8)   | —                      | P2  | ✅ 2026-08-20 — releases US-705's disabled sort; retires US-901's too |
| 90    | US-902 Sort projects, honestly                                                   | US-901                 | P2  | ✅ 2026-08-20 — the "pinned" criterion was already server-flag-only |
| 91    | US-1205 `[enabler]` Filter user search by permission (B10)                       | —                      | P2  | ✅ 2026-08-20 — released the next day by US-1206, row below |
| 92    | US-1206 Filter users by permission                                               | US-1205, US-1201       | P2  | ✅ 2026-08-21 — first criterion never built, deliberately (interim caption superseded before it could render) |
| 93    | US-411 `[enabler]` Carry the consent scope on the MCP authorization problem (B9) | —                      | P2  | upgrades US-412                           |
| 94    | US-412 revisit — consent scope rendered from the problem extension               | US-411                 | P1  | not a separate story                      |

**US-1302 is the one story here that inherits rather than creates.** `/admin/reports` already exists, is reachable by URL and is marked in the rail (US-1209, P6); this story replaces what the panel occupies and adds `ReportsStore` — which does **not** exist yet, deliberately, against the literal wording of its own first criterion. See the interim-behaviours table below.

**These enablers are a parallel track, not a final phase.** Of the ten below, US-907, US-909, US-706, US-1205, US-411 and US-1101 have no dependencies of their own, and all but US-411 have now landed. US-1102 waited on US-1101 and landed the day it cleared; US-1004 is the last of the ten still open. **US-1101 was the exception that proved blocked can mean temporarily**: it was stuck not on another enabler but on an upstream package release (`Andes.Extensions.AI.UI` 0.8.0, issue #14), and that release published to nuget.org on 2026-08-20; US-1101 took it up the next day, 2026-08-21, shipping whole — both halves (the `usage` projection and the `Finished` frame's `assistantMessageId`) together rather than the `usage` half alone as the interim plan had it. Landing one early removes an interim behavior rather than adding a feature — **US-907 is the proof**: it shipped with US-908 rather than in P3 as scheduled, and that was still early enough for US-908's interim drain ceiling never to reach a build. **US-1501/US-1502 are the second proof**: both landed together on 2026-08-19, closing the renderer-stack question below along with the two interim rows it left in the table. **US-909/US-910 are the third**: the pair landed together on 2026-08-20, so US-910's own interim criterion — device-local pins in `localStorage` for the case US-909 had not shipped yet — was never built. **US-1205/US-1206 are the fourth, a day apart rather than together** (2026-08-20 and 2026-08-21) — but with no release cut in between, US-1206's board-drawn "Filters loaded rows only." caption never reached a build either, on the identical logic. The migration question that gated the track is now settled for B3: it took a seventh migration, `20260820050001_AddProjectIsFavorite`, an ordinary `AddColumn`; B4 needed none, since the usage row's `AssistantMessageId` has existed since `InitialCreate`; B5 took an eighth, `20260821203334_AddMessageFeedback`, a whole new table rather than a column, because its rating is projected relationally as well as stored on the transcript document; B7 (US-1004) is the one still open.

---

## Deferred and interim behaviors to retire

Each of these ships deliberately incomplete and is closed later. Kept here so none of them quietly becomes permanent.

| Interim behavior                                                                                                                                                 | Shipped in                                  | Retired by                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| ~~Header star absent~~                                                                                                                                           | US-308 (P2)                                 | ✅ retired by US-305, 2026-08-13                                                                                                     |
| ~~Two kebab items (move to / remove from project) absent~~                                                                                                       | US-308 (P2)                                 | ✅ retired by US-307, 2026-08-15 — in the sidebar row's kebab and the chat header's, with "Remove from project" **absent rather than inert** on a standalone conversation |
| Tools rows show the server name without frame `2c`'s mono key — `McpDto` carries no key field                                                                    | US-403 (P3)                                 | a backend enabler adding a key, if ever prioritized                                                                                  |
| ~~409-busy and MCP-consent errors render the generic turn-error notice~~                                                                                         | US-406/407 (P3)                             | ✅ retired by US-408 and US-412, 2026-08-13                                                                                          |
| ~~`composer--streaming` dims `.composer__aux` — the microphone, the attach control and the project pill carry it; the conversation download does not exist yet~~ | US-407 (P3) | ✅ retired by US-1502, 2026-08-19 — the download control takes `composer__aux` and dims with the rest (US-413 did, 2026-08-13; US-801 did, 2026-08-14; US-307 did, 2026-08-15) |
| ~~Chat header does not collapse into frame `1d`'s 54px mobile navbar below 768px~~                                                                               | US-308 (P2)                                 | ✅ retired by US-1403, 2026-08-18 — the star and kebab move **into** the navbar through `ShellChrome` rather than disappearing with the bar, and the `<h1>` is clipped rather than hidden, because it is the document's only level-one heading |
| ~~Upload gating verified against a test host, not the composer~~                                                                                                 | US-204 (P2)                                 | ✅ retired by US-801, 2026-08-14 — the paperclip and the prompt box's drop zone are both bound to `canUploadFiles()`                 |
| No way to detach a document once its upload succeeded — the API has a `DELETE` for **project** documents only, so a conversation chip's `×` renames itself from "Remove" to "Dismiss" and hides the chip without detaching anything | US-801 (P5) | a backend enabler adding `DELETE api/documents/conversations/{id}/{documentId}`, if ever prioritized |
| MCP consent card with no action, because the scope to request one is not on the wire                                                                             | US-412 (P4)                                 | US-411 (P8)                                                                                                                          |
| Failed activity cards carry no reason, because `ActivityFailed` sends none                                                                                       | US-505 (P4)                                 | a backend enabler putting failure text on the event, if ever prioritized                                                             |
| The activity cost strip shows `duration` only — no server attributes tokens per activity                                                                         | US-504 (P4)                                 | a contract upgrade populating `AssistantActivity.usage`                                                                              |
| ~~The message footer is always visible rather than revealed on hover and focus~~                                                                                 | US-504 (P4)                                 | ✅ retired by US-607, 2026-08-14                                                                                                     |
| ~~The library shows the first page only — no counter, no Load more~~                                                                                             | US-701 (P5)                                 | ✅ retired by US-702, 2026-08-14                                                                                                     |
| ~~The library offers no favourites filter, and no row star~~                                                                                                     | US-701 (P5)                                 | ✅ retired by US-703, 2026-08-14                                                                                                     |
| ~~The library offers no row selection or bulk bar~~ (the **per-row kebab** was always US-307's)                                                                   | US-701 (P5)                                 | ✅ selection and the bulk bar retired by US-704, 2026-08-14; the kebab, with the project chip, by US-307, 2026-08-15                 |
| The library's row star is confirm-then-render rather than optimistic — `ConversationActionsStore` cannot patch the library's own entities, so a flip would have no rollback behind it | US-703 (P5)                                 | nothing planned; revisit only if the two stores ever share an entity surface                                                          |
| Diagrams appear when a turn settles, never as it streams                                                                                                         | US-605 (P4)                                 | nothing planned — re-laying-out an SVG per flush is the cost this avoids                                                             |
| The code-head Copy control has no `bi-copy` glyph — the sprite needs `svg`/`use` in the sanitizer profile                                                        | US-603 (P4)                                 | nothing planned; revisit only if the profile widens for another reason                                                               |
| ~~A code block that overflows its column scrolls by pointer only — the `<pre>` has no `tabindex`, so keyboard users cannot reach the rest of the line (WCAG 2.1.1)~~ | pre-dates US-603, which now owns the chrome | ✅ retired by US-1401, 2026-08-17 — `renderer.code` emits `tabindex="0"` and `tabindex` joined `CHAT_ALLOWED_ATTR`, but **without** the name that entry once anticipated: `<pre>` is `role="generic"`, whose name ARIA 1.2 prohibits, so the Copy control beside it carries it instead |
| Replayed history carries no activity timeline, since the stream is never persisted                                                                               | US-410 (P4)                                 | nothing planned — a server-side turn record would be a new enabler                                                                   |
| The `aria-busy` release that exposes the streamed answer is **unverified against a real screen reader** — it is an attribute change with no content mutation behind it, so it rests on the reader replaying what it buffered, and no unit test can observe that (jsdom has no accessibility tree) | US-1402 (P7) | a manual pass with NVDA, JAWS and VoiceOver; until then `turnStatus`'s `Answer ready` is the deliberate backstop, and removing it is what would make this a silent failure |
| A `TextDelta` framed **after** `Finished` would be announced on its own — `answerComplete` trusts the frame to be the last that matters, which the streaming contract supports and the vendored fold does not enforce, since it keeps appending text to a `Completed` snapshot | US-1402 (P7) | nothing planned — the alternative is dropping those deltas, which loses answer text to protect an announcement; revisit only if a provider is found that frames them. **Less reachable since US-1101** (P8): the service now holds the middleware's own `Finished` back and re-yields it only after the transcript write answers, so `Finished` is genuinely the *last* frame this application ever sends on a completed turn — a `TextDelta` framed after it would have to come from the middleware itself, which the streaming contract still does not forbid and the vendored fold still does not enforce |
| ~~Conversation and project sort stated rather than offered — the library's toolbar reads "Newest first" and carries the reason; there is no sort control~~          | US-705 (P5)                                 | ✅ retired by US-706, 2026-08-20 — `ORDER_EXPLANATION` and the two toolbar elements it lived in are gone, replaced by a select issuing `sort=`/`dir=`              |
| ~~No sort control on the projects grid; a truncation line states the ceiling in frame `4g`'s wording rather than frame `4d`'s, which names sorting~~                  | US-901 (P5)                                 | ✅ retired by US-902, 2026-08-20 — the select is never disabled at the ceiling, since the order is now the server's; the notice states the truncation and names the order instead                                                                                                          |
| ~~The project card's favourite star is absent — `ProjectSummaryDto` carries no flag~~                                                                              | US-901 (P5)                                 | ✅ retired by US-909, 2026-08-20 — the star turned on there, on the detail header (US-904's deferred criterion) and, with it, the whole **Favorite projects** section in the sidebar (US-910) |
| ~~The project dialogs mount on `ProjectsLayout` rather than the shell, because both invokers are still inside the feature~~                                       | US-903 (P5)                                 | ✅ retired by US-307, 2026-08-15 — and it retired `ProjectsLayout`'s destroy-time cancel with them; a dialog dismissed with browser Back now stays open over the next route, exactly as the conversation dialogs always have |
| ~~The project detail tab strip has no Conversations tab~~                                                                                                        | US-904 (P5)                                 | ✅ retired by US-908, 2026-08-15                                                                                                     |
| ~~The project composer has no project picker — the project is fixed by the route~~                                                                               | US-906 (P5)                                 | ✅ retired by US-307, 2026-08-15 — a pill with two arms: the picker on `/chat`, a static chip on a project's own screen, where the project **is** the route. Still absent on empty `/chat`, where there is no conversation to move |
| ~~`SessionBootstrap` still does not request projects (US-202's fourth criterion), because no root project list has a consumer yet~~                                | pre-EP-9                                    | ✅ retired by US-307, 2026-08-15 — `ProjectLookupStore` is the first consumer, feeding both the picker's list and every chip that has to name a `projectId` |
| **500-item drain ceiling with a visible notice — never shipped.** US-907 landed in the same batch as US-908, so the interim criterion was live for no build at all; the panel issues one filtered request | US-908 (P5) — not, in the end, shipped      | US-907, 2026-08-15                                                                                                                   |
| The users table has no **Last active** column, and the edit panel's identity line omits frame `5c`'s `· last active yesterday` — `UserDto` carries no timestamp of any kind, and `dateModified` moves only when an administrator edits the row, never when the user signs in | US-1201 (P6)                                | a backend enabler stamping a sign-in timestamp and exposing it on `UserDto`, if ever prioritized                                      |
| No sort control on the models table — frame `5e` draws none. Unlike the directory this set **is** sortable: the comparators are wired in the store and `canSort()` is true, so the absence is the template's | US-1207 (P6)                                | US-902's sibling story on this tab (P8), which is then a template change rather than a store change                                   |
| **No Restore for a retired model.** `DELETE` is a soft delete, the row stays in the table muted, and its kebab offers no items at all — there is no reactivate route, and `PUT` cannot clear `DateDeactivated` | US-1207 (P6)                                | a backend enabler exposing reactivation; until then a retired model can only be re-added under a new row                              |
| The **whole-number token rule is client-side only** — `ContextWindowSize` and `MaxOutputTokens` are `decimal(18,2)` and their validator carries only `GreaterThan(0)`, so a model seeded outside this screen can hold `400000.5` and the table will render it | US-1207 (P6)                                | a server-side rule, if ever prioritized; `formatContext` keeps one decimal precisely so the value is visible rather than rounded away |
| **No Restore for a deactivated MCP server.** `DELETE` is a soft delete, `GET`/`PUT`/`DELETE` on `{id}` all 404 on a deactivated row, and there is no reactivate route — so the row stays in the table muted and its kebab is **absent rather than disabled** | US-1208 (P6)                                | a backend enabler exposing reactivation, if ever prioritized; until then a deactivated server can only be re-registered under a new row, with a new linked permission everyone has to be re-granted |
| No sort control on the servers table — frame `5g` draws none. Like the models tab this set **is** sortable: `name` and `url` comparators are wired and `canSort()` is true, so the absence is the template's | US-1208 (P6)                                | US-902's sibling story on this tab (P8), which is then a template change rather than a store change                                   |
| The reports tab renders `UnavailablePanel` (frame `5i`) and no data — `GET api/reports/usage` does not exist, so the tab issues no request and has **no `ReportsStore`**, deliberately against US-1302's first criterion: an empty store would hold no state and load nothing, and FR-49 is met more strongly by a tab that instantiates none | US-1209 (P6)                                | US-1302 (P8), once US-1301 lands — it inherits the route, the rail entry, the title and the `<h1>`, and creates the store then       |
| **No dry-run validation, tool-surface view or health signal** — frame-adjacent capability an administrator wants when registering a server, and nothing routes to `McpToolProvider` or `McpClientCache` over HTTP today | US-1208 (P6)                                | [`docs/prd/mcp/mcp-server-integration.md`](mcp/mcp-server-integration.md)'s US-103, US-104 and US-405 respectively                    |
| **Single scope only** — `McpServer.Scope` is one `nvarchar(512)`, so the form binds one field and the note under it reads "One scope only."                                                                       | US-1208 (P6)                                | that PRD's US-101, whose `Scope` → `Scopes` rename is now a **breaking change to a shipped client** and has to carry the frontend change with it |
| **The suppressed-indicator scan reads `src/` only, not the compiled stylesheet** — which is exactly how Bootstrap's five `outline: 0` declarations passed the gate written to catch them, and how a future dependency's would | US-1401 (P7)                                | a follow-up running it over `dist/`'s `styles.css`; it needs a build step, and `check:forbidden` runs inside `npm run lint` with none |
| A focused `.btn` held down briefly regains Bootstrap's off-palette wash — `.btn:first-child:active:focus-visible` is `(0,3,0)` and out-specifies the override    | US-1401 (P7)                                | nothing planned — transient and cosmetic, and there is no persistent case: `.btn.active`, `.btn.show` and `.btn-check` appear nowhere in the app |
| **Every code block costs two tab stops** — the Copy control and the `<pre>` — unconditionally, including blocks that do not overflow, so an answer with six blocks puts twelve stops between the transcript and the composer | US-1401 (P7)                                | nothing planned; conditioning on overflow means measuring layout per block, which the renderer deliberately never does               |
| **US-1403's criterion 5 is vacuous** — "the reports charts stack into a single column below 1024px" has nothing to assert, because `/admin/reports` renders `UnavailablePanel` and no charts | US-1403 (P7)                                | US-1302 (P8), which builds the dashboard and inherits the criterion with it                                                          |
| ~~**`/documents` is absent from `AXE_ROUTES`**, so US-1405's first criterion is met for three of the four route groups it names — the fourth has no route to audit. The absence is asserted in `a11y-coverage.spec.ts`, so the gap is a failing test rather than a silent hole~~ | US-1405 (P7) | ✅ retired by US-1002, 2026-08-19 — the route, its `AXE_ROUTES` entry and `documents.a11y.spec.ts` landed together (36 browser audits with US-1003's flat-view pair), and the absence assertion was deleted exactly as its own comment demanded |
| Every documents row wears the `Uploaded` badge and the assistant-created filter is hidden — no origin discriminator exists anywhere in the data model, so `SourceBadge` is hard-coded at its call site | US-1002/US-1003 (P8) | US-1004 (B7) makes the badge data-driven; US-1005 then ships the filter |
| **The project-detail tab strip becomes `PillSubnav` below 768px, not frame `4e`'s stacked accordions** — a product-owner-approved departure: the three tabs are real child routes, one behind a `canDeactivate` guard that can refuse the navigation, so `aria-expanded` on a `routerLink` would announce a state the control neither owns nor can guarantee | US-1403 (P7) | nothing planned — revisit only if those tabs stop being routes |
| **PRD §5's "200ms for message appearance" describes an affordance the app does not implement** — a message is inserted with no transition and no animation. `_motion.scss` records this rather than inventing the animation inside the story about suppressing animation, and a fade-slide on insertion would disturb US-1402's live-region contract | US-1404 (P7) | nothing planned; it is a documentation-versus-implementation gap, and the document is the thing that is ahead |
| **`moderate` and `minor` axe violations are discarded rather than reported**, and `resultTypes: ['violations']` means `incomplete` is never populated either — so a rule axe could not decide is silent | US-1405 (P7) | nothing planned — deliberate: this is a gate, and a gate that prints advisories nobody reads trains people to skim its output |
| ~~Device-local project pins that do not sync~~                                                                                                                    | US-910's predecessor                        | ✅ never shipped — US-909 landed in the same batch as US-910, 2026-08-20, so the pins and their "Pinned on this device — pins don't sync." notice were live for no build at all; the sidebar reads the server flag through `ProjectLookupStore` from the start |
| ~~The sidebar's favourite projects inherit `ProjectLookupStore`'s 500-project drain ceiling, with no notice of its own — the picker's `ceilingNotice` already states the same shortfall, and a second wording for one limit would drift from it~~ | US-910 (P8)                                 | ✅ retired by US-706/US-902, 2026-08-20 — the lookup drains `sort=favorite&dir=desc`, so the sidebar's **Favourite projects** section is complete within the ceiling unless the reader has starred more than 500 projects, rather than best-effort                                       |
| The projects grid's order does not resettle when a star is toggled under Favourites first, or when a project is created under any order — both leave the affected row where it was inserted or already stood, and the true order lands on the next query | US-902 (P8)                                 | nothing planned — re-draining up to five pages under the reader's cursor for one click was judged the worse answer                                                       |
| The admin users tab still offers no sort control, though `GET api/users` has accepted `sort=`/`dir=` since US-706 — the gap is now the screen's, not the API's | US-1201 (P6)                                 | a follow-up on that tab; a template change over the store's existing shape, not a new store                                                       |
| `[busy]` on the projects and conversations search fields is raised by a sort re-query as well as a name search — recorded rather than separated, because telling the two apart means holding "which control started this request" in store state | US-706/US-902 (P8)                          | nothing planned — see the comment beside `[busy]` in `projects.html`                                                       |
| ~~Export control absent rather than an `UnavailablePanel`~~                                                                                                       | pre-US-1501                                 | ✅ retired by US-1501, 2026-08-19 — the control lives inside the composer, where a panel has nowhere to render, so it stays absent by design rather than becoming an `UnavailablePanel`; the row is retired because there is no longer a pre-US-1501 state for it to describe |
